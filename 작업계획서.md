# 기본폭탄 궁극기 작업계획서 (긴급/CSV 무변경)

## 1. 요구사항 요약

| 항목 | 내용 |
|---|---|
| 대상 폭탄 | 기본폭탄 (`BasicBomb`, ID `BO0001`) |
| 효과 | 발사 쿨타임 **80% 감소** |
| 지속시간 | **8초** |
| 적용 슬롯 규칙 | 기본폭탄이 위치한 슬롯만 적용 (Z에 있으면 Z만, X에 있으면 X만, 양쪽 모두 가능) |
| 다른 폭탄 보호 | 기본폭탄이 없는 슬롯에는 영향 없음 |
| **제약** | **백엔드 차트(CSV) 신규 추가 금지** — 기존 데이터만 사용 |

---

## 2. CSV 무변경 처리 방침 (핵심)

기존 궁극기들은 별도 ID(`BO0006`, `BO0008` 등)의 `BombStat`을 받아 `Cost`(궁극기 사용 시 차감 HP)를 가져오지만, **현재 모든 궁극기가 HP를 차감하지 않는 운영 상태**이므로 기본폭탄 궁극기도 동일하게 차감하지 않게 한다.

**채택안: 기본폭탄(`BO0001`) 자신의 `BombStat`을 그대로 재사용**
- `BasicBombUltimate.Init()`에서 `_bombStat = ItemDatabase.Instance.GetStat<BombStat>("BO0001")` 설정.
- 결과: `Ultimate.GetCost()` → `BO0001.Cost` 반환. 다른 폭탄들과 같은 무차감 동작을 자연스럽게 따라감.
- `Ultimate.GetCost()` virtual 변환은 **하지 않음** (다른 폭탄과 동일한 처리이므로 불필요).

---

## 3. 현재 시스템 활용 포인트 (변경 없음)

- 쿨타임 계산: `Player.cs:91-113` — `_zBombCooldownModifiers`, `_xBombCooldownModifiers` + `CalculateCooldown`. Multiplicative `value=80f` → `cooldown × (1 − 80/100) = × 0.2`.
- 모디파이어 추가/초기화: `Player.cs:1290-1311` — `BombCooldownReduction(slot, value, type)` 추가, `ResetBombCooldown()` 전체 클리어. **개별 제거 API 없음 → 보강 필요**.
- 슬롯-폭탄 매핑: `Player.cs:367-378` — Z=`EItemType.Bomb`, X=`EItemType.SubBomb`(없으면 메인폭탄 fallback).
- 기본폭탄 ID 상수: `BasicBomb.ID = "BO0001"`, `Player.BASIC_BOMB_ID = "BO0001"`.
- 궁극기 등록: `UltimateManager.prefab`의 `UltimateList` 인스펙터 항목.
- 사용자 트리거 흐름: `PlayerUltimateController.ExecuteUltimate()` → 게이지·HP 체크 → `_ultimate.ExcuteUltimate()` 호출 → 후처리(10초 쿨, 게이지 리셋, RPC 연출) 자동 진행.

---

## 4. 설계

### 4.1 새 클래스 `BasicBombUltimate : Ultimate`
**위치:** `Assets/02.Scripts/Bomb/UltimateBomb/BasicBombUltimate.cs`

```csharp
public class BasicBombUltimate : Ultimate
{
    private const string OWNER_ID = "BO0001";   // BasicBomb.ID
    private const float DURATION  = 8f;
    private const float REDUCTION_PERCENT = 80f; // Multiplicative 80%

    public override void Init()
    {
        _ownerBombID = OWNER_ID;
        _bombStat = ItemDatabase.Instance.GetStat<BombStat>(OWNER_ID); // 기본폭탄 스탯 재사용
    }

    public override void ExcuteUltimate()
    {
        _owner.ApplyBasicBombCooldownBuff(REDUCTION_PERCENT, OperationType.Multiplicative, DURATION);
    }
}
```

> 발사체/연출 인스턴스화 없음. 시각/사운드 연출은 `PlayerUltimateController`의 기존 `Rpc_UltimateProduction` 흐름(EventManager.Ultimate)에서 자동으로 처리됨 — 새로 추가할 것 없으면 그대로 두고, 보이는 게 너무 허전하면 별도 후속 작업.

### 4.2 Player에 시간제한 모디파이어 보강
기존 `BombCooldownReduction`은 추가만 가능하므로 **토큰 기반 추가/제거**로 최소 보강.

**모디파이어 자료구조 변경**
- 기존: `List<(float value, OperationType type)>`
- 변경: `List<(int id, float value, OperationType type)>` (또는 동일 필드를 가진 작은 struct)
- `CalculateCooldown`은 `id`만 무시 (시그니처 동일하게 유지).
- `BombCooldownReduction(slot, value, type)` 내부에서 자동 증가 ID 발급, **반환값으로 ID 전달** (기존 호출자는 반환값 무시 가능 → `LightweightCartridge` 호환).
- 신규: `RemoveBombCooldownModifier(BombSlot slot, int id)`.

**새 진입점 `ApplyBasicBombCooldownBuff`**
```csharp
public void ApplyBasicBombCooldownBuff(float value, OperationType type, float duration)
{
    bool zIsBasic = EquipedItemDict.TryGetValue(EItemType.Bomb, out var z) && z != null && z.ID == BASIC_BOMB_ID;

    bool xIsBasic;
    if (EquipedItemDict.TryGetValue(EItemType.SubBomb, out var sub) && sub != null)
        xIsBasic = sub.ID == BASIC_BOMB_ID;
    else
        xIsBasic = zIsBasic; // X슬롯이 비어 있으면 메인폭탄 fallback (Player.cs:367-371과 동일 규칙)

    int zId = zIsBasic ? BombCooldownReduction(BombSlot.ZSlot, value, type) : -1;
    int xId = xIsBasic ? BombCooldownReduction(BombSlot.XSlot, value, type) : -1;

    if (zId == -1 && xId == -1) return; // 둘 다 기본폭탄 아님 → 적용 대상 없음

    _basicBombBuffCts?.Cancel();
    _basicBombBuffCts?.Dispose();
    _basicBombBuffCts = new CancellationTokenSource();
    RemoveAfterAsync(zId, xId, duration, _basicBombBuffCts.Token).Forget();
}

private async UniTask RemoveAfterAsync(int zId, int xId, float duration, CancellationToken ct)
{
    await UniTask.Delay(System.TimeSpan.FromSeconds(duration), cancellationToken: ct);
    if (zId != -1) RemoveBombCooldownModifier(BombSlot.ZSlot, zId);
    if (xId != -1) RemoveBombCooldownModifier(BombSlot.XSlot, xId);
}
```

**정리(취소) 지점**
- `Player.OnDestroy` (있으면 합류) — `_basicBombBuffCts?.Cancel(); _basicBombBuffCts?.Dispose();`
- 라운드 종료 / 사망 처리 흐름(현재 `_ultimateController.OnRoundEnd` / `ResetUltimateChanceTimer` 호출 인근)에서 함께 취소.
- 단순화 우선이라면 OnDestroy 정리만으로도 일단 동작. 라운드 종료 시 잔존 버프 이슈는 테스트에서 확인 후 보강.

### 4.3 UltimateManager 등록 (Unity 에디터 작업)
- `Assets/03.Prefabs/Items/Bombs/Ultimates/BasicBombUltimate.prefab` 생성 (빈 GameObject + `BasicBombUltimate` 컴포넌트).
- `UltimateManager.prefab` 의 `UltimateList`에 위 프리팹 추가.

---

## 5. 작업 순서 (코드 기준)

| # | 작업 | 파일 |
|---|---|---|
| 1 | 모디파이어 자료구조에 ID 추가 + `BombCooldownReduction` 반환값 변경 + `RemoveBombCooldownModifier` 추가 | `Assets/02.Scripts/Player/Player.cs` |
| 2 | `ApplyBasicBombCooldownBuff` 메서드 + `_basicBombBuffCts` 필드 추가, `OnDestroy` 정리 | 동일 |
| 3 | `BasicBombUltimate.cs` 신규 작성 | `Assets/02.Scripts/Bomb/UltimateBomb/BasicBombUltimate.cs` |
| 4 | 프리팹 생성 + `UltimateManager.prefab.UltimateList` 등록 | Unity 에디터 |

---

## 6. 테스트 시나리오

1. Z=기본폭탄, X=다른폭탄 → 궁극 발동, **Z 쿨타임만 80% 감소**, 8초 후 원복.
2. Z=다른폭탄, X=기본폭탄(가능한 경우) → X만 감소.
3. Z=X=기본폭탄 → 양쪽 다 감소.
4. 어느 슬롯도 기본폭탄 아님 → 변화 없음 (HP/10초 쿨은 컨트롤러 정책상 진행될 수 있음).
5. 8초 만료 직전 사망/라운드 종료 → 토큰 취소로 잔존 모디파이어 없음.
6. 카트리지 쿨감 + 궁극 쿨감 동시 → 두 Multiplicative가 곱으로 누적되는지 확인.
7. ParrelSync 다중 클론에서 다른 클라이언트의 발동이 본인 쿨에 영향 안 주는지 확인.

---

## 7. 보류/후속 (이번 작업 범위 외)

- 신규 차트 ID(`BO0002`) 발급 및 정식 `Cost` 정의.
- 전용 시각 연출(파티클/사운드) 추가.
- 모디파이어 ID 토큰 방식의 다른 호출자 확장.
- 쿨감 cap(상한) 정책.

---

## 8. 리스크 (해소됨 / 잔여)

- ~~HP 차감~~ → **해소**: 다른 모든 궁극기와 동일하게 무차감 동작 (`BO0001.Cost` 그대로 사용).
- ~~X슬롯에 기본폭탄 장착 경로~~ → **해소**: 사용자 확인 완료, 정식 경로 존재.
- **잔여 — 모디파이어 자료구조 변경의 파급**: `_zBombCooldownModifiers` 직접 참조 코드가 추가로 있는지 확인 필요. 현재 검색 결과로는 `Player.cs` 내부와 `LightweightCartridge` 한 곳뿐 — 안전.
- **잔여 — 사망/라운드 종료 정리**: 우선 `OnDestroy` 토큰 취소만 적용. 실전에서 잔존 버프 이슈 발견되면 보강.
