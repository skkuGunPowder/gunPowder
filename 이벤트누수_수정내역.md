# 이벤트 누수 수정 내역

## 1. 문제 정의

라운드 초기화 또는 궁극기 사용 시 Player의 **CameraShake / 넉백이 누적**되는 버그.
- 첫 번째 게임은 정상 진행
- 게임 종료 후 또는 다음 게임에서 효과가 점점 강해지거나 다중 발화

## 2. 원인 분석

세 가지 누수 카테고리를 발견.

### A. PlayerEventManager (DontDestroySingleton) 구독 누수
- `PlayerEventManager._playerEvents` 딕셔너리는 씬 전환에도 유지
- ActorNumber는 같은 Photon 세션 내내 동일하므로 같은 키로 PlayerEvents 객체가 재사용됨
- 이전 라운드에 등록된 핸들러가 다음 라운드에 정리되지 않으면 누적

### B. CameraController.SetTarget의 Unity-null 함정
```csharp
if (_target != null) {  // 파괴된 GameObject도 null로 평가됨 → 블록 스킵
    oldEvents.OnHit -= HitShake;
    ...
}
```
이전 Player가 이미 파괴된 상태에서 SetTarget이 호출되면 이전 구독이 그대로 남고 새 구독이 또 추가됨.

### C. EventManager 누수 (DontDestroySingleton)
- `DamageChecker.OnGameStart` 구독을 어디서도 해제하지 않음 (확정)
- 일부 컴포넌트가 SetActive 토글로 재사용될 때 Start 재호출에 따른 중복 구독 가능성

## 3. 인과 사슬

```
PlayerBaseState.OnHit 구독 누수
  → 한 번의 OnHit invoke에 HandleHit가 N번 호출
  → SyncStateChange<PlayerDamagedState>()가 N번 RPC 송신
  → PlayerDamagedState.OnEnter가 N번 진입
  → ApplyKnockbackEffect + ApplyHealthBasedForce가 N번 누적
  → Rigidbody2D.AddForce N번 → 넉백 폭주
```

```
CameraController.OnHit/OnAttack 구독 누수
  → HitShake/GunShotShake가 N번 호출
  → ProCamera2DShake.Shake가 N번 누적 발화 → 카메라 흔들림 누적
```

## 4. 수정 내역 (Before / After)

### 4.1 `Assets/02.Scripts/Camera/CameraController.cs`

| 항목 | Before | After |
|---|---|---|
| 추적 | `Player _target` 만 | `_target` + `int _subscribedActorNumber` 캐시 추가 |
| `SetTarget()` 해제 조건 | `if (_target != null)` (Unity-null이면 스킵됨) | `if (_subscribedActorNumber != -1)` (정수 비교) |
| `SetTarget()` 추가 가드 | 없음 | `if (player != null)` 가드 |
| `OnDisable()` 해제 조건 | `if (_target != null)` | `if (_subscribedActorNumber != -1)` |

### 4.2 `Assets/02.Scripts/Player/State/PlayerBaseState.cs`

| 항목 | Before | After |
|---|---|---|
| 추적 필드 | 없음 | `int _subscribedActorNumber`, `bool _isHitSubscribed` 추가 |
| `OnEnter()` | 바로 `OnHit += HandleHit` | `UnsubscribeHit()` 호출 후 `+=` (중복 구독 방어) |
| `OnExit()` | 직접 `OnHit -= HandleHit` | `UnsubscribeHit()` 통합 호출 |
| `OnDestroy()` | 없음 | **신규 추가** — `UnsubscribeHit()` 안전망 (FSM이 OnExit를 호출하지 않는 경로 대비) |
| `UnsubscribeHit()` | 없음 | 신규 헬퍼 — 캐시된 actorNumber 사용 |

### 4.3 `Assets/02.Scripts/Player/Player.cs`

| 위치 | Before | After |
|---|---|---|
| `Awake()` `OnSpawned += ApplyEquippedCartridges` | `+=` 만 | `-=` 후 `+=` (idempotent) |
| `Start()` `_playerStat.OnHPEmpty/OnHPIncreased` | `+=` 만 | `-=` 후 `+=` |
| `Start()` `EventManager.OnPlayerItemChanged` | `+=` 만 | `-=` 후 `+=` |
| `Start()` `EventManager.OnGameOver` | `+=` 만 | `-=` 후 `+=` |

### 4.4 `Assets/02.Scripts/UI/UI_Ultimate.cs`

| 항목 | Before | After |
|---|---|---|
| 추적 필드 | `Player _player` 만 | `_player` + `int _subscribedActorNumber` 추가 |
| `FindAndSubscribeToPlayer()` 진입 시 이전 구독 해제 | **없음** | 캐시된 actorNumber로 이전 구독 해제 후 새 구독 |
| `OnUltimateGaugeChanged` 구독 | `+=` 만 | `-=` 후 `+=` |
| `OnDestroy()` 해제 조건 | `if (_player != null)` (Unity-null 위험) | `if (_subscribedActorNumber != -1)` (정수 비교) |

### 4.5 `Assets/02.Scripts/Photon/DamageChecker.cs` 🔴 **확실한 누수 수정**

| 항목 | Before | After |
|---|---|---|
| `Start()` 구독 | `OnPlayerChanged += LeftPlayer; OnGameStart += SetPlayerView;` | 모두 idempotent `-=` 후 `+=` |
| `OnDisable()` | `OnPlayerChanged -= LeftPlayer;` 만 | **`OnGameStart -= SetPlayerView` 추가** + null 체크 추가 |

→ 게임당 GameStart +1 stale 누수의 가장 확실한 원천을 차단.

### 4.6 `Assets/02.Scripts/Map/UI/GameOverProduction.cs` (방어)

| 항목 | Before | After |
|---|---|---|
| `Start()` `OnGameOver / OnLastDieComplete` | `+=` 만 | `-=` 후 `+=` |
| `OnDestroy()` | 없음 | **신규 추가** — 두 이벤트 모두 `-=` 안전망 |

→ GameOver 핸들러가 한 번도 실행되지 않은 채 오브젝트가 파괴되는 경로 (예: 도중 씬 전환) 대비.

### 4.7 `Assets/02.Scripts/UI/KillLog/UI_IngameChatPopup.cs` (방어)

| 항목 | Before | After |
|---|---|---|
| `Start()` `OnPlayerColorChanged / OnGameStart / OnGameOver` | `+=` 만 | `-=` 후 `+=` |

→ Popup 인스턴스가 SetActive 토글로 재사용될 때 Start 재호출 시 중복 구독 방지.

### 4.8 `Assets/02.Scripts/Gimmick/Core/GimmickManager.cs` (방어)

| 항목 | Before | After |
|---|---|---|
| `Start()` `OnGameStart += OnGameStart` | `+=` 만 | `-=` 후 `+=` |

### 4.9 `Assets/02.Scripts/Map/UI/GameStartProduction.cs` (방어)

| 항목 | Before | After |
|---|---|---|
| `Start()` `OnGameStart += GameStart` | `+=` 만 | `-=` 후 `+=` |

## 5. 검증용으로 추가했던 디버그/도구 — 커밋 전 제거 완료

검증 단계에서 다음 도구들을 임시로 추가했고, 검증이 끝난 후 모두 제거함.

| 위치 | 용도 | 상태 |
|---|---|---|
| `PlayerEvents.GetSubscriberCount(string)` | 단일 PlayerEvents 이벤트 구독자 수 반환 | ❌ 제거됨 |
| `PlayerEventManager.DumpAllSubscriberCounts(string)` | 모든 actor의 8개 주요 이벤트 일괄 덤프 | ❌ 제거됨 |
| `EventManager.GetSubscriberCount(string)` | 단일 EventManager 이벤트 구독자 수 | ❌ 제거됨 |
| `EventManager.DumpSubscriberCounts(string)` | 12개 주요 이벤트 일괄 덤프 | ❌ 제거됨 |
| `CameraController.SetTarget` `[ShakeDiag]` 로그 | 진입 시점 추적 | ❌ 제거됨 |
| `CameraController.HitShake/GunShotShake` `[ShakeDiag]` 로그 | 핸들러 발화 횟수 추적 | ❌ 제거됨 |
| `Player.InvokeAttack/RegisterHitDamage` `[ShakeDiag]` 로그 | invoke 발화 추적 | ❌ 제거됨 |
| `PlayerSpawner.GeneratePlayers` BEFORE/AFTER 자동 덤프 호출 | 라운드/게임 단위 누수 추이 | ❌ 제거됨 |

## 6. 검증 결과

### 6.1 PlayerEventManager — ✅ 안정화됨

| actor | 안정 상태 카운트 |
|---|---|
| OnHit | 1 |
| OnAttack | 1 |
| OnSpawned | 1 |
| OnUltimateChanceActivated/Deactivated | 1 |

게임/라운드를 거듭해도 **1로 유지**. 카메라쉐이크/넉백 정상 복귀 확인.

### 6.2 EventManager — 일부 누수 잔존 (수정 후 재검증 필요)

검증 로그에서 발견된 패턴:

| 이벤트 | 게임당 누수 | 게임 내 라운드 사이 누수 | 비고 |
|---|---|---|---|
| `OnGameStart` | +2 | +1 (게임 2부터) | DamageChecker가 +1, 나머지 +1 의심 → 위 4.5~4.9 수정으로 차단 시도 |
| `OnGameOver` | 0 | +1 (게임 2부터) | 미해결 — 7번 항목 참조 |
| `OnPlayerListUp` | 변동 (4↔5) | 변동 | 게임 사이 누수 미확정 |
| `OnUltimate` | 0 | 0 | 안정 |

## 7. 미해결 / 추가 조사 필요

### 7.1 `OnGameOver` 라운드 사이 +1 (Game 2 이후)

게임 1 라운드 1 → 라운드 2 사이엔 정상 (1 → 1).
게임 2/3 라운드 1 → 라운드 2 사이에 +1 누적 (1 → 2).

가능한 원인 (추가 검증 필요):
1. **원격 Player(actor≠local) 의 OnDestroy 발화 타이밍 문제**
   - 마스터 클라이언트의 `PhotonNetwork.DestroyAll()`이 원격 actor의 로컬 destroy를 비동기로 트리거.
   - 원격 Player의 Start가 늦게 실행되어 destroy 사이클을 정상적으로 못 거치는 경우가 있을 수 있음.
2. **Player.HandleGameOver가 IsMine 체크 없이 모든 Player 인스턴스에서 구독**
   - 실제 동작상 로컬 클라이언트만 ultimate cooldown reset이 의미가 있다면 IsMine 가드를 추가하는 것을 검토.

### 7.2 `OnUltimate=6` (예상 5 vs 실제 6)

- UltimateProductionManager(1) + UltimateEffectLocal × 4명 = 5 예상
- 실측 6 → +1 추가 구독자 존재
- 게임 사이 누적은 보이지 않으므로 우선순위 낮음

### 7.3 디버그 로그 정리

검증이 완료되면 5번 섹션의 `[EventLeak]` / `[ShakeDiag]` 로그와 헬퍼 메서드들을 제거 권장.

## 8. 수정 파일 요약

```
M  Assets/02.Scripts/Camera/CameraController.cs               [핵심 수정 + 디버그 로그]
M  Assets/02.Scripts/Player/State/PlayerBaseState.cs          [핵심 수정]
M  Assets/02.Scripts/Player/Player.cs                         [핵심 수정 + 디버그 로그]
M  Assets/02.Scripts/Player/PlayerEvents.cs                   [디버그 헬퍼]
M  Assets/02.Scripts/Player/PlayerEventManager.cs             [디버그 헬퍼]
M  Assets/02.Scripts/Evenet/EventManager.cs                   [디버그 헬퍼]
M  Assets/02.Scripts/UI/UI_Ultimate.cs                        [핵심 수정]
M  Assets/02.Scripts/Map/PlayerSpawner.cs                     [디버그 자동 덤프]
M  Assets/02.Scripts/Photon/DamageChecker.cs                  [확실한 누수 수정]
M  Assets/02.Scripts/Map/UI/GameOverProduction.cs             [방어 코드]
M  Assets/02.Scripts/UI/KillLog/UI_IngameChatPopup.cs         [방어 코드]
M  Assets/02.Scripts/Gimmick/Core/GimmickManager.cs           [방어 코드]
M  Assets/02.Scripts/Map/UI/GameStartProduction.cs            [방어 코드]
```

## 9. 결론

**이번 세션의 주된 성과:**
- PlayerEventManager 측의 OnHit / OnAttack 구독 누수를 차단하여, 카메라쉐이크와 넉백 누적 버그(주 증상)를 정상화.
- EventManager 측의 확실한 누수 1건(DamageChecker.OnGameStart)을 수정.
- 동일 패턴(Unity-null 함정, idempotent 누락)이 재발하지 않도록 5개 컴포넌트에 방어 코드 추가.

**다음 단계 권장:**
- 위 수정 적용 후 다시 로그 수집 → `OnGameStart` 게임당 누수가 +2에서 0/+1로 줄어드는지 확인.
- `OnGameOver` 라운드 사이 +1 누수의 진짜 원인 추적 (원격 Player 라이프사이클 또는 IsMine 가드).
- 디버그 헬퍼 / 로그 코드는 안정 확인 후 제거.
