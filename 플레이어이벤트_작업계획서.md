# 플레이어 이벤트 시스템 작업계획서

## 개요

플레이어의 주요 행동/상태 변화를 **이벤트(Action)** 로 표준화하여, 외부 시스템이 **구독/반응** 할 수 있도록 한다.

**원칙:**
- 모든 플레이어 이벤트는 `PlayerEventManager`(싱글톤) 내부의 `PlayerEvents` 클래스에 정의한다.
- 기존 `Player.cs`의 이벤트 6개도 `PlayerEvents`로 이동한다.
- 각 컨트롤러/상태에서는 `PlayerEventManager.Instance.GetEvents(actorNumber).InvokeOnXxx()` 로 발행한다.
- 기존 `EventManager`(글로벌)는 그대로 유지한다.
- `#region` 사용 금지. 주석은 `//` 만 사용한다.

---

## 아키텍처

### PlayerEventManager (싱글톤)

```csharp
public class PlayerEventManager : DontDestroySingleton<PlayerEventManager>
{
    private Dictionary<int, PlayerEvents> _playerEvents = new();

    public PlayerEvents Local => GetEvents(PhotonNetwork.LocalPlayer.ActorNumber);

    public PlayerEvents GetEvents(int actorNumber)
    {
        if (!_playerEvents.TryGetValue(actorNumber, out var events))
        {
            events = new PlayerEvents();
            _playerEvents[actorNumber] = events;
        }
        return events;
    }

    public void RemoveEvents(int actorNumber)
    {
        if (_playerEvents.TryGetValue(actorNumber, out var events))
        {
            events.Clear();
            _playerEvents.Remove(actorNumber);
        }
    }
}
```

### PlayerEvents (이벤트 정의 클래스)

모든 이벤트 선언 + Invoke 래퍼 + Clear()를 포함한다. 전체 코드는 아키텍처 섹션 내 `PlayerEvents` 코드 블록 참조.

### PlayerEventContext.cs (Context 구조체)

```csharp
public struct DeathContext
{
    public int KillerActorNumber;    // -1이면 자살/환경
    public bool IsLastKill;
    public bool IsNormalAttack;
}

public struct AttackHitContext
{
    public int VictimActorNumber;
    public int Damage;
    public int MaxDamage;
    public bool IsCritical;          // damage == maxDamage
    public Vector3 VictimPosition;
}

public struct KillContext
{
    public int VictimActorNumber;
    public bool IsLastKill;
    public bool IsNormalAttack;
}

public struct DamagedContext
{
    public int Damage;
    public int MaxDamage;
    public int AttackerActorNumber;
    public int AttackerViewId;
    public Vector3 AttackerBombPosition;
    public int StealPercent;
    public bool IsFallingOut;
    public bool IsNormalAttack;
}
```

---

## 전체 이벤트 목록

| 분류 | 이벤트명 | 시그니처 | 발행 시점 | 비고 |
|------|----------|----------|-----------|------|
| 생명주기 | `OnSpawned` | `Action` | 첫 스폰 완료 | 신규 |
| | `OnResurrected` | `Action` | 카트리지 부활 완료 | 신규 |
| | `OnDeath` | `Action<DeathContext>` | 사망 확정 | 신규 |
| | `OnDespawned` | `Action` | 오브젝트 비활성화 직전 | 신규 |
| 공격 | `OnAttack` | `Action` | 공격 입력 (Z/X 공통) | 기존 이동 |
| | `OnNormalAttack` | `Action` | Z 공격 발사 | 기존 이동 |
| | `OnSpecialAttack` | `Action` | X 공격 발사 | 기존 이동 |
| | `OnAttackHit` | `Action<AttackHitContext>` | 공격 적중 | 신규 |
| | `OnKillConfirmed` | `Action<KillContext>` | 적 처치 성공 | 신규 |
| 피격 | `OnHit` | `Action` | 피격 시 | 기존 이동 |
| | `OnDamaged` | `Action<DamagedContext>` | 데미지 받음 | 신규 |
| 궁극기 | `OnUltimateChanceActivated` | `Action` | 궁극기 사용 가능 | 기존 이동 |
| | `OnUltimateChanceDeactivated` | `Action` | 궁극기 사용 불가 | 기존 이동 |
| | `OnUltimateUsed` | `Action` | 궁극기 사용 확정 | 신규 |
| 패널티 | `OnNoAttackPenaltyStart` | `Action` | 경고 카운트 시작 | 신규 |
| | `OnNoAttackPenaltyTriggered` | `Action` | 패널티 발동 | 신규 |
| | `OnNoAttackPenaltyReset` | `Action` | 카운트 초기화 | 신규 |
| 물리/이동 | `OnAirborne` | `Action` | 지상 → 공중 전환 | 신규 |
| | `OnLanded` | `Action` | 착지 | 신규 |
| | `OnPlayerContact` | `Action<int>` | 플레이어 스침 (actorNumber) | 신규 |
| | `OnObjectContact` | `Action<Collider2D>` | 오브젝트 스침 | 신규 |
| GP | `OnGPSet` | `Action<int>` | GP 강제 조정 | 신규 |
| | `OnGPGained` | `Action<int>` | GP 획득 | 신규 |
| | `OnGPLost` | `Action<int>` | GP 손실 | 신규 |
| 아이템 | `OnItemPickedUp` | `Action<string>` | 아이템 획득 | 신규 |
| | `OnItemUsed` | `Action<string>` | 아이템 사용 | 신규 |

---

## 기존 코드 변경 내역 (Phase 2)

### Player.cs

```csharp
// [제거] 기존 이벤트 선언 6개 (line 66-71)
public event Action OnAttack;                    // 제거
public event Action OnHit;                       // 제거
public event Action OnNormalAttack;              // 제거
public event Action OnSpecialAttack;             // 제거
public event Action OnUltimateChanceActivated;   // 제거
public event Action OnUltimateChanceDeactivated; // 제거

// [변경] Start() 내 UltimateController 구독 (line 459-460)
// 변경 전:
_ultimateController.OnUltimateChanceActivated += () => OnUltimateChanceActivated?.Invoke();
_ultimateController.OnUltimateChanceDeactivated += () => OnUltimateChanceDeactivated?.Invoke();
// 변경 후:
_ultimateController.OnUltimateChanceActivated += () =>
    PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnUltimateChanceActivated();
_ultimateController.OnUltimateChanceDeactivated += () =>
    PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnUltimateChanceDeactivated();

// [변경] InvokeAttack() (line 1224)
// 변경 전: OnAttack?.Invoke();
// 변경 후:
PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnAttack();

// [변경] InvokeNormalAttack() (line 1229)
// 변경 전: OnNormalAttack?.Invoke();
// 변경 후:
PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnNormalAttack();

// [변경] InvokeSpecialAttack() (line 1234)
// 변경 전: OnSpecialAttack?.Invoke();
// 변경 후:
PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnSpecialAttack();

// [변경] RegisterHitDamage() (line 1246)
// 변경 전: OnHit?.Invoke();
// 변경 후:
PlayerEventManager.Instance.GetEvents(ActorNumber).InvokeOnHit();
```

### CameraController.cs

```csharp
// [변경] SetTarget() (line 72-78)
// 변경 전: _target.OnHit -= HitShake;  /  _target.OnAttack += GunShotShake; 등
// 변경 후:
var events = PlayerEventManager.Instance.GetEvents(_target.ActorNumber);
events.OnHit += HitShake;
events.OnAttack += GunShotShake;

// [변경] RemoveTarget() (line 257-258)
// 변경 전: _target.OnHit -= HitShake;  /  _target.OnAttack -= GunShotShake;
// 변경 후:
var events = PlayerEventManager.Instance.GetEvents(_target.ActorNumber);
events.OnHit -= HitShake;
events.OnAttack -= GunShotShake;
```

### PlayerBaseState.cs

```csharp
// [변경] OnEnter() (line 56)
// 변경 전: _owner.OnHit += HandleHit;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_owner.ActorNumber).OnHit += HandleHit;

// [변경] OnExit() (line 63)
// 변경 전: _owner.OnHit -= HandleHit;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_owner.ActorNumber).OnHit -= HandleHit;
```

### PlayerDieState.cs

```csharp
// [변경] OnEnter() (line 69)
// 변경 전: _owner.OnHit -= HandleHit;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_owner.ActorNumber).OnHit -= HandleHit;
```

### SuperAmorBuff.cs

```csharp
// [변경] StartBuff() (line 20)
// 변경 전: _owner.OnHit += OnPlayerHit;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_owner.ActorNumber).OnHit += OnPlayerHit;

// [변경] EndBuff() (line 29)
// 변경 전: _owner.OnHit -= OnPlayerHit;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_owner.ActorNumber).OnHit -= OnPlayerHit;
```

### PlayerCoolTime.cs

```csharp
// [변경] Start() (line 92-93)
// 변경 전: _myPlayer.OnNormalAttack += SetNormalAttackCoolTime;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_myPlayer.ActorNumber).OnNormalAttack += SetNormalAttackCoolTime;
// (OnSpecialAttack도 동일)

// [변경] OnDestroy() (line 226-227)
// 변경 전: _myPlayer.OnNormalAttack -= SetNormalAttackCoolTime;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_myPlayer.ActorNumber).OnNormalAttack -= SetNormalAttackCoolTime;
// (OnSpecialAttack도 동일)
```

### UI_Ultimate.cs

```csharp
// [변경] 초기화 (line 79-80)
// 변경 전: _player.OnUltimateChanceActivated += OnUltimateActivated;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_player.ActorNumber).OnUltimateChanceActivated += OnUltimateActivated;
// (OnUltimateChanceDeactivated도 동일)

// [변경] OnDestroy() (line 327-328)
// 변경 전: _player.OnUltimateChanceActivated -= OnUltimateActivated;
// 변경 후:
PlayerEventManager.Instance.GetEvents(_player.ActorNumber).OnUltimateChanceActivated -= OnUltimateActivated;
// (OnUltimateChanceDeactivated도 동일)
```

---

## 구현 계획

### Phase 1: 기반 생성

- [ ] `PlayerEventManager.cs` 생성 (싱글톤, Dictionary, Local 프로퍼티)
- [ ] `PlayerEvents.cs` 생성 (전체 이벤트 선언 + Invoke 래퍼 + Clear)
- [ ] `PlayerEventContext.cs` 생성 (4개 구조체)

### Phase 2: 기존 이벤트 마이그레이션

- [ ] Player.cs — 이벤트 선언 제거, Invoke를 PlayerEventManager 호출로 변경
- [ ] CameraController.cs — 구독/해제를 PlayerEventManager로 변경
- [ ] PlayerBaseState.cs — OnHit 구독/해제 변경
- [ ] PlayerDieState.cs — OnHit 해제 변경
- [ ] SuperAmorBuff.cs — OnHit 구독/해제 변경
- [ ] PlayerCoolTime.cs — OnNormalAttack, OnSpecialAttack 구독/해제 변경
- [ ] UI_Ultimate.cs — OnUltimateChanceActivated/Deactivated 구독/해제 변경

### Phase 3~8: 신규 이벤트 연결 (병행 가능)

- [ ] Phase 3: 생명주기 — OnSpawned, OnResurrected, OnDeath, OnDespawned
- [ ] Phase 4: 공격/피격 — OnAttackHit, OnKillConfirmed, OnDamaged, OnUltimateUsed
- [ ] Phase 5: 공격 정체 패널티 — OnNoAttackPenaltyStart/Triggered/Reset
- [ ] Phase 6: 물리/이동 — OnAirborne, OnLanded, OnPlayerContact, OnObjectContact
- [ ] Phase 7: GP — OnGPSet, OnGPGained, OnGPLost
- [ ] Phase 8: 아이템 — OnItemPickedUp, OnItemUsed

---

## 네트워크 고려사항

| 이벤트 | 실행 범위 | 비고 |
|--------|-----------|------|
| `OnSpawned`, `OnResurrected` | 로컬 (IsMine) | |
| `OnDeath`, `OnDespawned` | 모든 클라이언트 | 사망 연출 동기화 |
| `OnAttack`, `OnNormalAttack`, `OnSpecialAttack` | 로컬 (IsMine) | |
| `OnAttackHit`, `OnKillConfirmed` | 공격자 로컬 | |
| `OnHit`, `OnDamaged` | 모든 클라이언트 | RPC_TakeDamage 기반 |
| `OnUltimateChanceActivated/Deactivated` | RPC 구동 | 기존 유지 |
| `OnUltimateUsed` | 로컬 (IsMine) | |
| `OnNoAttackPenalty*` | 로컬 + RPC 시각 동기화 | |
| `OnAirborne`, `OnLanded` | 로컬 (IsMine) | |
| `OnPlayerContact`, `OnObjectContact` | 로컬 (IsMine) | |
| `OnGPSet/Gained/Lost` | 로컬 (IsMine) | |
| `OnItemPickedUp`, `OnItemUsed` | 로컬 (IsMine) | |
