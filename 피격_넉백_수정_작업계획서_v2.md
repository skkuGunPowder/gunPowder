# 피격 / 넉백 시스템 수정 작업계획서

> 작성일: 2026-03-12
> 수정일: 2026-03-16
> 목적: 현재 피격/넉백 로직을 기획에 맞게 교체

---

## 1. 기획 요약

### 피격 상태
- 피격 도중 **조작 불가** (이동, 공격 등)
- 피격 최대 시간 = 각 폭발의 **MaxStunTime** (기존: PlayerStat.DamagedTime 고정값)
- 거리에 따른 피격 시간: 가까이 → MaxStunTime의 100%, 멀리 → MaxStunTime의 30%

### 무적 시간
- 피격 시작 시 무적 상태가 되며, **피격 시간의 25%** 만큼만 무적 유지 (기존: 피격 상태 전체가 무적)
- 25% 경과 후 무적 해제, 이후 피격 상태는 유지되나 추가 피격 가능

### 넉백
- 기본 넉백: Explosion.AddExplosionForce2D (거리 비례 감소) → **유지**
- 추가 넉백 힘 = (1 - 체력비율) × 10 × 거리보정(가까이 100%, 멀리 50%) → **유지**
  - 기존 "건파우더" 기반이었으나 일반 체력 비율로 표현 변경 (기능 동일)

### Hit-Stop 중 넉백
- Hit-Stop 중 추가 피격 시, **가장 마지막에 맞은 폭발의 힘**을 받아 넉백

### 삭제 대상
- ~~피격 횟수 기반 추가 데미지~~ → 삭제
  - 기존: 피격 횟수 누적 → 15의 배수 초과 시마다 추가 데미지 +1
  - 변경: 해당 시스템 제거, **마지막 폭발의 힘으로 넉백**하는 단순한 방식으로 교체

### 바닥 착지 분기 제거
- 피격 중 바닥에 닿아도 조기 전환 없이, **피격 시간이 완전히 종료된 후에만** 상태 전환

---

## 2. 현재 코드 vs 기획 차이 분석

| # | 항목 | 현재 코드 | 기획 | 작업 |
|---|------|----------|------|------|
| A | 피격 최대 시간 소스 | `PlayerStat.DamagedTime` (고정값) | 폭발별 `ExplosionStat.MaxStunTime` | **교체** |
| B | MaxStunTime 전달 경로 | 없음 | Explosion → Player → DamagedState | **신규** |
| C | 무적 시간 | 피격 상태 전체 = 무적 | 피격 시간의 25%만 무적 | **교체** |
| D | 바닥 착지 분기 | 피격 중 바닥 착지 시 조기 전환 가능성 | 피격 시간 완료 전 조기 전환 제거 | **제거 확정** |
| E | 추가 넉백 힘 계산 | `(1-체력비율)×10×거리보정` | 동일 (건파우더→체력 표현 변경만) | **유지** |
| F | 기본 넉백 (Explosion) | `AddExplosionForce2D` | 동일 | **유지** |
| G | 피격 횟수 누적 → 추가 데미지 | `damage += damagedCount / 15` | 삭제, 마지막 폭발 넉백으로 교체 | **삭제 + 교체** |
| H | Hit-Stop 중 넉백 | 히트스탑 중 속도 저장/갱신 방식 | 마지막 폭발의 힘으로 넉백 | **구현 확정** |
| I | `CalculateStartDamping` 하드코딩 | `damageRatio = 1.0f` (거리 보정 무효화) | 기획에 언급 없음 | **확인 후 결정** |
| J | `UPWARD_FORCE(10)` | 추가 넉백 시 항상 위로 10 힘 | 기획에 언급 없음 | **확인 후 결정** |

---

## 3. 작업 상세

### 작업 A+B: MaxStunTime 전달 경로 구축

**목적**: 각 폭발의 `MaxStunTime`을 `PlayerDamagedState`까지 전달

**수정 파일 및 내용:**

#### 1) `IDamagable.cs` (시그니처 변경)
- `TakeDamage`에 `float maxStunTime` 파라미터 추가

#### 2) `Explosion.cs` > `Explode()`
- `damagable.TakeDamage(...)` 호출 시 `_stat.MaxStunTime` 전달

#### 3) `Player.cs`
- `TakeDamage()` 시그니처에 `maxStunTime` 추가
- `_damageController.TakeDamage()`로 전달

#### 4) `PlayerDamageController.cs`
- `TakeDamage()` 시그니처에 `maxStunTime` 추가
- `RPC_TakeDamage` RPC 파라미터에 `maxStunTime` 추가
- `_player.RegisterHitDamage()`에 `maxStunTime` 전달

#### 5) `Player.cs` > `RegisterHitDamage()`
- `float maxStunTime` 파라미터 추가
- `_lastMaxStunTime` 필드 추가 및 저장
- `LastMaxStunTime` 프로퍼티 추가

#### 6) IDamagable 구현체들 (시그니처 맞춤)
- `Player.cs` 외에 IDamagable을 구현하는 다른 클래스가 있다면 시그니처 업데이트
- 해당 클래스들에서는 `maxStunTime`을 무시해도 무방

#### 7) 각 Explosion 서브클래스 확인
- `BasicExplosion`, `BounceExplosion`, `MissileExplosion` 등에서 `Explode()`를 오버라이드하는 경우 동일하게 `MaxStunTime` 전달 확인

---

### 작업 C: 피격 시간 계산 소스 교체

**수정 파일:** `PlayerDamagedState.cs` > `CalculateActualDamagedTime()`

**현재:**
```csharp
float baseDamagedTime = _owner.PlayerStat.DamagedTime; // 고정값
_actualDamagedTime = Lerp(baseDamagedTime * 0.3f, baseDamagedTime, damageRatio);
```

**변경:**
```csharp
float baseDamagedTime = _owner.LastMaxStunTime; // 폭발별 MaxStunTime
_actualDamagedTime = Lerp(baseDamagedTime * 0.3f, baseDamagedTime, damageRatio);
```

- 거리 계산식(가까이 100%, 멀리 30%)은 동일하게 유지

---

### 작업 D: 무적 시간을 피격 시간의 25%로 교체

**수정 파일:** `PlayerDamagedState.cs`

**현재 흐름:**
```
OnEnter/InitializeDamaged → SetImmuneState(true)   // 피격 시작 = 무적 ON
OnExit                    → SetImmuneState(false)  // 피격 종료 = 무적 OFF
→ 피격 상태 전체가 무적
```

**변경 흐름:**
```
InitializeDamaged → SetImmuneState(true)           // 피격 시작 = 무적 ON
                  → _immuneTime = _actualDamagedTime * 0.25f  // 무적 시간 계산
MineUpdate        → _damagedTimer >= _immuneTime 시 SetImmuneState(false)  // 25% 경과 후 무적 OFF
OnExit            → SetImmuneState(false) (안전장치)
```

**추가 변경:**
- `_immuneTime` 필드 추가
- `_isImmuneActive` bool 필드 추가 (무적 해제 중복 호출 방지)
- `MineUpdate()`에서 히트스탑 끝난 후 Damaged 로직 구간에 무적 해제 타이밍 체크 추가

**OnHitStopComplete()에서도 동일 적용:**
- 히트스탑 완료 → Damaged 로직 시작 시 무적 ON + 25% 타이머 설정

---

### 작업 E: 바닥 착지 조기 전환 제거

**수정 파일:** `PlayerDamagedState.cs` > `MineUpdate()`

**기획 의도:** 피격 중 바닥에 닿아도 조기에 Idle로 전환되지 않으며, 반드시 피격 시간이 완전히 종료된 후에만 상태 전환

**확인 사항:**
- 피격 타이머(`_damagedTimer`) 완료 **전에** `IsGrounded2D()` 체크로 조기 전환하는 코드가 있는지 확인
- 해당 경로가 있다면 제거

**피격 시간 종료 후 전환:**
```csharp
if (_damagedTimer >= _actualDamagedTime)
{
    if (IsGrounded2D())
        SyncStateChange<PlayerIdleState>();   // 바닥 → Idle
    else
        SyncStateChange<PlayerFallState>();   // 공중 → Fall
}
```
- 피격 시간 종료 후 바닥/공중에 따른 상태 전환 분기는 물리적으로 자연스러우므로 유지
- 핵심: **피격 시간 종료 전 조기 전환 경로만 제거**

---

### 작업 F: 피격 횟수 누적 시스템 삭제 및 마지막 폭발 넉백으로 교체

> **기획 변경 맥락:** 기존에는 피격 횟수를 누적하여 15의 배수 초과 시마다 추가 데미지를 부여했으나, 이 시스템을 삭제하고 **Hit-Stop 중 마지막 폭발의 힘으로 넉백**하는 단순한 방식으로 교체

#### F-1: 피격 횟수 기반 추가 데미지 삭제

**수정 파일:** `PlayerDamageController.cs` > `RPC_TakeDamage()`

**삭제 대상:**
```csharp
// 플레이어가 맞은 횟수에 비례해서 데미지 증가
int increaseDamagePerDamagedCount = _playerStat.CurrentPlayerDamagedCount / 15;
damage += increaseDamagePerDamagedCount;
maxDamage += increaseDamagePerDamagedCount;
```

**추가 정리:**
- `_playerStat.IncreseDamagedCount()` 호출부 삭제
- `PlayerStat._currentPlayerDamagedCount`, `CurrentPlayerDamagedCount`, `IncreseDamagedCount()`, `ResetDamagedCount()`가 다른 곳에서 사용되는지 확인
- 미사용 시 관련 필드/메서드 모두 삭제

#### F-2: Hit-Stop 중 마지막 폭발 넉백 구현

**기획:** Hit-Stop 중에는 가장 마지막에 맞은 폭발의 힘을 받아 넉백

**현재 흐름의 문제점:**
1. 첫 피격 → `Explosion.AddExplosionForce2D()`로 Rigidbody에 힘 적용
2. `PlayerDamagedState.OnEnter()` → 히트스탑 시작 → `StoreCurrentVelocityAndFreeze()` (현재 속도 저장 + 정지)
3. 히트스탑 중 추가 피격 → `OnAdditionalHit()` → `UpdateStoredVelocity()` (새 속도로 갱신 + 정지)
4. 히트스탑 완료 → `OnHitStopComplete()` → `RestoreStoredVelocity()` (마지막 저장 속도 복원)

- 3번에서 히트스탑 중 Rigidbody 속도가 이미 0으로 초기화된 상태이므로, 새 폭발의 `AddExplosionForce2D`가 힘을 가해도 바로 0으로 초기화됨
- **마지막 폭발의 힘이 제대로 저장되지 않을 가능성 있음**

**수정 방안:**
기획 의도가 "단순하게 마지막 폭발의 힘으로 넉백"이므로, 속도 저장/복원 타이밍을 맞추는 복잡한 방식 대신:

1. `Player`에 마지막 폭발 정보(힘의 크기, 방향, 폭발 위치)를 **별도 필드로 저장**
2. 히트스탑 중 추가 피격 시 → 해당 필드를 새 폭발 정보로 **덮어쓰기**
3. 히트스탑 완료 시 → 저장된 마지막 폭발 정보로 **한 번에 넉백 적용**

이 방식이 기획 의도("마지막 폭발의 힘")에 가장 직관적으로 대응하며, 타이밍 이슈를 근본적으로 회피

→ **구현 시 타이밍 테스트 필요**

---

### 작업 G: 확인 후 결정 사항

#### G-1: `CalculateStartDamping()`의 `damageRatio = 1.0f` 하드코딩
- 감쇠값의 거리 보정이 무효화되어 있음
- 기획에서 감쇠 관련 언급 없으므로 확인 필요:
  - 거리에 따라 감쇠도 달라져야 하는지? (멀리서 맞으면 빨리 멈춤)
  - 아니면 현재처럼 하드코딩 유지?

#### G-2: `UPWARD_FORCE(10)`
- 추가 넉백 시 항상 위쪽으로 10의 힘이 추가됨
- 넉백 체감에 큰 영향을 주는 값이므로 기획 확인 필요:
  - 의도된 동작인지?
  - 제거해야 하는지?

---

## 4. 작업 순서

```
1. [작업 F-1] 피격 횟수 기반 추가 데미지 삭제 (독립적, 먼저 처리)
2. [작업 A+B] MaxStunTime 전달 경로 구축 (IDamagable → Explosion → Player → DamagedState)
3. [작업 C]   피격 시간 계산 소스 교체 (DamagedTime → MaxStunTime)
4. [작업 D]   무적 시간 교체 (전체 → 25%)
5. [작업 E]   바닥 착지 조기 전환 제거
6. [작업 F-2] Hit-Stop 중 마지막 폭발 넉백 구현
7. [작업 G]   확인 후 결정 사항 반영
```

---

## 5. 영향 범위

### 시그니처 변경으로 영향받는 파일들
- `IDamagable.cs` — 인터페이스 변경
- `Explosion.cs` 및 모든 서브클래스 — `Explode()` 내 `TakeDamage` 호출부
- `Player.cs` — `TakeDamage()` 시그니처
- `PlayerDamageController.cs` — `TakeDamage()`, `RPC_TakeDamage()` 시그니처
- IDamagable을 구현하는 다른 클래스들 (있다면)

### 로직 변경 파일
- `PlayerDamagedState.cs` — 핵심 수정 대상 (피격 시간, 무적, 바닥 분기, 넉백)
- `PlayerDamageController.cs` — 피격 횟수 로직 삭제
- `Player.cs` — 마지막 폭발 정보 저장 필드 추가

### 변경 없는 파일
- `Explosion/AddExplosionForce2D()` — 기본 넉백 유지
- `PlayerDamagedState/ApplyHealthBasedForce()` — 추가 넉백 유지
- `PlayerHitStopState.cs` — 히트스탑 로직 (F-2 구현 시 연동 확인 필요)
