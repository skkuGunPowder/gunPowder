# [KKH] 플레이어 폭탄 슬롯 네이밍 통일 작업내역서

## 개요

기존 Basic/Normal/Special로 혼재되어 있던 폭탄 슬롯 네이밍을 **Z슬롯(ZSlot) / X슬롯(XSlot)** 으로 통일.
Z슬롯과 X슬롯 모두 동일한 구조로 폭탄을 장착할 수 있도록 변경하고, 궁극기도 슬롯 기반으로 전환.

---

## 1. 리네이밍 매핑표

### Player.cs 필드/프로퍼티

| 이전 | 이후 |
|------|------|
| `BasicBombStat` | `ZSlotBombStat` |
| `SpecialBombStat` | `XSlotBombStat` |
| `NormalBombCoolTime` | `ZSlotBombCoolTime` |
| `SpecialBombCoolTime` | `XSlotBombCoolTime` |
| `_lastNormalBombTime` | `_lastZSlotBombTime` |
| `_lastSpecialBombTime` | `_lastXSlotBombTime` |
| `LastNormalBombTime` | `LastZSlotBombTime` |
| `LastSpecialBombTime` | `LastXSlotBombTime` |
| `_isNormalAttackEnabled` | `_isZSlotAttackEnabled` |
| `_isSpecialAttackEnabled` | `_isXSlotAttackEnabled` |
| `IsNormalAttackEnabled` | `IsZSlotAttackEnabled` |
| `IsSpecialAttackEnabled` | `IsXSlotAttackEnabled` |

### Player.cs 메서드

| 이전 | 이후 |
|------|------|
| `CanNormalBomb()` | `CanZSlotBomb()` |
| `CanSpecialBomb()` | `CanXSlotBomb()` |
| `SetLastNormalBombTime()` | `SetLastZSlotBombTime()` |
| `SetLastSpecialBombTime()` | `SetLastXSlotBombTime()` |
| `SetNormalAttackEnabled()` | `SetZSlotAttackEnabled()` |
| `SetSpecialAttackEnabled()` | `SetXSlotAttackEnabled()` |

### PlayerBaseState.cs 메서드

| 이전 | 이후 |
|------|------|
| `CanNormalBomb()` | `CanZSlotBomb()` |
| `CanSpecialBomb()` | `CanXSlotBomb()` |
| `SetLastNormalBombTime()` | `SetLastZSlotBombTime()` |
| `SetLastSpecialBombTime()` | `SetLastXSlotBombTime()` |

---

## 2. 궁극기 슬롯 통합

### 변경 전
- 궁극기 설정이 초기화 코드에 하드코딩 (`EItemType.Bomb` 고정)
- `ExecuteUltimate()`와 `SetUltimateBySlot(BombSlot)` 이 별도 함수

### 변경 후
- `SetUltimateBySlot(BombSlot slot = BombSlot.ZSlot)` : 기본값 Z슬롯, X슬롯도 지정 가능
- `ForceUltimateChance(BombSlot slot = BombSlot.ZSlot)` : 강제 궁극기 발동 시 슬롯 기준으로 궁극기를 먼저 설정 후 발동
- 초기화 코드에서도 `SetUltimateBySlot()` 호출로 통일

### 사용 예시
```csharp
// Z슬롯 궁극기 (기본)
player.SetUltimateBySlot();
player.ForceUltimateChance();

// X슬롯 궁극기로 전환
player.SetUltimateBySlot(BombSlot.XSlot);
player.ForceUltimateChance(BombSlot.XSlot);
```

---

## 3. 쿨타임 감소 시스템

### BombCooldownReduction (기존 유지)
```csharp
// LightweightCartridge.cs 에서 사용
public void BombCooldownReduction(BombSlot slot, float value, OperationType type)
```
- `BombSlot.ZSlot` / `BombSlot.XSlot` 으로 슬롯 구분
- `OperationType.Additive` (가산) / `OperationType.Multiplicative` (곱연산)
- `ResetBombCooldown()` 으로 초기화

### 쿨타임 프로퍼티
- `ZSlotBombCoolTime` = `ZSlotBombStat.CoolTime * _zBombCooldownTime`
- `XSlotBombCoolTime` = `XSlotBombStat.CoolTime * _xBombCooldownTime`

---

## 4. 주석 정리

Player.cs 내 모든 `/// <summary>` XML 주석을 `//` 한줄 주석으로 변환 (약 40개 블록)

---

## 5. 변경 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `Player/Player.cs` | 필드/프로퍼티/메서드 리네이밍, 궁극기 슬롯 통합, 주석 정리 |
| `Player/PlayerCoolTime.cs` | BombStat, CoolTime 프로퍼티 참조 변경 |
| `Player/State/PlayerBaseState.cs` | CanBomb, SetLast, BombStat 메서드 리네이밍 |
| `Player/State/PlayerIdleState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerJumpState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerDashState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerFallState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerJumpDashState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerRunState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |
| `Player/State/PlayerWalkState.cs` | CanZSlotBomb/CanXSlotBomb 변경 |

---

## 6. enum 정의 (LightweightCartridge.cs 상단)

```csharp
public enum BombSlot { ZSlot, XSlot }
public enum OperationType { Additive, Multiplicative }
```
