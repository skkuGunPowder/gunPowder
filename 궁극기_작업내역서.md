# 궁극기 시스템 리워크 작업내역서

> 작업일: 2026-03-23
> 브랜치: KKH_Test

---

## 1. 작업 개요

기존 HP 임계치 기반 1회성 궁극기 시스템을 **게이지 충전 방식**으로 전면 리워크.

**기존**: HP가 30 이하로 떨어지면 궁극기 기회 발생 → 10초 내 사용 → 생애 1회 제한
**변경**: GP 획득(30%)/손실(50%) 기반 게이지(0~100) 충전 → 만충 시 사용 → 사용 후 10초 뒤 게이지 초기화

---

## 2. 수정 파일 및 변경 내역 (총 8개 파일)

### 2.1 PlayerStat.cs
**경로**: `Assets/02.Scripts/Player/PlayerStat.cs`

**제거:**
- `_hasUltimateChance` (bool)
- `_hasUsedUltimateThisLife` (bool)
- `_ultimateChanceDuration` (float)
- `_ultimateTriggerThreshold` (int)
- 위 4개의 public 프로퍼티

**추가:**
- `_currentUltimateGauge` (float, 0~100)
- `_maxUltimateGauge` (float, 기본 100)
- `ULTIMATE_GAUGE_RATE_ON_GP_GAIN` (const 0.3f) — 획득 GP의 30%
- `ULTIMATE_GAUGE_RATE_ON_GP_LOSS` (const 0.5f) — 잃은 GP의 50%
- `IsUltimateReady` (bool 프로퍼티) — 게이지 만충 여부
- `OnUltimateGaugeChanged` (event Action<float, float>) — 게이지 변동 이벤트
- `IncreaseUltimateGauge(float)` — 게이지 증가 (최대치 클램프)
- `ResetUltimateGauge()` — 게이지 0 초기화
- `SetMaxUltimateGauge(float)` — 외부 환경에 의한 총량 변경
- `ChargeUltimateOnGPGain(int)` — GP 획득 시 30% 충전
- `ChargeUltimateOnGPLoss(int)` — GP 손실 시 50% 충전

**수정:**
- `DecreaseHP()`: HP 임계치 기반 궁극기 트리거 제거 → `ChargeUltimateOnGPLoss(amount)` 호출
- `IncreaseHP()`: `ChargeUltimateOnGPGain(amount)` 호출 추가
- `ResurrectPlayerStat()`: `_hasUsedUltimateThisLife`, `_hasUltimateChance` 초기화 코드 제거

---

### 2.2 PlayerUltimateController.cs
**경로**: `Assets/02.Scripts/Player/PlayerUltimateController.cs`

**제거:**
- `_ultimateChanceTimer` (float)
- `UltimateChanceTimer` 프로퍼티

**추가:**
- `_postUltimateTimer` (float) — 사용 후 경과 시간
- `_isPostUltimateCooldown` (bool) — 10초 대기 중 여부
- `POST_ULTIMATE_RESET_DELAY` (const 10f) — 사용 후 리셋 대기 시간
- `_wasUltimateReady` (bool) — 게이지 만충 이펙트 추적
- `OnRoundEnd()` — 라운드 종료 시 카운트다운 초기화

**수정:**
- `UpdateUltimateChanceTimer()`: 기존 10초 만료 체크 → 사용 후 10초 카운트다운 + 게이지 만충 이펙트 감지
- `ExecuteUltimate()`: 조건을 `IsUltimateReady && !_isPostUltimateCooldown`으로 변경, 사용 후 쿨다운 시작
- `ForceUltimateChance()`: 게이지를 max로 설정하는 방식으로 변경
- `ResetUltimateChanceTimer()`: 쿨다운 상태 및 `_wasUltimateReady` 초기화 추가
- `SetUltimateSystemEnabled()`: 기존 `HasUltimateChance` 참조 제거, 쿨다운 상태 초기화

---

### 2.3 Player.cs
**경로**: `Assets/02.Scripts/Player/Player.cs`

**제거:**
- `UltimateChanceTimer` 프로퍼티
- `HandleHPEmpty()`에서 `_playerStat.HasUltimateChance = false;` 코드

**추가:**
- `CurrentUltimateGauge` (프로퍼티) — PlayerStat 위임
- `MaxUltimateGauge` (프로퍼티) — PlayerStat 위임
- `IsUltimateReady` (프로퍼티) — PlayerStat 위임
- `HandleGameOver()` — 라운드 종료 시 `_ultimateController.OnRoundEnd()` 호출
- `EventManager.OnGameOver` 구독/해제
- `BASIC_BOMB_ID`에 TODO 주석 — 추후 Z슬롯 교체용
- `RPC_LoadItems()` 궁극기 로딩부에 TODO 주석 — 추후 Z슬롯 전환용

---

### 2.4 EventManager.cs
**경로**: `Assets/02.Scripts/Evenet/EventManager.cs`

**추가:**
- `OnUltimateGaugeChanged` (event Action<float, float>)
- `UltimateGaugeChanged(float, float)` 메서드

---

### 2.5 UI_Ultimate.cs
**경로**: `Assets/02.Scripts/UI/UI_Ultimate.cs`

**변경:**
- `_gaugeBarFill`: SerializeField 제거 → 런타임에 `UI_InGameProfile.LocalPlayerSlot`에서 참조 취득
- `FindAndSubscribeToPlayer()`에서 `FindAnyObjectByType<UI_InGameProfile>()`로 0번 슬롯의 게이지 바 참조
- 기존 게이지 바 초기화 코드를 참조 취득 시점으로 이동

---

### 2.6 UI_InGameProfileSlot.cs
**경로**: `Assets/02.Scripts/Map/UI/UI_InGameProfileSlot.cs`

**추가:**
- `_ultimateGaugeBarFill` (Image, SerializeField) — 궁극기 게이지 바 Fill 이미지
- `UltimateGaugeBarFill` (public 프로퍼티) — 외부 참조용

---

### 2.7 UI_InGameProfile.cs
**경로**: `Assets/02.Scripts/Map/UI/UI_InGameProfile.cs`

**추가:**
- `LocalPlayerSlot` (public 프로퍼티) — 0번 슬롯(로컬 플레이어) 접근자
- `Init()`에서 슬롯별 게이지 바 active 관리: index 0만 `SetActive(true)`, 나머지 `SetActive(false)`

---

### 2.8 UltimateManager.cs
**경로**: `Assets/02.Scripts/Bomb/UltimateBomb/UltimateManager.cs`

**추가:**
- `GetUltimate()` 메서드 위에 TODO 주석 — 추후 Z슬롯 bombID 전달로 변경

---

## 3. TODO 주석 목록 (추후 Z슬롯 전환용)

| 파일 | 위치 | 주석 내용 |
|------|------|----------|
| `Player.cs:94` | `BASIC_BOMB_ID` 상수 | Z슬롯 교체 가능하도록 변경 |
| `Player.cs:326` | `RPC_LoadItems()` 궁극기 로딩 | Z슬롯 아이템 ID 기반으로 변경 |
| `UltimateManager.cs:38` | `GetUltimate()` | 호출부에서 Z슬롯 bombID 전달로 변경 |

---

## 4. Unity 에디터 설정 필요사항

### UI_InGameProfileSlot 프리팹
1. 각 `UI_InGameProfileSlot` 프리팹에 게이지 바용 **Image** 오브젝트 추가
2. Image Type: `Filled`, Fill Method: `Horizontal`
3. 해당 Image를 `UI_InGameProfileSlot` 컴포넌트의 **Ultimate Gauge Bar Fill** 슬롯에 연결
4. Image의 기본 active 상태: **false** (코드에서 0번 슬롯만 활성화)

### PlayerStat Inspector
- 기존 "궁극기 시스템" 4개 필드 → "궁극기 게이지 시스템" 2개 필드로 변경
- `Current Ultimate Gauge`: 0 (런타임 자동 충전)
- `Max Ultimate Gauge`: 100 (기본값)

---

## 5. 게이지 충전 흐름도

```
[GP 손실 발생] (피격, 자동 감소 등)
    ↓
PlayerStat.DecreaseHP(amount)
    ↓
ChargeUltimateOnGPLoss(amount)  →  amount × 50% 게이지 충전
    ↓
OnUltimateGaugeChanged 이벤트 발행
    ↓
UI_Ultimate.OnUltimateGaugeChanged()  →  _gaugeBarFill.fillAmount 업데이트

[GP 획득 발생] (건파우더 아이템 등)
    ↓
PlayerStat.IncreaseHP(amount)
    ↓
ChargeUltimateOnGPGain(amount)  →  amount × 30% 게이지 충전
    ↓
(동일 흐름)

[게이지 100 도달]
    ↓
PlayerUltimateController.UpdateUltimateChanceTimer()
    ↓
_wasUltimateReady = true  →  SetUltimateEffectState(true)
    ↓
이펙트 + "Press C" UI 활성화

[C키 입력 → 궁극기 사용]
    ↓
ExecuteUltimate()  →  X슬롯 특수폭탄의 궁극기 발동
    ↓
_isPostUltimateCooldown = true  →  10초 카운트다운 시작
    ↓
10초 경과 → PlayerStat.ResetUltimateGauge()  →  게이지 0 초기화

[라운드 종료 (10초 이전)]
    ↓
HandleGameOver()  →  OnRoundEnd()  →  카운트다운 초기화 (게이지 유지)
```
