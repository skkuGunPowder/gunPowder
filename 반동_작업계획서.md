# 반동(Recoil) RecoilAmount 적용 작업 계획서

> 작성일: 2026-03-23
> 분기: KKH_Test

---

## 1. 현재 상태 분석

### 1.1 문제 요약

폭탄 데이터 테이블(BombCSV)에 `RecoilAmount` 컬럼이 존재하고, `BombStat.cs`에서 JSON 파싱까지 정상적으로 수행하지만, **실제 반동 로직에서 이 값을 참조하지 않고 하드코딩 상수를 사용 중**이다.

### 1.2 현재 반동 수치 흐름

```
[뒤끝 차트] BombCSV.csv → RecoilAmount 컬럼 존재
        ↓
[BombStat.cs:23] float.Parse(json["RecoilAmount"]) → BombStat.RecoilAmount에 저장
        ↓
[Player.cs:237] BasicBombStat = GetStat<BombStat>(BASIC_BOMB_ID)
[Player.cs:323] SpecialBombStat = GetStat<BombStat>(EquipedItemDict[EItemType.Bomb].ID)
        ↓
❌ 여기서 끊김 — RecoilAmount를 아무 곳에서도 읽지 않음
        ↓
[PlayerBaseState.cs:17-18] NORMAL_RECOIL_FORCE = 10f / STRONG_RECOIL_FORCE = 20f ← 하드코딩 사용 중
```

### 1.3 수정 대상 파일

| 파일 | 수정 내용 |
|------|-----------|
| `Assets/02.Scripts/Player/State/PlayerBaseState.cs` | `PlayBombAnimation()`에서 BombStat.RecoilAmount 참조로 변경 |

---

## 2. 상세 분석

### 2.1 반동이 적용되는 지점

`PlayerBaseState.cs`의 `PlayBombAnimation()` (line 603-634) 메서드 내에서 `ApplyRecoil()`을 호출한다.

```
PlayBombAnimation(BombActionType action, Transform bombSpawnPoint)
├── ThrowStraight → ApplyRecoil(bombSpawnPoint, STRONG_RECOIL_FORCE=20f, Y_RECOIL_FORCE=5f)  ← line 612
├── Throw         → ApplyRecoil(bombSpawnPoint, NORMAL_RECOIL_FORCE=10f, Y_RECOIL_FORCE=5f)  ← line 619
├── Place         → 반동 없음
└── Boost         → 반동 없음
```

### 2.2 ApplyRecoil 시그니처

```csharp
// PlayerBaseState.cs:637
protected virtual void ApplyRecoil(Transform bombSpawnPoint, float recoilPower = 5f, float upPower = 1f)
```

`recoilPower` 파라미터에 BombStat.RecoilAmount를 전달하면 된다. **시그니처 변경 불필요.**

### 2.3 BombStat 접근 경로

`PlayBombAnimation()`은 `ExecuteBombAction()` (line 576)에서 호출되며, `ExecuteBombAction()`은 두 경로에서 호출된다:

| 호출 경로 | 사용 폭탄 | BombStat 접근 |
|-----------|-----------|---------------|
| `HandleNormalBomb()` (line 379) | 기본 폭탄 | `_owner.BasicBombStat` |
| `HandleSpecialBomb()` (line 422) | 장착 폭탄 | `_owner.SpecialBombStat` |

**핵심:** `PlayBombAnimation()`은 현재 어떤 폭탄(Normal/Special)을 쏘는지 모른다. BombStat을 전달받을 수 있도록 해야 한다.

---

## 3. 작업 단계

### Step 1: PlayBombAnimation에 BombStat 파라미터 추가

**파일:** `PlayerBaseState.cs`

**현재:**
```csharp
// line 603
protected virtual void PlayBombAnimation(BombActionType action, Transform bombSpawnPoint)
```

**변경:**
```csharp
protected virtual void PlayBombAnimation(BombActionType action, Transform bombSpawnPoint, BombStat bombStat)
```

---

### Step 2: ExecuteBombAction에 BombStat 파라미터 추가

**파일:** `PlayerBaseState.cs`

**현재:**
```csharp
// line 565
protected virtual void ExecuteBombAction(string prefabName, Transform bombSpawnPoint, BombActionType action)
{
    // ...
    PlayBombAnimation(action, bombSpawnPoint);  // line 576
}
```

**변경:**
```csharp
protected virtual void ExecuteBombAction(string prefabName, Transform bombSpawnPoint, BombActionType action, BombStat bombStat)
{
    // ...
    PlayBombAnimation(action, bombSpawnPoint, bombStat);
}
```

---

### Step 3: HandleNormalBomb / HandleSpecialBomb에서 BombStat 전달

**파일:** `PlayerBaseState.cs`

**HandleNormalBomb (line 399):**
```
현재: ExecuteBombAction(prefabName, bombSpawnPoint, action);
변경: ExecuteBombAction(prefabName, bombSpawnPoint, action, _owner.BasicBombStat);
```

**HandleSpecialBomb (line 459):**
```
현재: ExecuteBombAction(prefabName, bombSpawnPoint, action);
변경: ExecuteBombAction(prefabName, bombSpawnPoint, action, _owner.SpecialBombStat);
```

---

### Step 4: PlayBombAnimation 내부에서 RecoilAmount 사용

**파일:** `PlayerBaseState.cs`

**현재 (line 612, 619):**
```csharp
case BombActionType.ThrowStraight:
    ApplyRecoil(bombSpawnPoint, STRONG_RECOIL_FORCE, Y_RECOIL_FORCE);  // 20f 고정

case BombActionType.Throw:
    ApplyRecoil(bombSpawnPoint, NORMAL_RECOIL_FORCE, Y_RECOIL_FORCE);  // 10f 고정
```

**변경:**
```csharp
case BombActionType.ThrowStraight:
    ApplyRecoil(bombSpawnPoint, bombStat.RecoilAmount, Y_RECOIL_FORCE);

case BombActionType.Throw:
    ApplyRecoil(bombSpawnPoint, bombStat.RecoilAmount, Y_RECOIL_FORCE);
```

> **참고:** ThrowStraight와 Throw의 반동 강도를 동일하게 `RecoilAmount` 값 그대로 쓸지, Throw에는 배율(예: `× 0.5f`)을 적용할지는 기획 확인 필요. 우선은 동일 적용 후 테스트로 조정.

---

### Step 5: 하드코딩 상수 정리

**파일:** `PlayerBaseState.cs` (line 17-18)

```csharp
protected const float NORMAL_RECOIL_FORCE = 10f;  // 제거 또는 주석 처리
protected const float STRONG_RECOIL_FORCE = 20f;  // 제거 또는 주석 처리
```

- `NORMAL_RECOIL_FORCE`, `STRONG_RECOIL_FORCE`는 더 이상 사용하지 않으므로 제거
- `Y_RECOIL_FORCE = 5f`는 그대로 유지 (수직 반동은 폭탄 데이터에 없음)

---

## 4. 수정 범위 요약

| 파일 | 수정 라인 | 내용 |
|------|-----------|------|
| `PlayerBaseState.cs:17-18` | 하드코딩 상수 `NORMAL_RECOIL_FORCE`, `STRONG_RECOIL_FORCE` 제거 |
| `PlayerBaseState.cs:399` | `HandleNormalBomb` → `ExecuteBombAction`에 `_owner.BasicBombStat` 전달 |
| `PlayerBaseState.cs:459` | `HandleSpecialBomb` → `ExecuteBombAction`에 `_owner.SpecialBombStat` 전달 |
| `PlayerBaseState.cs:565` | `ExecuteBombAction` 시그니처에 `BombStat bombStat` 추가 |
| `PlayerBaseState.cs:576` | `PlayBombAnimation` 호출에 `bombStat` 전달 |
| `PlayerBaseState.cs:603` | `PlayBombAnimation` 시그니처에 `BombStat bombStat` 추가 |
| `PlayerBaseState.cs:612` | `STRONG_RECOIL_FORCE` → `bombStat.RecoilAmount` |
| `PlayerBaseState.cs:619` | `NORMAL_RECOIL_FORCE` → `bombStat.RecoilAmount` |

**수정 파일: 1개 (`PlayerBaseState.cs`)**

---

## 5. 확인 사항

| 항목 | 내용 |
|------|------|
| `ExecuteBombAction`을 override하는 자식 클래스가 있는지 | 시그니처 변경 시 자식 클래스도 함께 수정 필요 |
| Throw vs ThrowStraight 반동 차이 | 동일 `RecoilAmount` 사용할지, 배율 차등 적용할지 기획 확인 |
| 뒤끝 차트의 RecoilAmount 값 | 각 폭탄별로 적절한 수치가 입력되어 있는지 확인 |

---

## 6. 테스트 시나리오

| 시나리오 | 확인 사항 |
|----------|-----------|
| 기본 폭탄(Z키) 발사 | `BasicBombStat.RecoilAmount` 값만큼 반동 적용 |
| 특수 폭탄(X키) 발사 | `SpecialBombStat.RecoilAmount` 값만큼 반동 적용 |
| 서로 다른 특수 폭탄 장착 후 비교 | 폭탄별 RecoilAmount 차이가 체감되는지 |
| Place/Boost 액션 | 반동 없음 유지 확인 |
| 멀티플레이어 | 반동 물리가 로컬에서만 적용되고 동기화 이상 없는지 |
