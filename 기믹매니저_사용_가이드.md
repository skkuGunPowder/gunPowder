# GimmickManager 사용 가이드

> 작성일: 2026-03-25

---

## 1. 개요

`GimmickManager`는 맵별 기믹을 중앙 관리하는 시스템이다.
모든 기믹은 `IGimmick` 인터페이스를 구현하고, 씬 로드 시 자동 등록된다.

---

## 2. 그룹별 동작 방식

| 그룹 | 활성화 시점 | 활성화 주체 | 설명 |
|------|-----------|------------|------|
| **Start** | 게임 시작 | GimmickManager (자동) | 게임 시작 시 전부 활성화. Cannon, RotationPlatform 등 |
| **During** | 게임 도중 (수동 호출) | 외부 클래스 (수동) | 게임 도중 외부에서 조건 판단 후 호출하는 기믹. CrabSpawner, AirDropSpawner 등 |
| **HurryUp** | 허리업 연출 완료 | HurryUpProduction (자동) | 랜덤 1개만 활성화 |

---

## 3. 신규 기믹 추가 방법

### 3.1 스크립트 작성

```csharp
using Photon.Pun;
using UnityEngine;

// MyNewGimmick 예시 
public class MyNewGimmick : MonoBehaviour, IGimmick
{
    private bool _isGimmickActive = false;

    // 1. 타입과 그룹 선언
    public GimmickType Type => GimmickType.MyNewGimmick;   // enum에 추가 필요
    public GimmickGroupType GroupType => GimmickGroupType.During;  // 원하는 그룹
    public bool IsActive => _isGimmickActive;

    // 2. 활성화/비활성화 로직
    public void Activate()
    {
        _isGimmickActive = true;
        // 기믹 시작 로직
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
        // 기믹 정지 로직
    }

    // 3. 등록/해제
    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }

    // 4. 기존 로직에 가드 추가
    private void Update()
    {
        if (!_isGimmickActive) return;
        // 기존 로직...
    }
}
```

### 3.2 GimmickType enum에 항목 추가

**파일**: `Assets/02.Scripts/Gimmick/Core/GimmickType.cs`

```csharp
public enum GimmickType
{
    Cannon,
    CrabSpawner,
    AirDropSpawner,
    RotationPlatform,
    ValleyBall,
    MyNewGimmick, // ← 여기에 추가
}
```

### 3.3 씬에 배치

1. 맵 씬(Beach1, Dock1, Forest1)에 기믹 오브젝트 생성
2. `MyNewGimmick` 컴포넌트 추가
3. 끝 — `Start()`에서 자동 등록됨

---

## 4. API 레퍼런스

### 4.1 활성화

```csharp
// 특정 타입 전부 활성화 (마스터 클라이언트만 호출 가능)
GimmickManager.Instance.ActivateByType(GimmickType.Cannon);

// 그룹에서 랜덤 N개 활성화
GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1);

// 그룹 전부 활성화
GimmickManager.Instance.ActivateAllByGroup(GimmickGroupType.Start);
```

### 4.2 비활성화

```csharp
// 특정 타입 전부 비활성화
GimmickManager.Instance.DeactivateByType(GimmickType.Cannon);

// 전체 비활성화 (게임 종료 시 자동 호출)
GimmickManager.Instance.DeactivateAll();
```

### 4.3 등록/해제

```csharp
// 기믹 등록 (Start에서 호출)
GimmickManager.Instance.Register(this);

// 기믹 해제 (OnDestroy에서 호출)
GimmickManager.Instance.Unregister(this);
```

---

## 5. 네트워크 동기화

| 항목 | 처리 주체 | 기믹 개발자가 할 일 |
|------|----------|-------------------|
| 기믹 활성화/비활성화 동기화 | GimmickManager (RPC) | 없음 (자동) |
| 랜덤 선택 동기화 | GimmickManager (마스터 선택 → RPC) | 없음 (자동) |
| 기믹 내 오브젝트 생성 | 기믹 자체 | `if (PhotonNetwork.IsMasterClient)` + `PhotonNetwork.Instantiate` |
| 기믹 내 오브젝트 파괴 | 기믹 자체 | `PhotonNetwork.Destroy` (마스터만) |

**핵심**: `ActivateByType`, `ActivateRandomByGroup` 등은 **마스터 클라이언트만 호출 가능**하다. 내부에서 RPC로 모든 클라이언트에 전파한다.

---

## 6. 주의사항

### 6.1 등록 순서
- 모든 클라이언트가 같은 씬을 로드하므로 하이어라키 순서 = 등록 순서
- `ActivateRandomByGroup`은 인덱스 기반 RPC이므로, 등록 순서가 같아야 동기화됨
- `Start()`에서 Register하면 순서가 보장됨

### 6.2 기존 클래스를 상속하는 경우
- `Bomb`을 상속하는 ValleyBall처럼 기존 클래스를 상속하는 경우, `OnDestroy`를 override해야 함:
```csharp
protected override void OnDestroy()
{
    base.OnDestroy();
    if (GimmickManager.Instance != null)
        GimmickManager.Instance.Unregister(this);
}
```

### 6.3 Activate에서 오브젝트 생성이 필요한 경우
- 마스터 클라이언트 체크를 Activate 내부에서 해야 함:
```csharp
public void Activate()
{
    _isGimmickActive = true;
    if (PhotonNetwork.IsMasterClient)
    {
        // PhotonNetwork.Instantiate(...)
    }
}
```

### 6.4 새로운 그룹 추가
1. `GimmickGroupType` enum에 항목 추가
2. 해당 그룹의 기믹들이 `GroupType`을 새 값으로 선언
3. 활성화 시점을 결정하는 외부 클래스에서 `ActivateRandomByGroup(새그룹, count)` 호출

---

## 7. 씬 설정 체크리스트

각 인게임 맵 씬(Beach1, Dock1, Forest1)에 다음이 필요:

- [ ] **GimmickManager** 오브젝트 (PhotonView 자동 추가됨)
- [ ] **AirDropSpawner** 오브젝트 (`_airDropJetPrefab` 참조 설정)
- [ ] **HurryUpProduction** 오브젝트 (Canvas 하위, 텍스트 UI + Y좌표 설정)
- [ ] 기존 기믹 오브젝트들 (Cannon, CrabSpawner 등) — 이미 배치되어 있으면 수정 불필요

---

## 8. 현재 등록된 기믹 목록

| 기믹 | GimmickType | GroupType | 파일 |
|------|-------------|-----------|------|
| Cannon | Cannon | Start | `Gimmick/Cannon/Cannon.cs` |
| RotationPlatform | RotationPlatform | Start | `Gimmick/RotationPlatform/RotationPlatform.cs` |
| ValleyBall | ValleyBall | Start | `Gimmick/ValleyBall/ValleyBall.cs` |
| CrabSpawner | CrabSpawner | During | `Gimmick/Crab/CrabSpawner.cs` |
| AirDropSpawner | AirDropSpawner | During | `Gimmick/AirDrop/AirDropSpawner.cs` |

---

## 9. 코드 수정 필요사항

### 9.1 GimmickManager.OnGameStart()에서 During 그룹 자동 활성화 제거

**파일**: `Assets/02.Scripts/Gimmick/Core/GimmickManager.cs`

현재 `OnGameStart()`에서 `Start`와 `During` 그룹을 모두 활성화하고 있음. During 그룹은 게임 시작 시 자동 활성화되면 안 되므로, 해당 줄을 제거해야 함.

```csharp
// 변경 전
private void OnGameStart()
{
    if (!PhotonNetwork.IsMasterClient) return;
    ActivateAllByGroup(GimmickGroupType.Start);
    ActivateAllByGroup(GimmickGroupType.During);  // ← 이 줄 제거
}

// 변경 후
private void OnGameStart()
{
    if (!PhotonNetwork.IsMasterClient) return;
    ActivateAllByGroup(GimmickGroupType.Start);
}
```

### 9.2 During 그룹 기믹의 활성화 호출 주체 결정

During 그룹(CrabSpawner, AirDropSpawner)을 언제, 어디서 활성화할지 결정 필요:
- 예: 일정 시간 경과 후, 특정 이벤트 발생 시 등
- 호출 예시: `GimmickManager.Instance.ActivateAllByGroup(GimmickGroupType.During);`
- 또는 개별 호출: `GimmickManager.Instance.ActivateByType(GimmickType.AirDropSpawner);`
