# 플레이어 이벤트 시스템 작업내역서

## 작업 개요

`플레이어이벤트_작업계획서.md`에 따라 PlayerEventManager 시스템을 구현하였다.
기존 Player.cs에 분산된 6개 이벤트를 중앙화하고, 16개 신규 이벤트를 추가하였다.

---

## 신규 생성 파일

| 파일 | 경로 | 설명 |
|------|------|------|
| PlayerEventManager.cs | Assets/02.Scripts/Player/ | DontDestroySingleton 기반 싱글톤. actorNumber별 PlayerEvents 관리 |
| PlayerEvents.cs | Assets/02.Scripts/Player/ | 22개 이벤트 선언 + Invoke 래퍼 + Clear() |
| PlayerEventContext.cs | Assets/02.Scripts/Player/ | DeathContext, AttackHitContext, KillContext, DamagedContext 구조체 |

---

## 수정 파일 목록

### Phase 2: 기존 이벤트 마이그레이션

| 파일 | 변경 내용 |
|------|----------|
| Player.cs | 이벤트 6개 제거, `ActorNumber` 프로퍼티 추가, Invoke/Start 메서드를 PlayerEventManager 호출로 변경 |
| CameraController.cs | SetTarget/OnDisable의 구독/해제를 PlayerEventManager로 변경. **기존 버그 수정 (라인 73 `+=` → `-=`)** |
| PlayerBaseState.cs | OnEnter/OnExit의 OnHit 구독/해제 변경 |
| PlayerDieState.cs | OnEnter의 OnHit 해제 변경 |
| SuperAmorBuff.cs | StartBuff/EndBuff의 OnHit 구독/해제 변경 |
| PlayerCoolTime.cs | Start/OnDestroy의 OnNormalAttack, OnSpecialAttack 구독/해제 변경 |
| UI_Ultimate.cs | FindAndSubscribeToPlayer/OnDestroy의 궁극기 이벤트 구독/해제 변경 |

### Phase 3: 생명주기 이벤트

| 파일 | 변경 내용 |
|------|----------|
| PlayerSpawner.cs | GeneratePlayers() 두 오버로드에 OnSpawned 발행 추가 |
| PlayerDieState.cs | OnEnter에 OnDeath(DeathContext) 발행, StartResurrection에 OnResurrected 발행, HandlePermanentDeath에 OnDespawned 발행 추가 |
| PlayerStat.cs | `_lastIsNormalAttack` 필드 + `LastIsNormalAttack` 프로퍼티 추가, DecreaseHP에서 저장 |

### Phase 4: 공격/피격 이벤트

| 파일 | 변경 내용 |
|------|----------|
| Explosion.cs | TakeDamage 호출 직후 공격자 로컬에서 OnAttackHit(AttackHitContext) 발행 추가 |
| PlayerStat.cs | RPC_IncreaseTotalKillCount 시그니처 확장 (victimActorNumber, isLastKill, isNormalAttack), OnKillConfirmed(KillContext) 발행 추가 |
| PlayerDamageController.cs | RPC_TakeDamage 내 OnDamaged(DamagedContext) 발행 추가 |
| PlayerUltimateController.cs | ExecuteUltimate 내 OnUltimateUsed 발행 추가 |

### Phase 5: 패널티 이벤트

| 파일 | 변경 내용 |
|------|----------|
| PlayerGunpowderController.cs | 경고 시작 시 OnNoAttackPenaltyStart, 패널티 발동 시 OnNoAttackPenaltyTriggered, ClearNoAttackWarning에서 OnNoAttackPenaltyReset 발행 추가 |

### Phase 6: 물리/이동 이벤트

| 파일 | 변경 내용 |
|------|----------|
| PlayerFallState.cs | OnEnter에 OnAirborne, DetectLanding에 OnLanded 발행 추가 |
| PlayerJumpDashState.cs | 착지 확정 시 OnLanded 발행 추가 |
| Player.cs | OnCollisionEnter2D, OnTriggerEnter2D 메서드 추가 → OnPlayerContact, OnObjectContact 발행 |

### Phase 7: GP 이벤트

| 파일 | 변경 내용 |
|------|----------|
| PlayerStat.cs | IncreaseGP에 OnGPGained, DecreaseGP에 OnGPLost, RPC_ChangeGP에 OnGPSet 발행 추가 |

### Phase 8: 아이템 이벤트

- PlayerEvents.cs에 OnItemPickedUp, OnItemUsed 선언 및 Invoke 래퍼만 작성 (발행 연결은 인게임 아이템 시스템 구현 시 추후 진행)

---

## 작업계획서 대비 차이점

| 항목 | 계획서 | 실제 구현 |
|------|--------|----------|
| ActorNumber | Player에 이미 존재하는 것으로 가정 | `public int ActorNumber => PhotonView.OwnerActorNr;` 프로퍼티 신규 추가 |
| CameraController.RemoveTarget() | RemoveTarget 메서드 기준 | 실제 코드는 OnDisable() 메서드였으므로 해당 메서드에 적용 |
| CameraController 버그 | 미언급 | SetTarget 라인 73의 `+=` → `-=` 버그 함께 수정 |
| RPC_IncreaseTotalKillCount | 변경 없음 | KillContext 전달을 위해 파라미터 3개 추가 (victimActorNumber, isLastKill, isNormalAttack) |
| OnDeath의 isNormalAttack | 접근 경로 미명시 | PlayerStat에 `_lastIsNormalAttack` 필드 신규 추가하여 해결 |

---

## 주의사항

- `RPC_IncreaseTotalKillCount` 시그니처가 변경되었으므로, 멀티플레이 테스트 시 모든 클라이언트가 동일 빌드를 사용해야 한다.
- PlayerEventManager는 DontDestroySingleton이므로 씬에 미리 배치하지 않아도 자동 생성된다.
