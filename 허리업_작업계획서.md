# 허리업 페이즈 작업계획서

> 작성일: 2026-03-24
> 브랜치: KKH_Test

---

## 1. 기획 요약

라운드 종료 시간까지 **30초** 남았을 때 허리업 페이즈가 실행된다.

### 1.1 연출 순서

| 순서 | 시점 | 내용 |
|------|------|------|
| 1 | 30초 남은 시점 | **배경 음악 일시 정지** |
| 2 | 동시 | 'Hurry Up!' 텍스트가 **화면 하단→중앙**으로 **1초**간 올라옴 |
| 3 | 텍스트 도착 후 | **1.5초간 0.5초마다** 붉은색↔노란색 점멸 (총 3회 전환) |
| 4 | 점멸 종료 후 | 텍스트가 **중앙→상단**으로 **1초**간 날아감 |
| 5 | 상단 도착 후 | **1초 후** 텍스트 비활성화 |
| 6 | 텍스트 비활성화 후 | **배경 음악 1.2배속**으로 **처음부터** 재생 |
| 7 | BGM 재생 후 | 허리업 기믹 중 **1가지**가 **랜덤**으로 실행 |

### 1.2 타임라인 (총 ~4.5초)

```
[0.0s] BGM 일시정지 + 텍스트 하단→중앙 이동 시작 (30초 남은 시점)
[1.0s] 텍스트 중앙 도착, 점멸 시작
       [1.0s] 색상A → [1.5s] 색상B → [2.0s] 색상A
[2.5s] 점멸 종료, 텍스트 중앙→상단 이동 시작
[3.5s] 텍스트 상단 도착
[4.5s] 텍스트 비활성화 → BGM 1.2배속 재생 → 랜덤 기믹 실행
```

---

## 2. 현재 시스템 분석

### 2.1 관련 기존 시스템

| 시스템 | 파일 | 활용 포인트 |
|--------|------|------------|
| 타이머 | `IngameTimer.cs` | `_timer` 값으로 30초 남은 시점 감지 |
| 사운드 | `SoundManager.cs` | `_currentBGM` 필드로 BGM 일시정지/재생 관리 |
| Sound 컴포넌트 | `Sound.cs` | `GetAudioSource()`로 AudioSource 접근 → pitch 조절 |
| 이벤트 | `EventManager.cs` | 허리업 이벤트 추가 |
| 게임 시작 연출 | `GameStartProduction.cs` | DOTween Sequence 패턴 참고 |
| BGM 재생 | `ClientManager.cs` | `PlayBGM(sceneName)` — 씬별 BGM 클립명 매핑 |
| 게임 상태 | `EGameState.cs` | 현재 상태: Waiting, Ready, Playing, Result, GameOver, Ultimate, Tutorial |
| 기믹 | `Gimmick/` 폴더 | AirDrop, Cannon, Crab, RotationPlatform, ValleyBall 등 |

### 2.2 타이머 현재 동작

```csharp
// IngameTimer.cs
private float _timer;  // 초 단위, 감소

private void GameTimer()
{
    _timer -= Time.deltaTime;

    if (_timer <= 0) { GameOver(); }

    // 정수 변경 시 UI 갱신
    int currentTime = Mathf.FloorToInt(_timer);
    if (_previousTime != currentTime) { ... }
}
```

### 2.3 BGM 현재 동작

```csharp
// SoundManager.cs
private Sound _currentBGM;  // 현재 재생 중인 BGM

public Sound PlayGlobalSound(string clipName, SoundType type, float delay, bool isLoop)
{
    // BGM 타입이면 기존 BGM 정지 후 새 BGM 재생
    if (type == SoundType.BGM && _currentBGM != null)
    {
        StopLoopSound(_currentBGM.name);
    }
    // ...
}

// Sound.cs
public AudioSource GetAudioSource() { return _audioSource; }
```

### 2.4 기존 BGM 재생 방식

```csharp
// ClientManager.cs — 씬별 BGM 클립명
case "Forest1": "Forest1"
case "Dock1":   "Dock1"
case "Beach1":  "Beach1"
```

---

## 3. 수정 대상 파일 및 작업 내역

### 3.1 EventManager.cs
**경로**: `Assets/02.Scripts/Evenet/EventManager.cs`

**추가 사항:**
- `OnHurryUp` 이벤트 추가 — 허리업 페이즈 시작 신호
- `HurryUp()` 발행 메서드 추가

```csharp
public event Action OnHurryUp;
public void HurryUp()
{
    OnHurryUp?.Invoke();
}
```

---

### 3.2 IngameTimer.cs
**경로**: `Assets/02.Scripts/Map/IngameTimer.cs`

**추가 사항:**
- `_isHurryUp` (bool): 허리업 중복 실행 방지 플래그
- `HURRY_UP_TIME` (const float): 허리업 트리거 시간 (30초)
- `GameTimer()` 내부에 허리업 체크 로직 추가

**변경 로직:**
```csharp
private const float HURRY_UP_TIME = 30f;
private bool _isHurryUp = false;

private void GameTimer()
{
    _timer -= Time.deltaTime;

    // 허리업 체크 (30초 남았을 때, 1회만 실행)
    if (!_isHurryUp && _timer <= HURRY_UP_TIME && _timer > 0)
    {
        _isHurryUp = true;
        EventManager.Instance.HurryUp();
    }

    // 기존 로직 유지...
}
```

**주의사항:**
- 타이머는 모든 클라이언트에서 로컬로 동작하므로, 각 클라이언트가 독립적으로 30초 시점을 감지함
- `Time.deltaTime` 기반이므로 클라이언트 간 약간의 오차가 있을 수 있으나, 연출용이므로 허용 범위 내

---

### 3.3 SoundManager.cs
**경로**: `Assets/02.Scripts/Sound/SoundManager.cs`

**추가 사항:**
- `PauseBGM()`: 현재 BGM 일시 정지
- `ReplayBGMWithPitch(float pitch)`: 현재 BGM을 지정 pitch로 처음부터 재생
- `GetCurrentBGM()`: 현재 BGM Sound 객체 반환 (외부 접근용)

```csharp
// 현재 BGM을 일시 정지
public void PauseBGM()
{
    if (_currentBGM != null)
    {
        _currentBGM.GetAudioSource().Pause();
    }
}

// 현재 BGM을 지정된 pitch로 처음부터 재생
public void ReplayBGMWithPitch(float pitch)
{
    if (_currentBGM != null)
    {
        AudioSource source = _currentBGM.GetAudioSource();
        source.pitch = pitch;
        source.time = 0f;  // 처음부터
        source.Play();
    }
}
```

---

### 3.4 HurryUpProduction.cs (신규)
**경로**: `Assets/02.Scripts/HurryUp/HurryUpProduction.cs`

> 허리업 관련 신규 스크립트는 모두 `Assets/02.Scripts/HurryUp/` 폴더에 배치한다.

**책임:** 허리업 페이즈의 전체 연출을 관리하는 클래스

**구조:**
```csharp
using DG.Tweening;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class HurryUpProduction : MonoBehaviour
{
    [Header("Hurry Up 텍스트")]
    [SerializeField] private RectTransform _hurryUpText;
    [SerializeField] private TextMeshProUGUI _hurryUpTextComponent;

    [Header("위치 설정")]
    [SerializeField] private float _bottomY;    // 하단 시작 Y 좌표
    [SerializeField] private float _centerY;    // 중앙 Y 좌표
    [SerializeField] private float _topY;       // 상단 Y 좌표

    [Header("색상 설정")]
    [SerializeField] private Color _redColor = Color.red;
    [SerializeField] private Color _yellowColor = Color.yellow;

    private void Awake()
    {
        EventManager.Instance.OnHurryUp += PlayHurryUp;
        _hurryUpText.gameObject.SetActive(false);
    }

    private void PlayHurryUp()
    {
        // 1. BGM 일시 정지
        SoundManager.Instance.PauseBGM();

        // 2. 텍스트 초기 위치 설정 (하단)
        _hurryUpText.anchoredPosition = new Vector2(
            _hurryUpText.anchoredPosition.x, _bottomY);
        _hurryUpText.gameObject.SetActive(true);

        // 3. DOTween Sequence 구성
        Sequence sequence = DOTween.Sequence();

        // [0.0s~1.0s] 하단→중앙 이동 (1초)
        sequence.Append(
            _hurryUpText.DOAnchorPosY(_centerY, 1f).SetEase(Ease.OutBack));

        // [1.0s~2.5s] 1.5초간 0.5초마다 붉은색↔노란색 점멸
        sequence.Append(
            _hurryUpTextComponent.DOColor(_redColor, 0f));   // 붉은색 시작
        sequence.AppendInterval(0.5f);
        sequence.Append(
            _hurryUpTextComponent.DOColor(_yellowColor, 0f)); // 노란색
        sequence.AppendInterval(0.5f);
        sequence.Append(
            _hurryUpTextComponent.DOColor(_redColor, 0f));    // 붉은색
        sequence.AppendInterval(0.5f);

        // [2.5s~3.5s] 중앙→상단 이동 (1초)
        sequence.Append(
            _hurryUpText.DOAnchorPosY(_topY, 1f).SetEase(Ease.InBack));

        // [3.5s~4.5s] 1초 대기 후 비활성화
        sequence.AppendInterval(1f);
        sequence.AppendCallback(() =>
        {
            _hurryUpText.gameObject.SetActive(false);

            // BGM 1.2배속으로 처음부터 재생
            SoundManager.Instance.ReplayBGMWithPitch(1.2f);

            // GimmickManager에게 허리업 그룹 기믹 중 랜덤 1개 실행 요청
            GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1);
        });
    }

    private void OnDisable()
    {
        EventManager.Instance.OnHurryUp -= PlayHurryUp;
    }
}
```

**프리팹/씬 설정:**
- 각 인게임 맵 씬(Beach1, Dock1, Forest1)의 Canvas 하위에 `HurryUpProduction` 오브젝트 배치
- 'Hurry Up!' 텍스트 UI를 자식으로 배치
- `_bottomY`, `_centerY`, `_topY` 값은 캔버스 해상도에 맞게 Inspector에서 설정

---

### 3.5 허리업 기믹 — 모듈화된 기믹 시스템 활용

> 허리업 기믹은 별도의 `기믹_모듈화_작업계획서.md`에서 설계하는 **모듈화된 기믹 시스템(`IGimmick` + `GimmickManager`)** 위에 구현한다.
> 기믹 모듈화 작업이 선행되어야 허리업 기믹을 깔끔하게 추가할 수 있다.

**핵심 동작:**
- 허리업 기믹들은 `GimmickGroupType.HurryUp`으로 분류되어 GimmickManager에 자동 등록됨
- 연출 완료 후 `ActivateRandomByGroup(GimmickGroupType.HurryUp, 1)` 호출
- 마스터 클라이언트가 HurryUp 그룹 기믹 중 **랜덤 1개**를 선택
- RPC로 모든 클라이언트에 전파 → 같은 기믹이 실행됨

**허리업 기믹 구현 클래스 경로**: `Assets/02.Scripts/HurryUp/Gimmick/`

기믹의 구체적인 내용은 기획 확정 후 구현하되, `IGimmick` 인터페이스를 구현하여 확장한다.

```csharp
// 예시: 허리업 전용 기믹
public class HurryUpBombRain : MonoBehaviour, IGimmick
{
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.BombRain;
    public GimmickGroupType GroupType => GimmickGroupType.HurryUp;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
        // 마스터 클라이언트만 폭탄 생성
        if (!PhotonNetwork.IsMasterClient) return;
        // 랜덤 위치에 폭탄 생성 로직
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
    }

    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
}
```

---

## 4. 작업 순서

### Phase 1: 이벤트 및 트리거 (EventManager + IngameTimer)
1. `EventManager.cs`에 `OnHurryUp` 이벤트 + `HurryUp()` 메서드 추가
2. `IngameTimer.cs`에 `_isHurryUp` 플래그 + 허리업 체크 로직 추가

### Phase 2: 사운드 시스템 확장 (SoundManager)
1. `PauseBGM()` 메서드 추가
2. `ReplayBGMWithPitch(float pitch)` 메서드 추가

### Phase 3: 연출 시스템 구현 (HurryUpProduction)
1. `HurryUpProduction.cs` 신규 작성
2. DOTween Sequence로 텍스트 이동 + 점멸 + 비활성화 타이밍 구성
3. BGM 일시정지 → 1.2배속 재생 연동
4. 각 인게임 맵 씬에 프리팹 배치 + Inspector 값 설정

### Phase 4: 기믹 시스템 구현 (IGimmick 구현 + 자동 등록)
1. 기획 확정된 기믹들을 `IGimmick` 구현 클래스로 작성 (GroupType = HurryUp)
2. `GimmickType` enum에 해당 기믹 항목 추가
3. 맵 씬에 기믹 오브젝트 배치 → Start()에서 GimmickManager에 자동 등록

### Phase 5: 테스트 및 미세 조정
1. DOTween 이징, 이동 좌표 등 연출 미세 조정
2. BGM pitch 변경 확인
3. 기믹 랜덤 실행 확인

---

## 5. 수정 파일 요약

| 파일 | 작업 유형 | 우선순위 |
|------|----------|---------|
| `EventManager.cs` | 이벤트 추가 | Phase 1 |
| `IngameTimer.cs` | 타이머 체크 로직 추가 | Phase 1 |
| `SoundManager.cs` | BGM 제어 메서드 추가 | Phase 2 |
| `HurryUpProduction.cs` (신규) | 연출 전체 관리 | Phase 3 |
| 기믹 구현 클래스들 (신규) | IGimmick 구현 (GroupType=HurryUp) + 자동 등록 | Phase 4 |

---

## 6. 주의사항

### 6.1 동기화
- **타이머 트리거**: 각 클라이언트가 로컬 `_timer`로 독립 감지 → 연출용이므로 약간의 오차 허용
- **BGM 제어**: 로컬 처리 (네트워크 동기화 불필요)
- **텍스트 연출**: 로컬 UI 처리 (네트워크 동기화 불필요)
- **기믹 실행**: 기믹 종류에 따라 마스터 클라이언트 권한 필요 (오브젝트 생성이 필요한 경우)

### 6.2 기믹 동기화
- 허리업 기믹은 `GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1)`으로 실행
- 마스터 클라이언트가 HurryUp 그룹 기믹 중 **랜덤 1개 선택** → RPC로 모든 클라이언트에 전파
- HurryUpProduction에 PhotonView 불필요 — GimmickManager가 동기화 담당

```csharp
// HurryUpProduction.cs — DOTween Sequence 마지막 콜백
sequence.AppendCallback(() =>
{
    _hurryUpText.gameObject.SetActive(false);
    SoundManager.Instance.ReplayBGMWithPitch(1.2f);

    // GimmickManager에게 허리업 그룹 기믹 중 랜덤 1개 실행 요청
    GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1);
});
```

### 6.3 엣지 케이스

| 상황 | 처리 방법 |
|------|----------|
| 플레이 시간이 30초 미만인 방 설정 | `_timer`의 초기값 자체가 30초 미만이면 허리업 즉시 실행될 수 있음 → 초기값 체크 후 허리업 스킵 처리 필요 |
| 허리업 연출 중 게임 종료 | DOTween Sequence `Kill()` 처리 + BGM pitch 초기화 |
| 허리업 연출 중 플레이어 사망 | 연출은 UI이므로 플레이어 상태와 무관하게 진행 |
| 튜토리얼 모드 | `EGameState.Tutorial`에서는 허리업 미실행 (IngameTimer에서 체크) |

### 6.4 게임 종료 시 정리

```csharp
// HurryUpProduction.cs — OnDisable 또는 GameOver 이벤트 구독
private void Cleanup()
{
    _sequence?.Kill();
    _hurryUpText.gameObject.SetActive(false);
    // BGM pitch는 씬 전환 시 새 BGM으로 교체되므로 별도 복구 불필요
}
```

### 6.5 DOTween SetUpdate 설정
- `GameStartProduction`은 `.SetUpdate(true)`를 사용하여 `Time.timeScale = 0`에서도 동작
- 허리업 연출은 게임 진행 중(`Playing`) 실행되므로 `.SetUpdate(true)` 불필요
- 단, 궁극기 사용 시 `Time.timeScale = 0`이 될 수 있으므로 `.SetUpdate(true)` 사용 권장

---

## 7. 확인 사항

### 7.1 기획 확인 필요
- [ ] 허리업 기믹의 구체적인 종류 및 내용 확정
- [ ] 'Hurry Up!' 텍스트의 폰트, 크기, 아웃라인 등 비주얼 스펙
- [ ] 점멸 시 색상 전환이 즉시 전환인지 페이드 전환인지 확인 (현재 기획: 즉시 전환)
- [ ] 허리업 전용 사운드 이펙트(SFX) 필요 여부
- [ ] 플레이 시간 30초 미만 방에서의 허리업 처리 방침

### 7.2 테스트 시나리오
- [ ] 30초 남은 시점에서 텍스트 연출이 정상 재생되는지
- [ ] BGM이 일시 정지 후 1.2배속으로 처음부터 재생되는지
- [ ] 기믹이 랜덤으로 1개만 실행되는지
- [ ] 멀티플레이어 환경에서 모든 클라이언트가 같은 기믹을 실행하는지
- [ ] 허리업 연출 중 게임 종료 시 정상 정리되는지
- [ ] 플레이 시간 1분 이하 방에서 허리업 처리 확인
