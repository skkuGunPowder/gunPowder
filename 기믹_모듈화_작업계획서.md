# 기믹 모듈화 작업계획서

> 작성일: 2026-03-24
> 최종 수정: 2026-03-24
> 브랜치: KKH_Test

---

## 1. 개요

### 1.1 목표
- 모든 기믹을 `IGimmick` 인터페이스로 통일
- `GimmickManager`가 맵별로 기믹을 중앙 관리
- 기믹 식별은 `GimmickType` enum, 그룹 분류는 `GimmickGroupType` enum
- GimmickManager API
  - `ActivateByType(GimmickType)` — 특정 기믹 지정 실행
  - `ActivateRandomByGroup(GimmickGroupType, int count)` — 그룹에서 랜덤 N개 실행
  - `ActivateAllByGroup(GimmickGroupType)` — 그룹 전체 실행
  - `DeactivateByType(GimmickType)` — 특정 기믹 비활성화
  - `DeactivateAll()` — 전체 비활성화
- Start/During 그룹은 게임 시작 시 GimmickManager가 자동 활성화
- HurryUp 그룹은 허리업 연출 완료 후 HurryUpProduction이 호출
- 그 외 특정 조건 활성화는 외부 클래스가 판단

### 1.2 왜 IGimmick 인터페이스인가
- AirDropJet은 Jet을 상속 중, ValleyBall은 Bomb을 상속 중 → 추상 클래스(GimmickBase) 사용 불가
- 프로젝트에 IAirDropItem, IBuff, IDamagable 등 인터페이스 패턴 사용 중 → 기존 컨벤션과 일치
- 기존/신규 기믹 모두 동일하게 적용 가능

---

## 2. 핵심 구조

### 2.1 Enum 2개

```csharp
// 개별 기믹 식별용 — 특정 기믹을 지정 호출할 때 사용
public enum GimmickType
{
    Cannon,
    CrabSpawner,
    AirDropSpawner,
    RotationPlatform,
    ValleyBall,
    BombRain,
    MeteorStrike,
    // 기믹 추가될 때마다 여기에 추가
}

// 기믹 그룹 분류 — 그룹 단위로 랜덤 호출할 때 사용
public enum GimmickGroupType
{
    Start,      // 게임 시작 시 실행되는 기믹
    During,     // 게임 중반에 실행되는 기믹
    HurryUp,    // 허리업 페이즈에 실행되는 기믹
}
```

### 2.2 IGimmick

```csharp
public interface IGimmick
{
    GimmickType Type { get; }
    GimmickGroupType GroupType { get; }
    void Activate();
    void Deactivate();
    bool IsActive { get; }
}
```

### 2.3 GimmickManager API

```csharp
// 특정 기믹을 지정해서 실행 (같은 Type이 여러 개면 전부 실행)
public void ActivateByType(GimmickType type)

// 해당 그룹의 기믹 중 랜덤 N개 실행
public void ActivateRandomByGroup(GimmickGroupType group, int count = 1)

// 해당 그룹의 기믹 전부 실행
public void ActivateAllByGroup(GimmickGroupType group)

// 특정 타입 비활성화
public void DeactivateByType(GimmickType type)

// 전체 비활성화 (게임 종료 시)
public void DeactivateAll()
```

### 2.4 전체 흐름

```
[기믹 개발자]
  1. IGimmick 구현한 스크립트 작성 (Type, GroupType 선언)
  2. 씬에 기믹 오브젝트 배치
  3. Start()에서 GimmickManager.Instance.Register(this) 호출
  4. 끝 — 자동으로 Type/GroupType별로 분류됨

[런타임 — 자동 활성화]
  게임 시작(OnGameStart) → GimmickManager가 Start/During 그룹 전부 자동 활성화
  허리업 연출 완료 → HurryUpProduction이 ActivateRandomByGroup(HurryUp, 1) 호출

[런타임 — 수동 호출 (외부 클래스가 조건 판단)]
  특정 조건 → GimmickManager.ActivateByType(GimmickType.Cannon)
  특정 조건 → GimmickManager.DeactivateByType(GimmickType.Cannon)
```

### 2.5 하이어라키 구조

```
[맵 씬]
├── GimmickManager          ← GimmickManager 컴포넌트
├── Cannon_1                ← Cannon (IGimmick, Type=Cannon, Group=Start)
├── Cannon_2                ← Cannon (IGimmick, Type=Cannon, Group=Start)
├── CrabSpawner             ← CrabSpawner (IGimmick, Type=CrabSpawner, Group=During)
├── AirDropSpawner          ← AirDropSpawner (IGimmick, Type=AirDropSpawner, Group=During)
├── BombRain                ← HurryUpBombRain (IGimmick, Type=BombRain, Group=HurryUp)
└── MeteorStrike            ← HurryUpMeteor (IGimmick, Type=MeteorStrike, Group=HurryUp)
```

---

## 3. 호출 예시

| 상황 | 호출 코드 | 동작 | 호출 주체 |
|------|----------|------|----------|
| 게임 시작 | `ActivateAllByGroup(Start)` + `ActivateAllByGroup(During)` | Start/During 그룹 전부 실행 | GimmickManager (자동) |
| 허리업 | `ActivateRandomByGroup(HurryUp, 1)` | HurryUp 그룹 중 랜덤 1개 | HurryUpProduction (자동) |
| 특정 기믹 실행 | `ActivateByType(GimmickType.Cannon)` | Cannon 타입 전부 실행 | 외부 클래스 (수동) |
| 그룹 랜덤 실행 | `ActivateRandomByGroup(During, 2)` | During 그룹 중 랜덤 2개 | 외부 클래스 (수동) |
| 특정 기믹 비활성화 | `DeactivateByType(GimmickType.Cannon)` | Cannon 타입 전부 비활성화 | 외부 클래스 (수동) |

---

## 4. 폴더 구조

```
Assets/02.Scripts/
├── Gimmick/
│   ├── Core/                         # [신규] 기믹 공통 모듈
│   │   ├── IGimmick.cs               # 기믹 인터페이스
│   │   ├── GimmickType.cs            # 개별 기믹 식별 enum
│   │   ├── GimmickGroupType.cs       # 기믹 그룹 분류 enum
│   │   └── GimmickManager.cs         # 기믹 중앙 관리자
│   ├── AirDrop/                      # 기존 유지 + AirDropSpawner.cs 신규
│   ├── Cannon/                       # 기존 Cannon.cs에 IGimmick 추가
│   ├── Crab/                         # 기존 CrabSpawner.cs에 IGimmick 추가
│   ├── OneWayPlatform/               # 수정 없음
│   ├── RotationPlatform/             # 기존 RotationPlatform.cs에 IGimmick 추가
│   └── ValleyBall/                   # 기존 ValleyBall.cs에 IGimmick 추가 (선택)
│
├── HurryUp/                          # [신규] 허리업 전용 폴더
│   ├── HurryUpProduction.cs          # 허리업 연출 관리
│   └── Gimmick/                      # 허리업 전용 기믹들
│       └── (기획 확정 후 추가)
```

---

## 5. 신규 파일 상세

### 5.1 GimmickType.cs
**경로**: `Assets/02.Scripts/Gimmick/Core/GimmickType.cs`

```csharp
// 개별 기믹 식별 enum
// 특정 기믹을 지정 호출할 때 사용
// 기믹 추가 시 여기에 항목 추가
public enum GimmickType
{
    Cannon,
    CrabSpawner,
    AirDropSpawner,
    RotationPlatform,
    ValleyBall,
    BombRain,
    MeteorStrike,
}
```

---

### 5.2 GimmickGroupType.cs
**경로**: `Assets/02.Scripts/Gimmick/Core/GimmickGroupType.cs`

```csharp
// 기믹 그룹 분류 enum
// 그룹 단위로 랜덤 호출할 때 사용
public enum GimmickGroupType
{
    Start,      // 게임 시작 시 실행
    During,     // 게임 중반에 실행
    HurryUp,    // 허리업 페이즈에 실행
}
```

---

### 5.3 IGimmick.cs
**경로**: `Assets/02.Scripts/Gimmick/Core/IGimmick.cs`

```csharp
// 모든 기믹이 구현하는 인터페이스
// 기믹 개발자는 이 인터페이스만 구현하면 됨
// Start()에서 GimmickManager.Instance.Register(this) 호출
// OnDestroy()에서 GimmickManager.Instance.Unregister(this) 호출
public interface IGimmick
{
    // 이 기믹의 개별 식별 타입
    GimmickType Type { get; }

    // 이 기믹의 그룹 분류
    GimmickGroupType GroupType { get; }

    // 기믹 활성화
    void Activate();

    // 기믹 비활성화
    void Deactivate();

    // 현재 활성 상태 여부
    bool IsActive { get; }
}
```

---

### 5.4 GimmickManager.cs
**경로**: `Assets/02.Scripts/Gimmick/Core/GimmickManager.cs`

```csharp
using System.Collections.Generic;
using System.Linq;
using Photon.Pun;
using UnityEngine;

// 맵별 기믹 중앙 관리자
// 맵 Scene마다 1개 존재, Scene 전환 시 파괴됨 (PhotonSingleton)
// 기믹들이 Start()에서 Register하면 Type/GroupType별로 자동 분류
// Start/During 그룹은 OnGameStart 시 자동 활성화
// HurryUp 그룹은 HurryUpProduction이 연출 완료 후 호출
[RequireComponent(typeof(PhotonView))]
public class GimmickManager : PhotonSingleton<GimmickManager>
{
    // Type별 기믹 관리 (같은 Type이 여러 개일 수 있음)
    private Dictionary<GimmickType, List<IGimmick>> _gimmicksByType = new Dictionary<GimmickType, List<IGimmick>>();

    // GroupType별 기믹 관리
    private Dictionary<GimmickGroupType, List<IGimmick>> _gimmicksByGroup = new Dictionary<GimmickGroupType, List<IGimmick>>();

    private void Start()
    {
        EventManager.Instance.OnGameStart += OnGameStart;
    }

    private void OnGameStart()
    {
        if (!PhotonNetwork.IsMasterClient) return;

        // Start 그룹: 게임 시작 시 전부 활성화
        ActivateAllByGroup(GimmickGroupType.Start);

        // During 그룹: 기존 동작 유지 (게임 시작 시 활성화)
        ActivateAllByGroup(GimmickGroupType.During);
    }

    // 해당 그룹의 기믹 전부 실행
    public void ActivateAllByGroup(GimmickGroupType group)
    {
        if (!PhotonNetwork.IsMasterClient) return;

        if (!_gimmicksByGroup.ContainsKey(group)) return;

        int count = _gimmicksByGroup[group].Count;
        if (count == 0) return;

        ActivateRandomByGroup(group, count);
    }

    // 등록
    public void Register(IGimmick gimmick)
    {
        // Type별 등록
        if (!_gimmicksByType.ContainsKey(gimmick.Type))
            _gimmicksByType[gimmick.Type] = new List<IGimmick>();

        if (!_gimmicksByType[gimmick.Type].Contains(gimmick))
            _gimmicksByType[gimmick.Type].Add(gimmick);

        // GroupType별 등록
        if (!_gimmicksByGroup.ContainsKey(gimmick.GroupType))
            _gimmicksByGroup[gimmick.GroupType] = new List<IGimmick>();

        if (!_gimmicksByGroup[gimmick.GroupType].Contains(gimmick))
            _gimmicksByGroup[gimmick.GroupType].Add(gimmick);
    }

    // 해제
    public void Unregister(IGimmick gimmick)
    {
        if (_gimmicksByType.ContainsKey(gimmick.Type))
            _gimmicksByType[gimmick.Type].Remove(gimmick);

        if (_gimmicksByGroup.ContainsKey(gimmick.GroupType))
            _gimmicksByGroup[gimmick.GroupType].Remove(gimmick);
    }

    // 특정 타입의 기믹 전부 실행
    public void ActivateByType(GimmickType type)
    {
        if (!PhotonNetwork.IsMasterClient) return;

        photonView.RPC(nameof(RPC_ActivateByType), RpcTarget.All, (int)type);
    }

    // 특정 그룹에서 랜덤 N개 실행
    // 마스터가 랜덤 선택 → RPC로 선택된 인덱스들 전파
    public void ActivateRandomByGroup(GimmickGroupType group, int count = 1)
    {
        if (!PhotonNetwork.IsMasterClient) return;

        if (!_gimmicksByGroup.ContainsKey(group)) return;

        List<IGimmick> candidates = _gimmicksByGroup[group];
        if (candidates.Count == 0) return;

        // count가 후보 수보다 크면 전부 실행
        count = Mathf.Min(count, candidates.Count);

        // 랜덤 인덱스 선택 (중복 없이)
        List<int> allIndices = new List<int>();
        for (int i = 0; i < candidates.Count; i++)
            allIndices.Add(i);

        int[] selectedIndices = new int[count];
        for (int i = 0; i < count; i++)
        {
            int randomPos = Random.Range(0, allIndices.Count);
            selectedIndices[i] = allIndices[randomPos];
            allIndices.RemoveAt(randomPos);
        }

        photonView.RPC(nameof(RPC_ActivateByGroupIndices), RpcTarget.All, (int)group, selectedIndices);
    }

    // 특정 타입의 기믹 전부 비활성화
    public void DeactivateByType(GimmickType type)
    {
        if (!PhotonNetwork.IsMasterClient) return;

        photonView.RPC(nameof(RPC_DeactivateByType), RpcTarget.All, (int)type);
    }

    // 모든 기믹 비활성화 (게임 종료 시)
    public void DeactivateAll()
    {
        foreach (var pair in _gimmicksByType)
        {
            foreach (IGimmick gimmick in pair.Value)
            {
                if (gimmick.IsActive)
                    gimmick.Deactivate();
            }
        }
    }

    [PunRPC]
    private void RPC_ActivateByType(int typeInt)
    {
        GimmickType type = (GimmickType)typeInt;
        if (!_gimmicksByType.ContainsKey(type)) return;

        foreach (IGimmick gimmick in _gimmicksByType[type])
        {
            gimmick.Activate();
        }
    }

    [PunRPC]
    private void RPC_ActivateByGroupIndices(int groupInt, int[] indices)
    {
        GimmickGroupType group = (GimmickGroupType)groupInt;
        if (!_gimmicksByGroup.ContainsKey(group)) return;

        List<IGimmick> candidates = _gimmicksByGroup[group];
        foreach (int index in indices)
        {
            if (index >= 0 && index < candidates.Count)
            {
                candidates[index].Activate();
            }
        }
    }

    [PunRPC]
    private void RPC_DeactivateByType(int typeInt)
    {
        GimmickType type = (GimmickType)typeInt;
        if (!_gimmicksByType.ContainsKey(type)) return;

        foreach (IGimmick gimmick in _gimmicksByType[type])
        {
            if (gimmick.IsActive)
                gimmick.Deactivate();
        }
    }

    public override void OnDisable()
    {
        base.OnDisable();
        DeactivateAll();

        if (EventManager.Instance != null)
            EventManager.Instance.OnGameStart -= OnGameStart;
    }
}
```

---

## 6. 기존 기믹별 IGimmick 구현 상세

> 모든 기믹은 동일한 패턴으로 IGimmick을 구현한다:
> 1. 클래스 선언에 `, IGimmick` 추가
> 2. `Type`, `GroupType`, `Activate()`, `Deactivate()`, `IsActive` 구현
> 3. `Start()`에서 `GimmickManager.Instance.Register(this)` 호출
> 4. `OnDestroy()`에서 `GimmickManager.Instance.Unregister(this)` 호출
> 5. 기존 로직의 진입점에 `if (!_isGimmickActive) return;` 가드 1줄 추가
> 6. 기존 로직은 일절 변경하지 않음

### 6.1 Cannon.cs

**클래스 선언 변경:**
```csharp
// 변경 전
public class Cannon : MonoBehaviour

// 변경 후
public class Cannon : MonoBehaviour, IGimmick
```

**추가 코드:**
```csharp
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.Cannon;
    public GimmickGroupType GroupType => GimmickGroupType.Start;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
        _isLoaded = false;
        StopAllCoroutines();
    }

    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
```

**기존 코드 수정 (1줄 추가):**
```csharp
    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (!_isGimmickActive) return;   // ← 추가
        if (_isLoaded) return;
        // ... 기존 코드 그대로 ...
    }
```

---

### 6.2 CrabSpawner.cs

**클래스 선언 변경:**
```csharp
// 변경 전
public class CrabSpawner : MonoBehaviour

// 변경 후
public class CrabSpawner : MonoBehaviour, IGimmick
```

**추가 코드:**
```csharp
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.CrabSpawner;
    public GimmickGroupType GroupType => GimmickGroupType.During;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
        if (PhotonNetwork.IsMasterClient)
        {
            SpawnCrabs();
        }
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
        if (PhotonNetwork.IsMasterClient)
        {
            DestroyAllCrabs();
        }
    }

    private void DestroyAllCrabs()
    {
        foreach (GameObject crab in _spawnedCrabs)
        {
            if (crab != null)
            {
                PhotonNetwork.Destroy(crab);
            }
        }
        _spawnedCrabs.Clear();
    }

    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
```

**기존 코드 수정 (1줄 추가):**
```csharp
    private void Update()
    {
        if (!_isGimmickActive) return;   // ← 추가
        if (!PhotonNetwork.IsMasterClient) return;
        // ... 기존 코드 그대로 ...
    }
```

**Crab.cs, BabyCrab.cs, KingCrab.cs: 수정 없음**

---

### 6.3 AirDrop — AirDropSpawner.cs (신규) + IngameTimer.cs (수정)

**AirDrop 스포닝 로직이 현재 IngameTimer.cs에 있으므로 분리한다.**

#### AirDropSpawner.cs (신규)
**경로**: `Assets/02.Scripts/Gimmick/AirDrop/AirDropSpawner.cs`

```csharp
using Photon.Pun;
using UnityEngine;

// AirDrop 스포닝 담당
// 기존 IngameTimer.cs에 있던 로직을 분리
public class AirDropSpawner : MonoBehaviour, IGimmick
{
    [Header("스포닝 설정")]
    [SerializeField] private float _spawnInterval = 30f;
    [SerializeField] private float _spawnChance = 0.1f;
    [SerializeField] private GameObject _airDropJetPrefab;

    private float _timer;
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.AirDropSpawner;
    public GimmickGroupType GroupType => GimmickGroupType.During;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
        _timer = 0f;
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
    }

    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void Update()
    {
        if (!_isGimmickActive) return;
        if (!PhotonNetwork.IsMasterClient) return;
        if (GameManager.Instance.CurrentGameState != EGameState.Playing) return;

        _timer += Time.deltaTime;
        if (_timer > _spawnInterval)
        {
            _timer = 0f;
            if (Random.Range(0f, 1.0f) <= _spawnChance)
            {
                PhotonNetwork.Instantiate(_airDropJetPrefab.name, transform.position, Quaternion.identity);
            }
        }
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
}
```

**AirDropJet.cs, AirDropBox.cs: 수정 없음**

#### IngameTimer.cs (수정)
**경로**: `Assets/02.Scripts/Map/IngameTimer.cs`

**제거 대상:**
```csharp
// 필드 제거
[Header("드랍 떨어지는 시간")]
public float AirDropTime = 30f;
private float _airDropTimer;
[SerializeField] private GameObject _airDropJetPrefab;

// GameTimer() 내부의 AirDrop 스포닝 블록 전체 제거
```

---

### 6.4 RotationPlatform.cs

**클래스 선언 변경:**
```csharp
// 변경 전
public class RotationPlatform : MonoBehaviour

// 변경 후
public class RotationPlatform : MonoBehaviour, IGimmick
```

**추가 코드:**
```csharp
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.RotationPlatform;
    public GimmickGroupType GroupType => GimmickGroupType.Start;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
    }

    // 기존 Awake() 마지막에 추가
    if (GimmickManager.Instance != null)
        GimmickManager.Instance.Register(this);

    // OnDestroy 추가
    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
```

**기존 Rotate() 수정 (1줄 추가):**
```csharp
    public void Rotate(bool isClockWise)
    {
        if (!_isGimmickActive) return;   // ← 추가
        if (_isRotate) return;
        // ... 기존 코드 그대로 ...
    }
```

**SidePlatform.cs: 수정 없음**

---

### 6.5 ValleyBall.cs (선택, 우선순위 낮음)

**클래스 선언 변경:**
```csharp
// 변경 전
public class ValleyBall : Bomb, IDamagable

// 변경 후
public class ValleyBall : Bomb, IDamagable, IGimmick
```

**추가 코드:**
```csharp
    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.ValleyBall;
    public GimmickGroupType GroupType => GimmickGroupType.Start;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        _isGimmickActive = true;
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
    }
```

**Init()에 Register 추가, OnDestroy에 Unregister 추가 필요.**

---

### 6.6 신규 허리업 기믹 예시

**경로**: `Assets/02.Scripts/HurryUp/Gimmick/`

```csharp
using System.Collections;
using Photon.Pun;
using UnityEngine;

// 허리업 기믹 예시: 폭탄 비
public class HurryUpBombRain : MonoBehaviour, IGimmick
{
    [SerializeField] private GameObject _bombPrefab;
    [SerializeField] private int _bombCount = 8;
    [SerializeField] private float _spawnInterval = 0.3f;
    [SerializeField] private Vector2 _spawnRangeX = new Vector2(-15f, 15f);
    [SerializeField] private float _spawnHeight = 20f;

    private bool _isGimmickActive = false;

    public GimmickType Type => GimmickType.BombRain;
    public GimmickGroupType GroupType => GimmickGroupType.HurryUp;
    public bool IsActive => _isGimmickActive;

    public void Activate()
    {
        if (_isGimmickActive) return;
        _isGimmickActive = true;

        // 마스터만 오브젝트 생성
        if (PhotonNetwork.IsMasterClient)
        {
            StartCoroutine(BombRainCoroutine());
        }
    }

    public void Deactivate()
    {
        _isGimmickActive = false;
        StopAllCoroutines();
    }

    private IEnumerator BombRainCoroutine()
    {
        for (int i = 0; i < _bombCount; i++)
        {
            float x = Random.Range(_spawnRangeX.x, _spawnRangeX.y);
            Vector3 pos = new Vector3(x, _spawnHeight, 0f);
            PhotonNetwork.Instantiate(_bombPrefab.name, pos, Quaternion.identity);
            yield return new WaitForSeconds(_spawnInterval);
        }
    }

    private void Start()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Register(this);
    }

    private void OnDestroy()
    {
        if (GimmickManager.Instance != null)
            GimmickManager.Instance.Unregister(this);
    }
}
```

---

## 7. 허리업과의 연동 구조

```
EventManager.OnGameStart
    ↓ GimmickManager.OnGameStart()
    ↓ ActivateAllByGroup(Start) — Start 그룹 전부 활성화
    ↓ ActivateAllByGroup(During) — During 그룹 전부 활성화

IngameTimer (30초 남음)
    ↓ EventManager.Instance.HurryUp()
HurryUpProduction (연출 재생: BGM 정지 → 텍스트 → BGM 1.2배속)
    ↓ 연출 완료 콜백
GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1)
    ↓ 마스터가 HurryUp 그룹 기믹 중 랜덤 1개 선택
    ↓ RPC로 선택된 인덱스 전파
선택된 허리업 기믹 1개만 Activate()
```

---

## 8. 네트워크 동기화 정리

| 상황 | 처리 주체 | 기믹 개발자가 할 일 |
|------|----------|-------------------|
| 기믹 활성화 동기화 | GimmickManager (RPC) | 없음 (자동) |
| 기믹 비활성화 동기화 | GimmickManager (RPC) | 없음 (자동) |
| 랜덤 선택 동기화 | GimmickManager (마스터 선택 → RPC) | 없음 (자동) |
| 기믹 내 오브젝트 생성 | 기믹 자체 | `if (PhotonNetwork.IsMasterClient)` + `PhotonNetwork.Instantiate` |
| 기믹 내 오브젝트 위치 동기화 | Photon (자동) | PhotonView가 프리팹에 붙어있으면 자동 |
| 기믹 내 오브젝트 파괴 | 기믹 자체 | `PhotonNetwork.Destroy` (마스터만) |

### 기존 기믹의 동기화 — 변경 없음

| 기믹 | 기존 동기화 방식 | IGimmick 추가 후 |
|------|-----------------|-----------------|
| Cannon | 로컬 물리 | 변경 없음 |
| CrabSpawner | PhotonNetwork.Instantiate/Destroy | 변경 없음 |
| Crab 이동 | PhotonView로 위치 동기화 | 변경 없음 |
| AirDropJet | PhotonNetwork.Instantiate | IngameTimer → AirDropSpawner로 이동만 |
| AirDropBox | PhotonNetwork.Instantiate/Destroy | 변경 없음 |
| RotationPlatform | 로컬 물리 | 변경 없음 |
| ValleyBall | Bomb의 RPC | 변경 없음 |

---

## 9. 작업 순서

### Phase 1: Core 모듈 구현 (신규 파일 4개)
1. `Gimmick/Core/GimmickType.cs` — 개별 기믹 식별 enum
2. `Gimmick/Core/GimmickGroupType.cs` — 기믹 그룹 분류 enum
3. `Gimmick/Core/IGimmick.cs` — 인터페이스
4. `Gimmick/Core/GimmickManager.cs` — 중앙 관리자

### Phase 2: 씬 설정
1. 각 인게임 맵 씬(Beach1, Dock1, Forest1)에 GimmickManager 오브젝트 생성
2. `GimmickManager` 컴포넌트 + `PhotonView` 컴포넌트 추가

### Phase 3: 기존 기믹 IGimmick 구현
1. `Cannon.cs` — IGimmick 추가
2. `CrabSpawner.cs` — IGimmick 추가
3. `RotationPlatform.cs` — IGimmick 추가
4. `AirDropSpawner.cs` 신규 작성 + `IngameTimer.cs`에서 AirDrop 로직 제거
5. `ValleyBall.cs` — IGimmick 추가 (선택, 우선순위 낮음)

### Phase 4: 허리업 연동
1. `HurryUpProduction.cs`에서 `GimmickManager.Instance.ActivateRandomByGroup(GimmickGroupType.HurryUp, 1)` 호출
2. 허리업 기믹 구현체 개발 (기획 확정 후)
3. 맵 씬에 허리업 기믹 오브젝트 배치 → Start()에서 자동 등록

---

## 10. 수정 파일 요약

| 파일 | 작업 유형 | Phase |
|------|----------|-------|
| `Gimmick/Core/GimmickType.cs` | 신규 | 1 |
| `Gimmick/Core/GimmickGroupType.cs` | 신규 | 1 |
| `Gimmick/Core/IGimmick.cs` | 신규 | 1 |
| `Gimmick/Core/GimmickManager.cs` | 신규 | 1 |
| `Cannon/Cannon.cs` | IGimmick 추가 | 3 |
| `Crab/CrabSpawner.cs` | IGimmick 추가 | 3 |
| `RotationPlatform/RotationPlatform.cs` | IGimmick 추가 | 3 |
| `AirDrop/AirDropSpawner.cs` | 신규 (로직 이동) | 3 |
| `Map/IngameTimer.cs` | AirDrop 로직 제거 | 3 |
| `ValleyBall/ValleyBall.cs` | IGimmick 추가 (선택) | 3 |

---

## 11. 주의사항

### 11.1 GimmickManager 초기화 순서
- `PhotonSingleton.Awake()`에서 Instance 설정
- 기존 기믹들은 `Start()`에서 Register → Awake보다 늦으므로 GimmickManager가 먼저 초기화됨
- 만약 순서 문제 발생 시 `Start()`에서 null 체크로 방어 (코드에 이미 반영됨)

### 11.2 같은 GimmickType이 여러 개인 경우
- 예: 씬에 Cannon이 3개 → 모두 `GimmickType.Cannon`으로 등록
- `ActivateByType(GimmickType.Cannon)` 호출 시 3개 전부 실행
- 개별 제어가 필요하면 GimmickType에 Cannon_1, Cannon_2 등 추가

### 11.3 ActivateRandomByGroup의 랜덤 동기화
- 마스터 클라이언트가 랜덤 인덱스를 선택
- 선택된 인덱스를 int[] 배열로 RPC 전파
- 모든 클라이언트에서 같은 기믹이 실행됨
- 단, 모든 클라이언트의 등록 순서가 동일해야 인덱스가 일치함
  → Start()에서 Register하므로 씬 하이어라키 순서에 의존
  → 모든 클라이언트가 같은 씬을 로드하므로 순서 동일 보장

### 11.4 AirDrop 마이그레이션 시 씬 설정
- 기존: IngameTimer에 `_airDropJetPrefab` 참조
- 변경: AirDropSpawner에 해당 프리팹 참조 이동
- 각 맵 씬마다 설정 필요

### 11.5 게임 종료 시 정리
- `GimmickManager.OnDisable()`에서 `DeactivateAll()` 호출
- 씬 전환 시 자동 정리

---

## 12. 기믹 추가 절차 요약

### 신규 기믹 추가 시:
1. `GimmickType` enum에 항목 추가
2. IGimmick 구현한 스크립트 작성 (Type, GroupType 선언)
3. Start()에서 Register, OnDestroy()에서 Unregister
4. 씬에 기믹 오브젝트 배치
5. 끝 — 자동으로 Type/GroupType별 분류됨

### 새로운 그룹 추가 시:
1. `GimmickGroupType` enum에 항목 추가
2. 해당 그룹의 기믹들이 GroupType을 새 값으로 선언
3. 외부 클래스에서 `ActivateRandomByGroup(새그룹, count)` 호출

---

## 13. 미결정 사항

### 13.1 기획 확인 필요
- [ ] 허리업 기믹의 구체적인 종류 및 효과 확정
- [ ] During 그룹 기믹의 실행 시점/조건 확정
- [ ] Start 그룹 기믹을 GimmickManager.Start()에서 자동 실행할지, 외부에서 호출할지

### 13.2 테스트 시나리오
- [ ] 모든 기믹이 GimmickManager에 정상 등록되는지
- [ ] ActivateByType으로 특정 기믹만 실행되는지
- [ ] ActivateRandomByGroup으로 랜덤 N개만 실행되는지
- [ ] 모든 클라이언트에서 같은 기믹이 활성화되는지 (RPC 동기화)
- [ ] AirDropSpawner로 이동 후 AirDrop 정상 동작하는지
- [ ] IngameTimer에서 AirDrop 로직 제거 후 타이머/UI 정상인지
- [ ] 게임 종료 시 모든 기믹 Deactivate 정상 호출되는지
