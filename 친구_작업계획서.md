# 친구 시스템 작업계획서

## 개요

로비에서 접근 가능한 친구 패널(UI_FriendsListPopup)을 구현한다.
백엔드는 **TheBackend SDK (뒤끝) 5.18.8**의 `Backend.Friend` API를 사용한다.
온라인 상태(Online/InGame/Offline)는 **Firebase Firestore `users` 컬렉션**에 필드를 추가하여 관리하고,
프로필 아웃핏은 이미 저장되고 있는 **Firebase Firestore `Inventory` 컬렉션**에서 읽어온다.
1:1 채팅 및 파티 채팅은 기존 **UIChatManager (Backend Chat SDK)** 를 활용한다.

---

## 기존 코드 → Legacy 전환 계획

현재 Friend 폴더의 코드는 Firebase 기반으로 설계되어 있으므로 Legacy로 분리하고,
뒤끝 SDK 기반 DDD 구조를 새로 구축한다. **(UI 코드는 건들지 않음)**

### Legacy로 이동할 파일 (코드만)

| 현재 파일 | Legacy 이동 | 사유 |
|----------|------------|------|
| `2.Repository/FriendRepository.cs` | `Legacy/FriendRepositoryLegacy.cs`로 리네임 | Firebase Firestore 기반 CRUD |
| `3.Manager/FriendManager.cs` | `Legacy/FriendManagerOld.cs`로 리네임 | 스켈레톤 + `DontDestroySingleton` 자리 양보 |
| `3.Manager/FriendManagerLegacy.cs` | `Legacy/FriendManagerLegacy.cs` (이동만) | 네이밍은 Legacy지만 Backend SDK 기반으로 가장 완성도 높음. 참조용 보존 |
| `1.Domain/Friend.cs` | 덮어쓰기 | 빈 MonoBehaviour → 도메인 Entity 클래스로 대체 |
| `1.Domain/FriendDTO.cs` | 덮어쓰기 | 빈 MonoBehaviour → 읽기전용 DTO로 대체 |
| `1.Domain/FriendInfo.cs` | `Legacy/FriendInfo.cs`로 이동 | 새 Friend 클래스가 역할 대체 |

### UI 파일 (변경 없음, 이후 Phase 2에서 수정)

- `4.UI/UI_FriendList.cs`
- `4.UI/UI_PanelFriendSlot.cs`
- `4.UI/UI_PanelFriendAdd.cs`
- `4.UI/UI_PanelFriendAccept.cs`
- `4.UI/UI_RequestFriendslot.cs`
- `4.UI/UI_PanelFriendUser.cs`

---

## 뒤끝 SDK (5.18.8) Friend API 레퍼런스

> 참조: [뒤끝 개발자 문서](https://docs.backnd.com/en/sdk-docs/backend/base/friend/request-to-another-user/apply-request/)

모든 API는 **동기 / 비동기(콜백) / SendQueue** 3가지 패턴을 지원한다.
본 프로젝트에서는 **비동기(콜백) 패턴**을 기본으로 사용한다.

### API 목록

| 기능 | 메서드 | 파라미터 | 성공 코드 |
|------|--------|---------|----------|
| 친구 요청 | `Backend.Friend.RequestFriend(gamerIndate)` | `string gamerIndate` | 204 |
| 요청 수락 | `Backend.Friend.AcceptFriend(gamerIndate)` | `string gamerIndate` | 204 |
| 요청 거절 | `Backend.Friend.RejectFriend(gamerIndate)` | `string gamerIndate` | 204 |
| 요청 취소 | `Backend.Friend.RevokeSentRequest(gamerIndate)` | `string gamerIndate` | 204 |
| 친구 삭제 | `Backend.Friend.BreakFriend(gamerIndate)` | `string gamerIndate` | 204 |
| 친구 목록 | `Backend.Friend.GetFriendList()` | `(int limit=100, int offset=0)` | 200 |
| 받은 요청 목록 | `Backend.Friend.GetReceivedRequestList()` | `(int limit=100, int offset=0)` | 200 |
| 보낸 요청 목록 | `Backend.Friend.GetSentRequestList()` | `(int limit=100, int offset=0)` | 200 |

### 중요 변경점 (기존 Legacy 코드 대비)

1. **`RequestFriend`가 닉네임이 아닌 `gamerIndate` 기반으로 동작**
   - 기존 `FriendManagerLegacy`는 `Backend.Friend.RequestFriend(nickname)` 으로 호출 중
   - 최신 SDK에서는 `gamerIndate`를 사용해야 함
   - → 닉네임으로 검색 후 inDate를 조회하는 단계가 선행되어야 함

2. **`RevokeSentRequest` 추가됨** — 보낸 요청 취소 기능 (기존 Legacy에 없음)

3. **최대 친구 수 초과 시 서버 자동 거부:**
   - `AcceptFriend` 호출 시 statusCode **412 (PreconditionFailed)** 반환
   - `"maxRequestedGamerFriend"` → 요청자(A)의 친구 수 초과
   - `"maxGamerFriend"` → 수락자(B)의 친구 수 초과

### 비동기 콜백 패턴 (프로젝트 표준)

```csharp
// 비동기 콜백
Backend.Friend.RequestFriend("2020-02-11T01:08:12.603Z", callback =>
{
    if (callback.IsSuccess()) { /* 성공 처리 */ }
    else { /* 에러 처리: callback.GetStatusCode(), callback.GetMessage() */ }
});

// SendQueue (UI 스레드 안전)
SendQueue.Enqueue(Backend.Friend.RequestFriend, "2020-02-11T01:08:12.603Z",
    callback => { /* 처리 */ });
```

### 목록 조회 응답 JSON 구조

```json
{
  "rows": [
    {
      "nickname": { "S": "플레이어닉네임" },
      "inDate": { "S": "2018-12-27T04:49:00.493Z" },
      "createdAt": { "S": "2018-12-27T04:50:03.955Z" },
      "lastLogin": { "S": "2021-07-23T09:13:14.186Z" }
    }
  ]
}
```

> **참고:** 닉네임 미설정 유저는 `nickname` 필드가 없을 수 있음

---

## 새 DDD 구조 설계

기존 프로젝트의 Account/ItemStorage DDD 패턴을 따른다.

```
Assets/02.Scripts/Friend/
├── 1.Domain/
│   ├── Friend.cs              — (덮어쓰기) 도메인 Entity: 명세, 제한사항, 도메인 정보와 함수
│   └── FriendDTO.cs           — (덮어쓰기) 읽기전용 DTO, UI 전달용
├── 2.Repository/
│   └── FriendRepository.cs    — (신규) Backend.Friend.* API 래핑, JSON 파싱, Raw 데이터 저장/불러오기
├── 3.Manager/
│   └── FriendManager.cs       — (신규) DontDestroySingleton, CRUD 비즈니스 로직
├── 4.UI/
│   └── (기존 유지, 이후 수정)
└── Legacy/
    ├── FriendRepositoryLegacy.cs  — (기존 Firebase 기반)
    ├── FriendManagerLegacy.cs     — (기존 Backend SDK 참조용)
    ├── FriendManagerOld.cs        — (기존 스켈레톤)
    └── FriendInfo.cs              — (기존 nickname/inDate 데이터 클래스)
```

### 계층별 역할 분담

| 계층 | 역할 | 하는 것 | 하지 않는 것 |
|------|------|---------|------------|
| **Domain** | 명세/제한사항 정의 | 데이터 구조, 유효성 검증, 상수, 도메인 함수 | SDK 호출, 비즈니스 로직, 이벤트 |
| **Repository** | Raw 데이터 CRUD | Backend.Friend.* API 호출, JSON 파싱, `Friend` 객체 반환 | 캐싱, 이벤트 발행, 비즈니스 판단 |
| **Manager** | 비즈니스 로직 CRUD | Repo 호출, 캐시 관리, 이벤트 발행, 상태 관리 | SDK 직접 호출, JSON 파싱 |

### 1.Domain - Friend.cs (도메인 Entity)

```csharp
using System.Collections.Generic;

/// <summary>
/// 친구 도메인 Entity
/// 친구 데이터의 명세, 제한사항, 도메인 규칙을 정의한다.
/// 기능 로직은 Repository/Manager에서 구현한다.
/// </summary>
public class Friend
{
    // === 상수 ===
    public const int MAX_FRIEND_COUNT = 50;
    public const int DEFAULT_QUERY_LIMIT = 100;

    // === 데이터 필드 ===
    public string Nickname { get; private set; }
    public string InDate { get; private set; }
    public string CreatedAt { get; private set; }
    public string LastLogin { get; private set; }
    public EFriendStatus Status { get; private set; }
    public List<string> OutfitIds { get; private set; }

    // === 생성자 ===
    public Friend(string nickname, string inDate, string createdAt, string lastLogin)
    {
        Nickname = nickname ?? "";
        InDate = inDate;
        CreatedAt = createdAt;
        LastLogin = lastLogin;
        Status = EFriendStatus.Offline;
        OutfitIds = new List<string>();
    }

    // === 도메인 함수 ===

    /// <summary>
    /// 온라인 상태 갱신 (Firebase에서 조회한 값 반영)
    /// </summary>
    public void UpdateStatus(EFriendStatus newStatus)
    {
        Status = newStatus;
    }

    /// <summary>
    /// 프로필 아웃핏 갱신 (Firebase에서 조회한 값 반영)
    /// </summary>
    public void UpdateOutfitIds(List<string> outfitIds)
    {
        OutfitIds = outfitIds ?? new List<string>();
    }

    /// <summary>
    /// "닉네임#태그" 형태의 표시명 반환
    /// (Discriminator는 AccountManager에서 별도 조회 필요)
    /// </summary>
    public string GetDisplayName(string discriminator = null)
    {
        if (string.IsNullOrEmpty(discriminator))
            return Nickname;
        return $"{Nickname}#{discriminator}";
    }

    /// <summary>
    /// 유효한 친구 데이터인지 검증
    /// </summary>
    public bool IsValid()
    {
        return !string.IsNullOrEmpty(InDate);
    }

    /// <summary>
    /// 읽기전용 DTO로 변환
    /// </summary>
    public FriendDTO ToDTO()
    {
        return new FriendDTO(Nickname, InDate, CreatedAt, LastLogin, Status, OutfitIds);
    }

    // === 정적 유틸리티 ===

    /// <summary>
    /// 주어진 친구 수가 최대치에 도달했는지 검사
    /// </summary>
    public static bool IsFriendListFull(int currentCount)
    {
        return currentCount >= MAX_FRIEND_COUNT;
    }
}
```

### 1.Domain - FriendDTO.cs (읽기전용 DTO)

```csharp
using System.Collections.Generic;

/// <summary>
/// UI에 전달되는 읽기전용 친구 정보.
/// Friend Entity의 스냅샷으로, UI 계층에서는 이 DTO만 참조한다.
/// </summary>
public class FriendDTO
{
    public readonly string Nickname;
    public readonly string InDate;
    public readonly string CreatedAt;
    public readonly string LastLogin;
    public readonly EFriendStatus Status;
    public readonly IReadOnlyList<string> OutfitIds;

    public FriendDTO(string nickname, string inDate, string createdAt, string lastLogin,
                     EFriendStatus status, List<string> outfitIds)
    {
        Nickname = nickname;
        InDate = inDate;
        CreatedAt = createdAt;
        LastLogin = lastLogin;
        Status = status;
        OutfitIds = outfitIds?.AsReadOnly();
    }
}
```

### 2.Repository - FriendRepository.cs (뒤끝 SDK 래핑 + Raw 데이터 처리)

```csharp
using System;
using System.Collections.Generic;
using BackEnd;
using LitJson;

/// <summary>
/// Backend.Friend API를 래핑하여 도메인 객체(Friend)로 변환하는 Repository.
/// Raw 데이터의 저장/불러오기를 담당한다. 비동기 콜백 패턴 사용.
/// </summary>
public class FriendRepository
{
    // ===== 쓰기 (Command) =====

    public void RequestFriend(string gamerIndate, Action<bool, string> callback)
    {
        Backend.Friend.RequestFriend(gamerIndate, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, "친구 요청을 보냈습니다.");
            else
                callback?.Invoke(false, ParseError(bro));
        });
    }

    public void AcceptFriend(string gamerIndate, Action<bool, string> callback)
    {
        Backend.Friend.AcceptFriend(gamerIndate, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, "친구 요청을 수락했습니다.");
            else
                callback?.Invoke(false, ParseError(bro));
        });
    }

    public void RejectFriend(string gamerIndate, Action<bool, string> callback)
    {
        Backend.Friend.RejectFriend(gamerIndate, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, "친구 요청을 거절했습니다.");
            else
                callback?.Invoke(false, ParseError(bro));
        });
    }

    public void RevokeSentRequest(string gamerIndate, Action<bool, string> callback)
    {
        Backend.Friend.RevokeSentRequest(gamerIndate, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, "친구 요청을 취소했습니다.");
            else
                callback?.Invoke(false, ParseError(bro));
        });
    }

    public void BreakFriend(string gamerIndate, Action<bool, string> callback)
    {
        Backend.Friend.BreakFriend(gamerIndate, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, "친구를 삭제했습니다.");
            else
                callback?.Invoke(false, ParseError(bro));
        });
    }

    // ===== 읽기 (Query) → Friend 도메인 객체로 변환하여 반환 =====

    public void GetFriendList(int limit, int offset, Action<bool, List<Friend>> callback)
    {
        Backend.Friend.GetFriendList(limit, offset, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, ParseRows(bro));
            else
                callback?.Invoke(false, null);
        });
    }

    public void GetReceivedRequestList(int limit, int offset, Action<bool, List<Friend>> callback)
    {
        Backend.Friend.GetReceivedRequestList(limit, offset, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, ParseRows(bro));
            else
                callback?.Invoke(false, null);
        });
    }

    public void GetSentRequestList(int limit, int offset, Action<bool, List<Friend>> callback)
    {
        Backend.Friend.GetSentRequestList(limit, offset, bro =>
        {
            if (bro.IsSuccess())
                callback?.Invoke(true, ParseRows(bro));
            else
                callback?.Invoke(false, null);
        });
    }

    // ===== Raw 데이터 파싱 =====

    private List<Friend> ParseRows(BackendReturnObject bro)
    {
        List<Friend> list = new List<Friend>();
        JsonData rows = bro.GetReturnValuetoJSON()["rows"];

        if (rows == null || rows.Count == 0)
            return list;

        for (int i = 0; i < rows.Count; i++)
        {
            JsonData row = rows[i];
            string nickname = row.ContainsKey("nickname") ? row["nickname"]["S"].ToString() : "";
            string inDate   = row.ContainsKey("inDate")   ? row["inDate"]["S"].ToString()   : "";
            string createdAt = row.ContainsKey("createdAt") ? row["createdAt"]["S"].ToString() : "";
            string lastLogin = row.ContainsKey("lastLogin") ? row["lastLogin"]["S"].ToString() : "";

            list.Add(new Friend(nickname, inDate, createdAt, lastLogin));
        }

        return list;
    }

    private string ParseError(BackendReturnObject bro)
    {
        int statusCode = int.Parse(bro.GetStatusCode());

        if (statusCode == 412)
        {
            string errorCode = bro.GetErrorCode();
            if (errorCode.Contains("maxRequestedGamerFriend"))
                return "상대방의 친구가 최대 인원수에 도달하였습니다.";
            if (errorCode.Contains("maxGamerFriend"))
                return "친구가 최대 인원수에 도달하였습니다.";
        }

        return $"오류 발생: {bro.GetMessage()}";
    }
}
```

### 3.Manager - FriendManager.cs (싱글톤, CRUD 비즈니스 로직)

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 친구 시스템 매니저 (DontDestroySingleton).
/// Repository를 통해 CRUD를 수행하고, 캐시 관리 및 이벤트 발행을 담당한다.
/// </summary>
public class FriendManager : DontDestroySingleton<FriendManager>
{
    private FriendRepository _repo;
    private List<Friend> _cachedFriendList = new List<Friend>();

    public event Action OnFriendListChanged;
    public event Action<string> OnFriendError;

    public IReadOnlyList<Friend> CachedFriendList => _cachedFriendList;

    protected override void Awake()
    {
        base.Awake();
        _repo = new FriendRepository();
    }

    // ===== Create (친구 요청) =====

    public void RequestFriend(string gamerIndate, Action<bool, string> callback = null)
    {
        _repo.RequestFriend(gamerIndate, (success, msg) =>
        {
            if (!success) OnFriendError?.Invoke(msg);
            callback?.Invoke(success, msg);
        });
    }

    public void AcceptFriend(string gamerIndate, Action<bool, string> callback = null)
    {
        _repo.AcceptFriend(gamerIndate, (success, msg) =>
        {
            if (success) RefreshFriendList();
            else OnFriendError?.Invoke(msg);
            callback?.Invoke(success, msg);
        });
    }

    public void RejectFriend(string gamerIndate, Action<bool, string> callback = null)
    {
        _repo.RejectFriend(gamerIndate, callback);
    }

    public void RevokeSentRequest(string gamerIndate, Action<bool, string> callback = null)
    {
        _repo.RevokeSentRequest(gamerIndate, callback);
    }

    // ===== Delete (친구 삭제) =====

    public void BreakFriend(string gamerIndate, Action<bool, string> callback = null)
    {
        _repo.BreakFriend(gamerIndate, (success, msg) =>
        {
            if (success) RefreshFriendList();
            else OnFriendError?.Invoke(msg);
            callback?.Invoke(success, msg);
        });
    }

    // ===== Read (목록 조회) =====

    public void RefreshFriendList(int limit = Friend.DEFAULT_QUERY_LIMIT, int offset = 0)
    {
        _repo.GetFriendList(limit, offset, (success, list) =>
        {
            if (success)
            {
                _cachedFriendList = list;
                OnFriendListChanged?.Invoke();
            }
        });
    }

    public void GetReceivedRequests(Action<bool, List<Friend>> callback,
        int limit = Friend.DEFAULT_QUERY_LIMIT, int offset = 0)
    {
        _repo.GetReceivedRequestList(limit, offset, callback);
    }

    public void GetSentRequests(Action<bool, List<Friend>> callback,
        int limit = Friend.DEFAULT_QUERY_LIMIT, int offset = 0)
    {
        _repo.GetSentRequestList(limit, offset, callback);
    }

    // ===== 유틸리티 =====

    public int GetFriendCount() => _cachedFriendList.Count;

    public bool IsFriendListFull() => Friend.IsFriendListFull(_cachedFriendList.Count);

    /// <summary>
    /// 캐시된 친구 목록을 FriendDTO 리스트로 변환하여 반환 (UI 전달용)
    /// </summary>
    public List<FriendDTO> GetFriendDTOList()
    {
        List<FriendDTO> dtoList = new List<FriendDTO>(_cachedFriendList.Count);
        foreach (var friend in _cachedFriendList)
        {
            dtoList.Add(friend.ToDTO());
        }
        return dtoList;
    }
}
```

---

## UI 구조 설계

```
┌─────────────────────────────────────┐
│  [상단] 내 프로필 영역               │
│  ┌──────┐  닉네임#태그              │
│  │ 프로필 │  친구 수 / 최대 친구 수   │
│  │ 이미지 │                          │
│  └──────┘                           │
├─────────────────────────────────────┤  ← Mask 영역 시작
│  [초대 슬롯] (비활성 상태, 초대 시 활성)│
│  ┌─ 초대 ──────── 초대 개수(2+) ──┐  │
│  │ 아이콘 | 이름#태그              │  │
│  │ "OO 초대를 보냈습니다."         │  │
│  │           [수락] [거절]         │  │
│  └────────────────────────────────┘  │
│                                     │
│  [파티 채팅 슬롯] (비활성 상태)       │
│  ┌────────────────────────────────┐  │
│  │ 파티아이콘 | 파티 채팅           │  │
│  └────────────────────────────────┘  │
│                                     │
│  [친구 리스트 헤더]                   │
│  ┌─ 친구 ─────── 3 / 50 ─────────┐  │
│                                     │
│  [친구 목록 ScrollView]              │
│  ┌────────────────────────────────┐  │
│  │ 프로필 | 닉네임#태그 | ● 온라인  │  │
│  │ 프로필 | 닉네임#태그 | ● 게임 중  │  │
│  │ 프로필 | 닉네임#태그 | ● 오프라인 │  │
│  └────────────────────────────────┘  │
├─────────────────────────────────────┤  ← Mask 영역 끝
│  [하단]                              │
│         [친구 추가 버튼]              │
└─────────────────────────────────────┘
```

---

## 세부 작업 항목

### Phase 0: Legacy 분리 (선행 작업)

1. `Friend/Legacy/` 폴더 생성
2. `FriendRepository.cs` → `Legacy/FriendRepositoryLegacy.cs`로 이동 + 리네임
3. `FriendManager.cs` → `Legacy/FriendManagerOld.cs`로 이동 + 리네임
4. `FriendManagerLegacy.cs` → `Legacy/FriendManagerLegacy.cs`로 이동
5. `FriendInfo.cs` → `Legacy/FriendInfo.cs`로 이동
6. `Friend.cs`, `FriendDTO.cs` 빈 파일은 Phase 1에서 덮어쓰기
7. **UI 파일은 건들지 않음**

### Phase 1: 도메인 모델 및 데이터 계층 (DDD 기반)

#### 1-1. Friend 도메인 Entity
**파일:** `Friend/1.Domain/Friend.cs` (기존 빈 MonoBehaviour 덮어쓰기)
- 친구 데이터의 **명세와 제한사항**을 정의하는 순수 C# 클래스
- 상수: `MAX_FRIEND_COUNT = 50`, `DEFAULT_QUERY_LIMIT = 100`
- 필드: Nickname, InDate, CreatedAt, LastLogin, Status, OutfitIds
- 도메인 함수: `UpdateStatus()`, `UpdateOutfitIds()`, `GetDisplayName()`, `IsValid()`, `ToDTO()`
- 정적 유틸: `IsFriendListFull(int currentCount)`
- 위 "1.Domain - Friend.cs" 섹션의 코드 참조

#### 1-2. FriendDTO 정의
**파일:** `Friend/1.Domain/FriendDTO.cs` (기존 빈 MonoBehaviour 덮어쓰기)
- `Friend.ToDTO()`로 생성되는 **읽기전용** 데이터 전달 객체
- 모든 필드 `readonly`, OutfitIds는 `IReadOnlyList<string>`
- UI 계층에서는 이 DTO만 참조
- 위 "1.Domain - FriendDTO.cs" 섹션의 코드 참조

#### 1-3. EFriendStatus 열거형 추가
**파일:** `Enum/EFriendStatus.cs` (신규)

```csharp
public enum EFriendStatus
{
    Online,     // 연두색
    InGame,     // 하늘색
    Offline     // 짙은 회색
}
```

#### 1-4. EInviteType 열거형 추가
**파일:** `Enum/EInviteType.cs` (신규)

```csharp
public enum EInviteType
{
    Game,   // 게임 초대
    Party   // 파티 초대
}
```

#### 1-5. FriendRepository 구현 (뒤끝 SDK 래핑 + Raw 데이터 처리)
**파일:** `Friend/2.Repository/FriendRepository.cs` (신규)
- `Backend.Friend.*` API 호출 → 비동기 콜백 패턴
- JSON 파싱 → **`List<Friend>`** 도메인 객체로 변환하여 반환
- 에러 코드 파싱 (412 PreconditionFailed → 친구 수 초과 메시지)
- 위 "2.Repository" 섹션의 코드 참조

#### 1-6. FriendManager 구현 (싱글톤, CRUD)
**파일:** `Friend/3.Manager/FriendManager.cs` (신규)
- `DontDestroySingleton<FriendManager>` 상속
- Repository를 내부에서 생성하여 CRUD 수행
- 캐싱된 친구 목록 관리 (`_cachedFriendList: List<Friend>`)
- `GetFriendDTOList()` → UI에 `List<FriendDTO>` 제공
- `IsFriendListFull()` → `Friend.IsFriendListFull()` 위임
- 이벤트: `OnFriendListChanged`, `OnFriendError`
- 위 "3.Manager" 섹션의 코드 참조

#### 1-7. 온라인 상태 및 프로필 아웃핏 (Firebase Firestore 기존 컬렉션 활용)

기존에 이미 사용 중인 Firestore 컬렉션에 필드를 추가/활용한다. 새 컬렉션은 만들지 않는다.

**① 온라인 상태 — `users/{uid}` 컬렉션 (기존 `AccountRepository`가 사용 중)**

```
Firestore "users/{uid}"
{
  nickname, discriminator, email, ...  // (기존 필드)
  "status": "Online" | "InGame" | "Offline",   // ← 추가
  "lastStatusUpdate": Timestamp                 // ← 추가 (비정상 종료 대비)
}
```

**상태 쓰기 시점:**
| 시점 | status 값 | 쓰기 주체 |
|------|-----------|----------|
| 로그인 성공 | `"Online"` | `AccountManager` 또는 `FriendStatusUpdater` |
| 게임 씬 진입 (Beach1, Dock1 등) | `"InGame"` | `FriendStatusUpdater` |
| ResultScene → Lobby 복귀 | `"Online"` | `FriendStatusUpdater` |
| 앱 종료 / 로그아웃 | `"Offline"` | `FriendStatusUpdater.OnApplicationQuit()` |
| Heartbeat (매 3분) | `lastStatusUpdate = now` | `FriendStatusUpdater` (코루틴) |

**비정상 종료 대비:** 조회 시 `lastStatusUpdate`가 현재 시각보다 5분 이상 경과하면 `Offline`으로 간주한다.

**② 프로필 아웃핏 — `Inventory/{uid}` 컬렉션 (기존 `ItemStorageRepo`가 사용 중)**

```
Firestore "Inventory/{uid}"
{
  "Equipments": {
    "Head": { "ID": "HD0001", "IsEquipped": true },
    "Face": { "ID": "FC0003", "IsEquipped": true },
    "Chest": { "ID": "CH0002", "IsEquipped": true },
    ...
  }
}
```

- 이미 `ItemStorageRepo.SaveInventory()`가 장착 변경 시마다 저장하고 있음
- 친구 프로필 표시 시 **친구의 UID로 `Inventory/{friendUid}` 문서를 읽어** 장착 아이템 ID 추출
- 추출한 ID 리스트 → `Friend.UpdateOutfitIds()` → `ProfileSkin.Init(itemIds)` 호출
- **추가 동기화 로직 불필요** — 기존 저장 흐름을 그대로 활용

**③ 친구 상태/아웃핏 조회 흐름 (FriendManager에서 통합)**

```
1. FriendManager.RefreshFriendList()
   → FriendRepository.GetFriendList() [뒤끝 SDK]
   → List<Friend> 반환 (nickname, inDate 등)

2. FriendManager.RefreshFriendStatuses()
   → 각 Friend의 nickname → AccountManager로 UID 조회
   → Firestore "users/{uid}" 에서 status, lastStatusUpdate 읽기
   → Friend.UpdateStatus(parsedStatus) 호출
   → Firestore "Inventory/{uid}" 에서 Equipments 읽기
   → Friend.UpdateOutfitIds(parsedIds) 호출

3. OnFriendListChanged 이벤트 → UI 갱신
```

---

### Phase 2: UI 구현

#### 2-1. 상단 - 내 프로필 영역
**파일:** `Friend/4.UI/UI_FriendList.cs` (기존 파일 수정)

**구성 요소:**
- `ProfileSkin myProfileSkin` — `ItemStorage.Instance`에서 장착 아이템 조회 → `ProfileSkin.Init(itemIds)`
- `TMP_Text nicknameText` — `AccountManager.Instance.CurrentAccount.User_Name`
- `TMP_Text friendCountText` — `"{FriendManager.Instance.GetFriendCount()} / 50"`

#### 2-2. 중단 - 초대 슬롯 (기본 비활성)

**파일:** `Friend/4.UI/UI_InvitePanel.cs` (신규)

- 상단 바: "초대" 텍스트 (좌측) + 초대 개수 텍스트 (우측, 2개 이상일 때만 표시)
- ScrollView (고정 크기, 슬롯 1개 크기와 동일)

**파일:** `Friend/4.UI/UI_InviteSlot.cs` (신규)

| 요소 | 설명 |
|------|------|
| `Image inviteIcon` | 게임/파티 초대 아이콘 (EInviteType에 따라 변경) |
| `TMP_Text nameText` | 보낸 사람의 "닉네임#태그" |
| `TMP_Text descText` | "게임 초대를 보냈습니다." / "파티 초대를 보냈습니다." |
| `Button acceptButton` | 수락 버튼 |
| `Button declineButton` | 거절 버튼 |

#### 2-3. 중단 - 파티 채팅 슬롯 (기본 비활성)

**파일:** `Friend/4.UI/UI_PartyChatSlot.cs` (신규)
- 파티 아이콘 + "파티 채팅" 텍스트
- 클릭 시 파티 채팅창 팝업 (상대방 말풍선 좌측 상단에 닉네임 표시)

#### 2-4. 중단 - 친구 리스트

**친구 슬롯:** `Friend/4.UI/UI_PanelFriendSlot.cs` (기존 수정)

| 요소 | 설명 |
|------|------|
| `ProfileSkin friendProfileSkin` | Firestore `OutfitIds`로 `ProfileSkin.Init(itemIds)` |
| `TMP_Text nameText` | "닉네임#태그" |
| `TMP_Text statusText` | "온라인" / "게임 중" / "오프라인" |

**상태별 색상:**
```csharp
switch (status)
{
    case EFriendStatus.Online:  statusText.color = new Color(0.56f, 0.93f, 0.56f); break; // 연두색
    case EFriendStatus.InGame:  statusText.color = new Color(0.53f, 0.81f, 0.92f); break; // 하늘색
    case EFriendStatus.Offline: statusText.color = new Color(0.41f, 0.41f, 0.41f); break; // 짙은 회색
}
```

**정렬 순서:** 온라인 → 게임 중 → 오프라인

#### 2-5. 중단 - Mask 및 레이아웃 관리

- 중앙 영역 최상단 오브젝트에 `RectMask2D` 적용
- `VerticalLayoutGroup` + `ContentSizeFitter`로 자동 정렬
- `LayoutRebuilder.ForceRebuildLayoutImmediate()` 호출로 즉시 갱신

#### 2-6. 하단 - 친구 추가 버튼

**정상 상태:** 클릭 → 친구 추가/요청 팝업

**최대 인원 도달 시 (50명):**
- `button.interactable = false` + 이미지 어둡게 처리
- 마우스 호버 시 툴팁: "더 이상 친구 추가를 할 수 없습니다."

---

### Phase 3: 친구 추가 / 요청 팝업

#### 3-1. 친구 추가 팝업
**파일:** `Friend/4.UI/UI_PanelFriendAdd.cs` (기존 수정)

1. 닉네임 입력 필드
2. 검색 → `AccountManager.Instance.GetUidsWithNickname(nickname)` 호출
3. 검색 결과에서 각 유저의 `gamerIndate` 조회
4. "친구 추가" 버튼 → `FriendManager.Instance.RequestFriend(gamerIndate)`

> **주의:** 기존 Legacy는 닉네임으로 직접 요청했지만, 최신 SDK는 `gamerIndate` 필요.
> 닉네임 검색 → inDate 확보 → RequestFriend 순서로 진행해야 함.

#### 3-2. 받은 친구 요청 탭
**파일:** `Friend/4.UI/UI_PanelFriendAccept.cs` (기존 수정)

1. `FriendManager.Instance.GetReceivedRequests(callback)` 호출
2. `UI_RequestFriendSlot`으로 각 요청 렌더링
3. 수락 → `FriendManager.Instance.AcceptFriend(gamerIndate)`
4. 거절 → `FriendManager.Instance.RejectFriend(gamerIndate)`

#### 3-3. 최대 인원 도달 시 처리

뒤끝 SDK가 서버 측에서 자동으로 412 에러를 반환하므로, 클라이언트는 에러 응답만 처리하면 된다.

**`AcceptFriend` 412 응답 처리:**
- `"maxRequestedGamerFriend"` → 팝업: "[상대 닉네임]님의 친구가 최대 인원수에 도달하였습니다."
- `"maxGamerFriend"` → 팝업: "더 이상 친구를 추가할 수 없습니다."

> `FriendRepository.ParseError()`에서 이미 에러 메시지를 분기 처리함.
> `FriendManager.OnFriendError` 이벤트 → UI에서 `UI_SystemPopup`으로 표시.

---

### Phase 4: 1:1 채팅 연동

#### 4-1. 친구 슬롯에서 1:1 채팅 열기

1. 친구 슬롯 클릭 → 컨텍스트 메뉴에서 "채팅" 선택
2. `UIChatManager`를 통해 1:1 채팅 채널 진입
3. `ChatMessageData.outfit` 필드로 상대 프로필 렌더링

#### 4-2. 파티 채팅과의 차이점

| 항목 | 1:1 채팅 | 파티 채팅 |
|------|---------|----------|
| 닉네임 표시 | 표시하지 않음 | 상대방 말풍선 좌측 상단에 닉네임 표시 |
| 채널 | 1:1 전용 채널 | 파티 채널 (`PartyManager` 연동) |

---

### Phase 5: 온라인 상태 Heartbeat 시스템

#### 5-1. FriendStatusUpdater 구현
**파일:** `Friend/3.Manager/FriendStatusUpdater.cs` (신규)

Firestore `users/{myUid}` 문서에 자신의 상태를 쓰는 역할.

**쓰기 대상:** `users/{myUid}.status`, `users/{myUid}.lastStatusUpdate`

```csharp
// 핵심 메서드
public async void SetStatus(string status)
{
    var userDoc = FirebaseManager.Instance.DB.Collection("users")
        .Document(FirebaseManager.Instance.Auth.CurrentUser.UserId);
    await userDoc.UpdateAsync(new Dictionary<string, object>
    {
        { "status", status },
        { "lastStatusUpdate", FieldValue.ServerTimestamp }
    });
}
```

**상태 쓰기 시점:**
```
로그인 성공       → SetStatus("Online")
게임 씬 진입      → SetStatus("InGame")
로비 복귀         → SetStatus("Online")
로그아웃/앱 종료  → SetStatus("Offline")  (OnApplicationQuit)
매 3분 Heartbeat → lastStatusUpdate만 갱신
```

#### 5-2. 친구 상태/아웃핏 조회 (읽기)

FriendManager에서 친구들의 상태와 아웃핏을 Firestore에서 읽어와 `Friend` 객체에 반영.

**조회 대상:** `users/{friendUid}.status`, `Inventory/{friendUid}.Equipments`

```csharp
// FriendManager 내 메서드
public async void RefreshFriendStatuses()
{
    foreach (var friend in _cachedFriendList)
    {
        // 1. UID 조회 (nickname → uid, AccountManager 활용)
        // 2. users/{uid} 에서 status, lastStatusUpdate 읽기
        // 3. 비정상 종료 판정: lastStatusUpdate + 5분 < now → Offline
        // 4. friend.UpdateStatus(parsedStatus)
        // 5. Inventory/{uid} 에서 Equipments 읽기 → ID 리스트 추출
        // 6. friend.UpdateOutfitIds(idList)
    }
    OnFriendListChanged?.Invoke();
}
```

**조회 주기:**
- 친구 패널 **열릴 때** — 전체 갱신
- 패널 **열려있는 동안** — 30초마다 갱신 (코루틴)
- 패널 **닫힐 때** — 코루틴 중지 (불필요한 Firestore 읽기 방지)

---

## 작업 우선순위 및 의존 관계

```
Phase 0 (Legacy 분리) ← 최우선
  └─ Firebase 코드 Legacy 폴더로 이동, 빈 파일 정리
       │
Phase 1 (DDD 기반 구축) ← Phase 0 완료 후
  ├─ 1-1. Friend 도메인 Entity (명세/제한사항/도메인 함수)
  ├─ 1-2. FriendDTO (읽기전용 전달 객체)
  ├─ 1-3. EFriendStatus 열거형
  ├─ 1-4. EInviteType 열거형
  ├─ 1-5. FriendRepository (뒤끝 SDK 래핑 + Raw 데이터 처리)
  ├─ 1-6. FriendManager (싱글톤, CRUD 비즈니스 로직)
  └─ 1-7. 온라인 상태 시스템 (Firestore)
       │
Phase 2 (UI) ← Phase 1 완료 후
  ├─ 2-1~2-6. 친구 패널 UI 전체
       │
Phase 3 (친구 추가 팝업) ← Phase 1 완료 후 (Phase 2와 병행 가능)
  ├─ 3-1~3-3. 검색, 요청, 최대 인원 처리
       │
Phase 4 (채팅 연동) ← Phase 2 완료 후
  ├─ 4-1~4-2. 1:1 채팅, 파티 채팅
       │
Phase 5 (Heartbeat) ← Phase 1-7 완료 후
  ├─ 5-1~5-2. 상태 동기화
```

---

## 파일 변경 요약

### 신규 / 덮어쓰기 파일

| 파일 | 설명 |
|------|------|
| `Friend/1.Domain/Friend.cs` | 도메인 Entity (명세/제한사항/도메인 함수) — 기존 빈 MonoBehaviour 덮어쓰기 |
| `Friend/1.Domain/FriendDTO.cs` | 읽기전용 DTO (UI 전달용) — 기존 빈 MonoBehaviour 덮어쓰기 |
| `Friend/2.Repository/FriendRepository.cs` | Backend.Friend.* API 래핑 + Raw 데이터 처리 |
| `Friend/3.Manager/FriendManager.cs` | DontDestroySingleton, CRUD 비즈니스 로직 |
| `Friend/3.Manager/FriendStatusUpdater.cs` | Heartbeat 관리 |
| `Friend/4.UI/UI_InvitePanel.cs` | 초대 패널 |
| `Friend/4.UI/UI_InviteSlot.cs` | 초대 슬롯 |
| `Friend/4.UI/UI_PartyChatSlot.cs` | 파티 채팅 슬롯 |
| `Enum/EFriendStatus.cs` | 친구 상태 열거형 |
| `Enum/EInviteType.cs` | 초대 타입 열거형 |

### Legacy 이동 (코드만)

| 원본 | 이동 후 |
|------|--------|
| `Friend/2.Repository/FriendRepository.cs` | `Friend/Legacy/FriendRepositoryLegacy.cs` |
| `Friend/3.Manager/FriendManager.cs` | `Friend/Legacy/FriendManagerOld.cs` |
| `Friend/3.Manager/FriendManagerLegacy.cs` | `Friend/Legacy/FriendManagerLegacy.cs` |
| `Friend/1.Domain/FriendInfo.cs` | `Friend/Legacy/FriendInfo.cs` |
| `Friend/1.Domain/Friend.cs` | 덮어쓰기 (빈 MonoBehaviour → 도메인 Entity) |
| `Friend/1.Domain/FriendDTO.cs` | 덮어쓰기 (빈 MonoBehaviour → 읽기전용 DTO) |

### UI 수정 (Phase 2~3에서)

| 파일 | 수정 내용 |
|------|----------|
| `Friend/4.UI/UI_FriendList.cs` | 프로필, 헤더, 버튼, 레이아웃 |
| `Friend/4.UI/UI_PanelFriendSlot.cs` | 프로필 이미지, 상태 색상 |
| `Friend/4.UI/UI_PanelFriendAdd.cs` | gamerIndate 기반 요청으로 변경 |
| `Friend/4.UI/UI_PanelFriendAccept.cs` | 받은 요청 탭 완성 |

---

## 주의사항

1. **`RequestFriend`는 gamerIndate 기반:** 닉네임 검색 → inDate 확보 → 요청 순서 필수. 기존 Legacy의 닉네임 기반 호출은 더 이상 사용 불가.

2. **최대 친구 수 서버 자동 체크:** 뒤끝 콘솔에 50명으로 설정되어 있으므로 `AcceptFriend` 시 412 에러로 자동 거부됨. 클라이언트 UI에서도 사전 차단하되, 최종 방어는 서버에 의존.

3. **EventManager 구독/해제:** 모든 이벤트 구독은 반드시 `OnDestroy`에서 해제할 것. (CLAUDE.md 규칙)

4. **UIChatManager.SendChatMessage 사용:** `SendMessage`가 아닌 `SendChatMessage`를 사용할 것. (CLAUDE.md 알려진 이슈)

5. **비동기 콜백 패턴 통일:** 뒤끝 SDK가 콜백 기반이므로, Repository → Manager → UI 전체를 `Action<bool, T> callback` 패턴으로 통일. Firebase 조회(상태/아웃핏)는 `async/await`이지만 Manager 내부에서 처리하고 UI에는 이벤트로 전달.

6. **Firebase 기존 컬렉션 활용 원칙:**
   - 온라인 상태 → `users/{uid}` (기존 `AccountRepository` 사용 컬렉션)에 `status`, `lastStatusUpdate` 필드 추가
   - 프로필 아웃핏 → `Inventory/{uid}` (기존 `ItemStorageRepo` 사용 컬렉션)에서 읽기만
   - **새 컬렉션 생성 없음** — 기존 인프라 최대 활용

7. **레이아웃 관리:** `VerticalLayoutGroup` + `ContentSizeFitter` + `RectMask2D` 조합.
