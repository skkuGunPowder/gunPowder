# 친구 시스템 작업계획서

## 개요

로비 및 대기방에서 접근 가능한 친구 패널을 구현한다.

- **친구 CRUD:** TheBackend SDK (뒤끝) `Backend.Friend` API
- **온라인 상태:** 뒤끝 실시간 알림 (`Backend.Notification`)
- **프로필 아웃핏:** Firebase Firestore `Inventory/{uid}`
- **채팅:** 기존 UIChatManager (Backend Chat SDK)

---

## Legacy 정리

기존 Firebase 기반 코드 및 미사용 클래스는 `Friend/Legacy/` 폴더에 모아두고, 활성 코드와 분리한다.

| Legacy 파일 | 사유 |
|-------------|------|
| `FriendRepositoryLegacy.cs` | Firebase Firestore 기반 CRUD |
| `FriendManagerLegacy.cs` | 뒤끝 기반이지만 구조화 안 된 버전 |
| `FriendManagerOld.cs` | 이전 FriendManager 스켈레톤 |
| `FriendInfo.cs` | Friend 클래스가 역할 대체 |
| `FriendStatusUpdater.cs` | 뒤끝 실시간 알림으로 대체 → 제거 대상 |

> Legacy 폴더는 참조용으로만 보존. 새 코드에서 Legacy 클래스를 참조하지 않는다.

---

## DDD 구조

```
Assets/02.Scripts/Friend/
├── 1.Domain/
│   ├── Friend.cs           — 도메인 Entity (상수, 필드, 검증, 도메인 함수)
│   └── FriendDTO.cs        — 읽기전용 DTO (UI 전달용)
├── 2.Repository/
│   └── FriendRepository.cs — Backend.Friend.* API 래핑, JSON 파싱
├── 3.Manager/
│   └── FriendManager.cs    — DontDestroySingleton, CRUD, 캐시, 이벤트
├── 4.UI/
│   ├── UI_FriendList.cs, UI_PanelFriendSlot.cs, UI_PanelFriendAdd.cs
│   ├── UI_PanelFriendAccept.cs, UI_PanelFriendUser.cs, UI_RequestFriendslot.cs
│   └── UI_InvitePanel.cs, UI_InviteSlot.cs
└── Legacy/                 — 기존 코드 (참조용 보존)
```

### 계층별 역할

| 계층 | 하는 것 | 하지 않는 것 |
|------|---------|------------|
| **Domain** | 데이터 구조, 유효성 검증, 상수, 도메인 함수 | SDK 호출, 비즈니스 로직 |
| **Repository** | Backend.Friend.* API 호출, JSON 파싱, `Friend` 객체 반환 | 캐싱, 이벤트 발행 |
| **Manager** | Repo 호출, 캐시 관리, 이벤트 발행, 실시간 알림 연동 | SDK 직접 호출, JSON 파싱 |

---

## 뒤끝 Friend API 요약

| 기능 | 메서드 | 성공 코드 |
|------|--------|----------|
| 친구 요청 | `Backend.Friend.RequestFriend(gamerIndate)` | 204 |
| 요청 수락 | `Backend.Friend.AcceptFriend(gamerIndate)` | 204 |
| 요청 거절 | `Backend.Friend.RejectFriend(gamerIndate)` | 204 |
| 요청 취소 | `Backend.Friend.RevokeSentRequest(gamerIndate)` | 204 |
| 친구 삭제 | `Backend.Friend.BreakFriend(gamerIndate)` | 204 |
| 친구 목록 | `Backend.Friend.GetFriendList(limit, offset)` | 200 |
| 받은 요청 목록 | `Backend.Friend.GetReceivedRequestList(limit, offset)` | 200 |
| 보낸 요청 목록 | `Backend.Friend.GetSentRequestList(limit, offset)` | 200 |

**주의사항:**
- `RequestFriend`는 `gamerIndate` 기반 → 닉네임 검색 후 inDate 조회가 선행되어야 함
- 최대 친구 수 초과 시 412 에러: `"maxRequestedGamerFriend"` (상대 초과), `"maxGamerFriend"` (내 초과)

---

## 뒤끝 실시간 알림 (`Backend.Notification`)

### 서버 연결
로그인 완료 후 1회 호출. 서버가 접속 상태를 자동 추적하므로 heartbeat 불필요.

```csharp
Backend.Notification.Connect();
Backend.Notification.OnAuthorize = (bool result, string reason) => { };
```

### 이벤트 핸들러

| 이벤트 | 핸들러 | 설명 |
|--------|--------|------|
| 접속 여부 응답 | `OnIsConnectUser` | `UserIsConnectByIndate()` 호출에 대한 응답 |
| 친구 접속 | `OnFriendConnected` | 친구가 게임에 접속하면 자동 호출 |
| 친구 종료 | `OnFriendDisconnected` | 친구가 게임을 종료하면 자동 호출 |
| 친구 요청 도착 | `OnFriendRequestReceived` | 다른 유저가 나에게 친구 요청을 보냈을 때 |
| 친구 요청 수락 | `OnFriendRequestAccepted` | 내가 보낸 요청이 수락되었을 때 |
| 친구 요청 거절 | `OnFriendRequestRejected` | 내가 보낸 요청이 거절되었을 때 |

### 접속 상태 조회 흐름

```
1. 친구 패널 열릴 때 → 각 Friend의 inDate로 UserIsConnectByIndate() 일괄 호출
2. OnIsConnectUser 핸들러에서 Friend.UpdateStatus() → UI 갱신
3. 이후 친구 접속/종료 시 → 실시간 이벤트가 자동으로 상태 갱신 (폴링 불필요)
```

---

## 프로필 아웃핏 (Firebase 유지)

Firestore `Inventory/{uid}`에서 장착 아이템 조회. 기존 `ItemStorageRepo` 저장 흐름 그대로 활용.

```
친구의 nickname → AccountManager로 UID 조회
→ Firestore "Inventory/{uid}" 에서 Equipments 읽기
→ Friend.UpdateOutfitIds(idList) → ProfileSkin.Init(itemIds)
```

---

## UI 구조

```
┌─────────────────────────────────────┐
│  [상단] 내 프로필 영역               │
│  프로필 이미지 | 닉네임#태그 | N / 50 │
├─────────────────────────────────────┤  ← Mask 영역
│  [초대 슬롯] (비활성 상태, 초대 시 활성)│
│  [친구 리스트 헤더] 친구 ── N / 50   │
│  [친구 목록 ScrollView]              │
│  │ 프로필 | 닉네임#태그 | ● 온라인   │
│  │ 프로필 | 닉네임#태그 | ● 오프라인  │
├─────────────────────────────────────┤
│  [하단] 친구 추가 버튼               │
└─────────────────────────────────────┘
```

**상태 색상:** Online → 연두색 `(0.56, 0.93, 0.56)` / Offline → 짙은 회색 `(0.41, 0.41, 0.41)`
**정렬:** 온라인 → 오프라인

---

## Phase별 작업 항목

### Phase 1: 도메인 + 데이터 계층

- [x] `Friend.cs` — 도메인 Entity (MAX_FRIEND_COUNT=50, 필드, 검증, ToDTO)
- [x] `FriendDTO.cs` — 읽기전용 DTO
- [x] `EFriendStatus.cs` — `{ Online, Offline }`
- [x] `FriendRepository.cs` — 뒤끝 API 래핑, JSON 파싱
- [x] `FriendManager.cs` — DontDestroySingleton, CRUD, 캐시, 이벤트
- [x] Legacy 정리 — 미사용 클래스를 `Legacy/` 폴더로 이동, 활성 코드만 남기기

### Phase 2: UI 구현

- [x] `UI_FriendList.cs` — 내 프로필, 친구 수 헤더, 레이아웃 (RectMask2D + VLG)
- [x] `UI_InvitePanel.cs` / `UI_InviteSlot.cs` — 초대 패널
- [x] `UI_PanelFriendSlot.cs` — 프로필 이미지, 상태 색상, 정렬
- [x] 친구 추가 버튼 (50명 시 비활성)

### Phase 3: 친구 추가 / 요청 팝업

- [x] `UI_PanelFriendAdd.cs` — 닉네임 검색 → inDate 조회 → RequestFriend
- [x] `UI_PanelFriendAccept.cs` — 받은 요청 목록, 수락/거절
- [x] 412 에러 처리 (최대 인원 초과 팝업)

### Phase 4: 채팅 연동

- [ ] 로비/대기방: 친구 슬롯 클릭 → 1:1 채팅 (토글)
- [ ] 인게임: 기존 채팅에 탭 형태로 통합 (Tab 키 전환)

### Phase 5: 뒤끝 실시간 알림 연동

- [x] `Backend.Notification.Connect()` — 로그인 후 연결 (`FriendManager.ConnectNotification()`)
- [x] 친구 접속/종료 이벤트 핸들러 → `Friend.UpdateStatus()` → UI 갱신
- [x] 친구 요청 도착/수락/거절 이벤트 핸들러
- [x] 친구 패널 열 때 `UserIsConnectByIndate()` 일괄 조회 (`QueryAllFriendConnectionStatus()`)
- [x] `FriendStatusUpdater.cs` 제거 → `Legacy/` 폴더로 이동

---

## 작업 의존 관계

```
Phase 1 (DDD 기반)
  ├→ Phase 2 (UI)
  ├→ Phase 3 (친구 추가 팝업) — Phase 2와 병행 가능
  └→ Phase 5 (실시간 알림)
        │
Phase 2 → Phase 4 (채팅)
```
