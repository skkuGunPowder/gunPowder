# [KKH] RecoilAmount 적용 작업내역서

> 작업 브랜치: `KKH_Test`
> 작성일: 2026-03-23

---

## 1. 변경점 요약

### 개요

폭탄 발사 시 반동(Recoil) 강도가 **하드코딩 상수**로 고정되어 있던 것을, **뒤끝 차트(BombCSV)의 폭탄별 `RecoilAmount` 값**을 직접 참조하도록 변경하였습니다.

| 구분 | 이전 | 현재 |
|------|------|------|
| **반동 수치 참조** | 하드코딩 상수 (`NORMAL_RECOIL_FORCE=10f`, `STRONG_RECOIL_FORCE=20f`) | `BombStat.RecoilAmount` (폭탄 데이터 테이블) |
| **폭탄별 차이** | 모든 폭탄 동일 반동 | 폭탄마다 다른 반동 적용 가능 |
| **일반/특수 폭탄 구분** | 동일 상수 사용 | `BasicBombStat` / `SpecialBombStat` 각각의 RecoilAmount 참조 |

---

## 2. 수정 파일

**수정 파일: 1개** — `Assets/02.Scripts/Player/State/PlayerBaseState.cs`

---

## 3. 상세 변경 내역

### 3-1. 하드코딩 상수 제거

| 이전 | 현재 | 비고 |
|------|------|------|
| `NORMAL_RECOIL_FORCE = 10f` | 삭제 | `bombStat.RecoilAmount`로 대체 |
| `STRONG_RECOIL_FORCE = 20f` | 삭제 | `bombStat.RecoilAmount`로 대체 |
| `Y_RECOIL_FORCE = 5f` | 유지 | 수직 반동은 차트 데이터에 없으므로 기존 상수 유지 |

### 3-2. 메서드 시그니처 변경

| 메서드 | 이전 시그니처 | 현재 시그니처 |
|--------|-------------|-------------|
| `ExecuteBombAction` | `(string, Transform, BombActionType)` | `(string, Transform, BombActionType, BombStat)` |
| `PlayBombAnimation` | `(BombActionType, Transform)` | `(BombActionType, Transform, BombStat)` |

### 3-3. 호출부 변경

| 호출 위치 | 이전 | 현재 |
|-----------|------|------|
| `HandleNormalBomb` → `ExecuteBombAction` | `(prefabName, bombSpawnPoint, action)` | `(prefabName, bombSpawnPoint, action, _owner.BasicBombStat)` |
| `HandleSpecialBomb` → `ExecuteBombAction` | `(prefabName, bombSpawnPoint, action)` | `(prefabName, bombSpawnPoint, action, _owner.SpecialBombStat)` |

### 3-4. 반동 적용부 변경

| BombActionType | 이전 | 현재 |
|----------------|------|------|
| `ThrowStraight` | `ApplyRecoil(bombSpawnPoint, STRONG_RECOIL_FORCE, Y_RECOIL_FORCE)` | `ApplyRecoil(bombSpawnPoint, bombStat.RecoilAmount, Y_RECOIL_FORCE)` |
| `Throw` | `ApplyRecoil(bombSpawnPoint, NORMAL_RECOIL_FORCE, Y_RECOIL_FORCE)` | `ApplyRecoil(bombSpawnPoint, bombStat.RecoilAmount, Y_RECOIL_FORCE)` |
| `Place` | 반동 없음 | 변경 없음 |
| `Boost` | 반동 없음 | 변경 없음 |

---

## 4. 데이터 흐름

```
[뒤끝 차트] BombCSV.csv → RecoilAmount 컬럼
        ↓
[ItemDatabaseRepo.cs] GetChartContents → new BombStat(json)
        ↓
[BombStat.cs] float.Parse(json["RecoilAmount"]) → BombStat.RecoilAmount
        ↓
[Player.cs] BasicBombStat / SpecialBombStat에 저장
        ↓
[PlayerBaseState.cs] HandleNormalBomb → ExecuteBombAction(bombStat) → PlayBombAnimation(bombStat)
                     HandleSpecialBomb → ExecuteBombAction(bombStat) → PlayBombAnimation(bombStat)
        ↓
[PlayerBaseState.cs] ApplyRecoil(bombSpawnPoint, bombStat.RecoilAmount, Y_RECOIL_FORCE)
```

---

## 5. 영향 범위

- `ExecuteBombAction`, `PlayBombAnimation`을 override하는 자식 클래스: **없음** (확인 완료)
- `NORMAL_RECOIL_FORCE`, `STRONG_RECOIL_FORCE`를 외부에서 참조하는 코드: **없음** (확인 완료)
- `ApplyRecoil` 메서드 자체의 시그니처: **변경 없음**

---

## 6. 확인 필요 사항

| 항목 | 내용 |
|------|------|
| 뒤끝 차트 값 확인 | 각 폭탄의 `RecoilAmount`에 적절한 수치가 입력되어 있는지 확인 필요 |
| Throw vs ThrowStraight 차등 | 현재 둘 다 동일한 `RecoilAmount`를 사용 — 차등이 필요하면 차트에 컬럼 추가 또는 코드에서 배율 적용 |
