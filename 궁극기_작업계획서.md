# 궁극기 시스템 리워크 작업계획서

> 작성일: 2026-03-23
> 브랜치: KKH_Test

---

## 1. 현재 시스템 vs 신규 기획 비교

| 항목 | 현재 시스템 | 신규 기획 |
|------|------------|----------|
| 발동 조건 | HP가 임계치(30) 이하로 떨어지면 기회 발생 | 궁극기 게이지 100 충전 시 사용 가능 |
| 게이지 | 없음 (bool 기반 on/off) | 0~100 게이지 (총량 외부 변동 가능) |
| 충전 방식 | 없음 (HP 임계치 트리거) | 획득 GP의 30% + 잃은 GP의 50% |
| 사용 제한 | 생애 1회 (`_hasUsedUltimateThisLife`) | 제한 없음 (게이지 충전 시마다 사용 가능) |
| 사용 후 처리 | 즉시 기회 소멸 | 사용 후 10초 뒤 게이지 0 초기화 |
| 시간 제한 | 10초 내 미사용 시 기회 소멸 | 없음 (충전 완료 시 유지) |
| 라운드 종료 시 | - | 10초 카운트다운 초기화 (게이지 유지) |
| 입력 키 | C키 (유지) | C키 (유지) |
| 사용 폭탄 | X슬롯(특수폭탄)의 궁극기 | **최종: Z슬롯 폭탄의 궁극기** (현재 테스트용: X슬롯 유지) |

---

## 2. 궁극기 폭탄 슬롯 전환 계획 (X슬롯 → Z슬롯)

### 2.1 현재 상황
- **Z키**: 기본폭탄 고정 (ID: "BO0001", 교체 불가)
- **X키**: 특수폭탄 (장착 교체 가능, `EItemType.Bomb`)
- **C키(궁극기)**: X키에 장착된 특수폭탄의 궁극기를 사용

### 2.2 최종 기획 목표
- **Z키**: 폭탄 교체 가능하도록 변경 (추후 작업)
- **C키(궁극기)**: Z키에 장착된 폭탄의 궁극기를 사용

### 2.3 현재 작업 방침
- Z키 폭탄 교체 기능이 아직 없으므로, **테스트를 위해 X슬롯(특수폭탄) 궁극기를 그대로 유지**
- 추후 Z슬롯 전환 시 수정이 필요한 코드에 **`// TODO: [궁극기 슬롯 전환] Z슬롯 폭탄 교체 구현 후, 이 부분을 Z슬롯 기반으로 변경`** 주석을 남김

### 2.4 추후 수정 필요 지점 (주석 대상)

| 파일 | 위치 | 현재 코드 | 추후 변경 방향 |
|------|------|----------|--------------|
| `Player.cs` | `RPC_LoadItems()` 내 궁극기 로딩 | `EquipedItemDict[EItemType.Bomb].ID`로 궁극기 결정 | Z슬롯 아이템 ID 기반으로 변경 |
| `Player.cs` | `BASIC_BOMB_ID` 상수 정의 | 고정값 `"BO0001"` | Z슬롯 교체 가능하도록 변경 |
| `UltimateManager.cs` | `GetUltimate(bombID)` 호출부 | X슬롯 bombID 전달 | Z슬롯 bombID 전달 |

---

## 3. 수정 대상 파일 및 작업 내역

### 3.1 PlayerStat.cs
**경로**: `Assets/02.Scripts/Player/PlayerStat.cs`

**변경 사항:**
- `_hasUltimateChance` (bool) → 제거
- `_hasUsedUltimateThisLife` (bool) → 제거
- `_ultimateChanceDuration` (float) → 제거
- `_ultimateTriggerThreshold` (int) → 제거

**추가 사항:**
- `_currentUltimateGauge` (float): 현재 충전된 궁극기 게이지 (0~100)
- `_maxUltimateGauge` (float): 궁극기 게이지 총량 (기본 100, 외부 변동 가능)
- `IsUltimateReady` (bool, 프로퍼티): `_currentUltimateGauge >= _maxUltimateGauge`
- `IncreaseUltimateGauge(float amount)`: 게이지 증가 (최대치 클램프)
- `ResetUltimateGauge()`: 게이지 0 초기화
- `SetMaxUltimateGauge(float max)`: 외부 환경에 의한 총량 변경

**GP 변동 시 게이지 충전 로직 연동:**
- `DecreaseHP()` 내부에서 잃은 GP의 50%만큼 `IncreaseUltimateGauge` 호출
- GP 획득 관련 메서드에서 획득 GP의 30%만큼 `IncreaseUltimateGauge` 호출

---

### 3.2 PlayerUltimateController.cs
**경로**: `Assets/02.Scripts/Player/PlayerUltimateController.cs`

**변경 사항:**

#### UpdateUltimateChanceTimer() → 리워크
- 기존: 10초 타이머로 기회 만료 체크
- 변경: 궁극기 **사용 후** 10초 카운트다운 관리
  - `_postUltimateTimer` (float): 사용 후 경과 시간
  - `_isPostUltimateCooldown` (bool): 10초 대기 중 여부
  - 10초 경과 시 `PlayerStat.ResetUltimateGauge()` 호출

#### ExecuteUltimate() → 수정
- 기존 조건: `HasUltimateChance && !HasUsedUltimateThisLife && HP >= Cost`
- 변경 조건: `IsUltimateReady && !_isPostUltimateCooldown`
- 사용 후: `_isPostUltimateCooldown = true`, 타이머 시작
- `_hasUsedUltimateThisLife` 관련 로직 제거

#### SetUltimateEffectState() → 수정
- 게이지 100 도달 시 이펙트 활성화
- 사용 후 이펙트 비활성화

#### 라운드 종료 처리 추가
- `OnRoundEnd()`: 10초 카운트다운 초기화 (`_isPostUltimateCooldown = false`, 타이머 리셋)
- EventManager의 게임 종료 이벤트에 구독

---

### 3.3 Player.cs
**경로**: `Assets/02.Scripts/Player/Player.cs`

**변경 사항:**

#### GP 감소 시 게이지 충전
- `RPC_TakeDamage` 또는 `DecreaseGunPowderCount` 호출 부분에서 잃은 GP × 0.5 만큼 게이지 충전
- 건파우더 자동 감소 시에도 동일 적용

#### GP 획득 시 게이지 충전
- `RPC_RequestIncreaseGunPowder` 등 GP 획득 로직에서 획득 GP × 0.3 만큼 게이지 충전

#### 기존 궁극기 트리거 로직 제거
- HP 임계치 기반 `_hasUltimateChance` 설정 코드 제거

---

### 3.4 UI_Ultimate.cs
**경로**: `Assets/02.Scripts/UI/UI_Ultimate.cs`

**변경 사항:**
- 기존: "Press C" 깜빡임 표시 (기회 발생/만료 이벤트 기반)
- 변경: 궁극기 게이지 바를 `UI_InGameProfileSlot`(0번 슬롯)에서 런타임에 참조
  - `_gaugeBarFill`을 SerializeField에서 제거 → 런타임에 `UI_InGameProfile.LocalPlayerSlot`에서 가져옴
  - 게이지 충전량 실시간 표시 (0~100)
  - 게이지 100 도달 시 "Press C" 활성화 + 깜빡임 (기존 로직 재활용)

**이벤트 구독 변경:**
- `OnUltimateChanceActivated` → 게이지 100 도달 시 발동
- `OnUltimateChanceDeactivated` → 궁극기 사용 시 비활성화
- `OnUltimateGaugeChanged(float current, float max)` 구독하여 게이지 바 업데이트

### 3.6 UI_InGameProfileSlot.cs
**경로**: `Assets/02.Scripts/Map/UI/UI_InGameProfileSlot.cs`

**추가 사항:**
- `_ultimateGaugeBarFill` (Image): 궁극기 게이지 바 Fill 이미지 추가
- `UltimateGaugeBarFill` (public 프로퍼티): 외부 참조용

### 3.7 UI_InGameProfile.cs
**경로**: `Assets/02.Scripts/Map/UI/UI_InGameProfile.cs`

**추가 사항:**
- `LocalPlayerSlot` (public 프로퍼티): 로컬 플레이어 슬롯(index 0) 접근자
- `Init()`에서 로컬 플레이어(index 0)만 게이지 바 `SetActive(true)`, 나머지는 `SetActive(false)`

---

### 3.5 EventManager.cs
**경로**: `Assets/02.Scripts/Evenet/EventManager.cs`

**추가 사항:**
- `OnUltimateGaugeChanged` 이벤트 추가 (float current, float max)
- 게이지 변동 시 UI 업데이트용

---

## 4. 작업 순서

### Phase 1: 데이터 레이어 (PlayerStat.cs)
1. 기존 궁극기 bool 필드 제거
2. 궁극기 게이지 필드/프로퍼티 추가
3. 게이지 증가/초기화/총량변경 메서드 추가
4. GP 감소 메서드 내 게이지 충전 로직 추가 (잃은 GP × 50%)

### Phase 2: 컨트롤러 레이어 (PlayerUltimateController.cs)
1. 기존 타이머 로직을 사용 후 10초 카운트다운으로 변경
2. `ExecuteUltimate()` 조건 수정
3. 이펙트 활성화/비활성화 조건 수정
4. 라운드 종료 시 카운트다운 초기화 로직 추가

### Phase 3: 플레이어 연동 (Player.cs)
1. GP 감소 시 게이지 충전 연동
2. GP 획득 시 게이지 충전 연동
3. 기존 HP 임계치 기반 궁극기 트리거 코드 제거
4. 게이지 100 도달 시 이펙트 활성화 연동
5. **궁극기 슬롯 전환 주석 추가** — 추후 Z슬롯 전환 시 수정 필요한 코드에 TODO 주석 삽입
   - `Player.cs` → `RPC_LoadItems()` 내 `EquipedItemDict[EItemType.Bomb].ID` 사용부
   - `Player.cs` → `BASIC_BOMB_ID` 상수 정의부
   - `UltimateManager.cs` → `GetUltimate()` 호출부

### Phase 4: 이벤트 & UI
1. EventManager에 게이지 변동 이벤트 추가
2. UI_Ultimate에 게이지 바 업데이트 로직 추가
3. 기존 "Press C" 깜빡임은 게이지 100 도달 조건으로 변경

### Phase 5: 네트워크 동기화 확인
1. 게이지 충전은 소유자(Owner) 클라이언트에서만 계산
2. 궁극기 사용 RPC는 기존 로직 유지
3. 게이지 값 자체는 로컬 계산 (GP 변동 RPC와 연동되므로 별도 동기화 불필요)

---

## 5. 주의사항

### 5.1 하위 호환성
- `ForceUltimateChance()` (디버그/튜토리얼용) → 게이지를 max로 설정하는 방식으로 변경
- `_hasUsedUltimateThisLife` 제거에 따라 튜토리얼 로직 확인 필요

### 5.2 네트워크
- 게이지 충전은 로컬에서 계산하되, GP 변동 자체가 RPC로 동기화되므로 결과적으로 일관성 유지
- 궁극기 사용 시 기존 RPC (`Rpc_UltimateProduction`, `RPC_UltimateEffect`) 그대로 활용

### 5.3 외부 환경에 의한 총량 변동
- `_maxUltimateGauge`를 변경할 수 있는 인터페이스 제공
- 현재 충전량이 새 총량을 초과할 경우 클램프 처리

### 5.4 게이지 오버플로우 방지
- 게이지가 이미 100(max)인 상태에서 추가 충전 시 무시
- "궁극기 게이지를 총량만큼 충전했을 땐, 더 이상 충전되지 않는다" 기획 반영

---

## 6. 수정 파일 요약

| 파일 | 작업 유형 | 우선순위 |
|------|----------|---------|
| `PlayerStat.cs` | 필드 교체 + 메서드 추가 | Phase 1 |
| `PlayerUltimateController.cs` | 핵심 로직 리워크 | Phase 2 |
| `Player.cs` | 연동 로직 수정 | Phase 3 |
| `EventManager.cs` | 이벤트 추가 | Phase 4 |
| `UI_Ultimate.cs` | 게이지 바 참조를 슬롯에서 가져오도록 변경 | Phase 4 |
| `UI_InGameProfileSlot.cs` | 게이지 바 Image 필드 추가 | Phase 4 |
| `UI_InGameProfile.cs` | 로컬 슬롯 접근자 + 게이지 active 관리 | Phase 4 |
| `UltimateManager.cs` | TODO 주석 추가 | Phase 3 |

---

## 7. 궁극기 게이지 타 클라이언트 동기화 (로컬 계산 방식)

### 7.1 배경
- 현재 게이지 충전은 소유자(Owner) 클라이언트에서만 계산됨
- 다른 플레이어의 궁극기 게이지를 보려면 동기화가 필요
- 2D 실시간 대전 액션 게임 특성상 RPC 호출이 빈번하므로, 게이지 전용 RPC 추가는 비효율적

### 7.2 방식: 수신 측 로컬 계산

별도 RPC 없이, **이미 동기화되고 있는 HP 변동 RPC를 활용**하여 각 클라이언트가 로컬에서 게이지를 계산한다.

**원리:**
- `RPC_ChangeHP`는 모든 클라이언트에서 수신됨 (HP, Life, attacker 정보 포함)
- 수신 측에서 **이전 HP와 새 HP를 비교**하여 차이를 구함
- HP 감소 → 손실량의 50% 게이지 충전
- HP 증가 → 획득량의 30% 게이지 충전
- 추가 네트워크 비용: **0** (기존 RPC에 편승)

### 7.3 수정 대상

| 파일 | 변경 내용 |
|------|----------|
| `PlayerStat.cs` | `RPC_ChangeHP` 수신 시 이전 HP와 비교하여 게이지 충전 로직 추가. 소유자가 아닌 클라이언트에서도 `_currentUltimateGauge` 갱신 |
| `PlayerStat.cs` | `_previousHP` 필드 추가 (이전 HP 저장용, RPC 수신 시 비교 기준) |
| `PlayerStat.cs` | 게이지 충전 로직을 소유자 전용(`IsMine`) 체크 밖으로 이동하거나, RPC 수신 핸들러에서 별도 호출 |
| `UI (추후)` | 다른 플레이어의 게이지를 표시할 UI 위치 확정 후 연동 |

### 7.4 주의사항

- **소유자 클라이언트에서는 기존 로직 유지** — `DecreaseHP`/`IncreaseHP` 내부에서 직접 충전 (현재 구현)
- **비소유자 클라이언트에서는 RPC 수신 시점에 계산** — `RPC_ChangeHP` 핸들러에서 HP 차이 기반 충전
- 양쪽 모두 같은 비율(30%/50%)로 계산하므로 결과가 동일함
- 궁극기 사용 후 게이지 리셋은 이펙트 RPC(`UltimateEffect(false)`)와 연동하여 처리
- 라운드 시작/부활 시 `_previousHP` 초기값 동기화 필요
