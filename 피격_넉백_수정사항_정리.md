# 피격 / 넉백 시스템 수정사항 정리

> 수정 완료일: 2026-03-16
> 대상 브랜치: KKH_Test

---

## 1. 변경사항 요약

| # | 항목 | 변경 전 | 변경 후 | 상태 |
|---|------|--------|--------|------|
| 1 | 피격 최대 시간 소스 | `PlayerStat.DamagedTime` (모든 폭발 동일 고정값) | 폭발별 `ExplosionStat.MaxStunTime` | ✅ 완료 |
| 2 | MaxStunTime 전달 경로 | 없음 | Explosion → Player → DamagedState 전달 체인 구축 | ✅ 완료 |
| 3 | 무적 시간 | 피격 상태 **전체** = 무적 | 피격 시간의 **25%만** 무적 | ✅ 완료 |
| 4 | 바닥 착지 조기 전환 | 피격 중 바닥 닿으면 조기 전환 가능성 | 피격 시간 완료 전 조기 전환 **없음** | ✅ 완료 |
| 5 | 피격 횟수 추가 데미지 | 피격 횟수 누적 → 15마다 +1 데미지 | **삭제** | ✅ 완료 |
| 6 | 히트스탑 중 넉백 | Rigidbody 속도 저장/복원 방식 | 마지막 폭발 정보 저장 → 직접 적용 방식 | ✅ 완료 |

---

## 2. 각 항목 상세 비교

### 2-1. 피격 최대 시간 소스 교체

**변경 전:**
```
모든 폭발 → 동일한 고정값 (PlayerStat.DamagedTime)
예: 기본폭탄이든 미사일이든 피격 시간이 같았음
```

**변경 후:**
```
각 폭발 → 자신의 MaxStunTime 전달 (ExplosionStat.MaxStunTime)
예: 기본폭탄 0.5초, 미사일 0.8초 등 폭발마다 다른 피격 시간
```

**작동 원리:**
```
[Explosion.Explode()]
  → damagable.TakeDamage(..., _stat.MaxStunTime, ...)
    → [Player.TakeDamage()]
      → [PlayerDamageController.RPC_TakeDamage()]
        → [Player.RegisterHitDamage(damage, maxDamage, maxStunTime)]
          → _lastMaxStunTime에 저장
            → [PlayerDamagedState.CalculateActualDamagedTime()]
              → _owner.LastMaxStunTime 사용
```

**관련 파일:**
- `IDamagable.cs` — 인터페이스에 `maxStunTime` 파라미터 추가
- `Explosion.cs` — `_stat.MaxStunTime` 전달
- `Player.cs` — `_lastMaxStunTime` 필드 및 `LastMaxStunTime` 프로퍼티
- `PlayerDamageController.cs` — RPC 파라미터로 전달
- `PlayerDamagedState.cs` — `CalculateActualDamagedTime()`에서 사용

---

### 2-2. 거리에 따른 피격 시간 계산

**변경 전:**
```csharp
// PlayerDamagedState.CalculateActualDamagedTime()
float baseDamagedTime = _owner.PlayerStat.DamagedTime;  // 고정값
_actualDamagedTime = Lerp(baseDamagedTime * 0.3f, baseDamagedTime, damageRatio);
```

**변경 후:**
```csharp
// PlayerDamagedState.CalculateActualDamagedTime()
float baseDamagedTime = _owner.LastMaxStunTime;  // 폭발별 MaxStunTime
_actualDamagedTime = Lerp(baseDamagedTime * 0.3f, baseDamagedTime, damageRatio);
```

**거리 보정 공식 (동일 유지):**
```
가까이서 맞음 (damageRatio = 1.0) → 피격시간 = MaxStunTime × 100%
멀리서 맞음   (damageRatio = 0.0) → 피격시간 = MaxStunTime × 30%
```

---

### 2-3. 무적 시간 교체

**변경 전:**
```
피격 시작 ──── 무적 ON ────────────────── 무적 OFF ── 피격 종료
              ↑ 피격 상태 전체가 무적 ↑
              (추가 피격 불가능)
```

**변경 후:**
```
피격 시작 ── 무적 ON ── 무적 OFF ──────────────────── 피격 종료
             ↑ 25% ↑    ↑ 이후 추가 피격 가능 ↑
```

**작동 원리:**
```csharp
// PlayerDamagedState.InitializeDamaged()
SetImmuneState(true);                              // 피격 시작 = 무적 ON
_immuneTime = _actualDamagedTime * 0.25f;          // 무적 지속 = 피격시간의 25%

// PlayerDamagedState.MineUpdate()
if (_isImmuneActive && _damagedTimer >= _immuneTime)
{
    SetImmuneState(false);                         // 25% 경과 = 무적 OFF
}
```

**예시:**
```
피격시간 = 1.0초일 때
  → 무적시간 = 0.25초
  → 0.25초 이후에는 추가 피격 가능
```

---

### 2-4. 바닥 착지 조기 전환 제거

**변경 전:**
```
(피격 중 바닥에 닿으면 조기에 상태 전환될 가능성이 있었음)
```

**변경 후:**
```csharp
// PlayerDamagedState.MineUpdate()
// 피격 시간이 완전히 끝난 후에만 상태 전환
if (_damagedTimer >= _actualDamagedTime)   // ← 반드시 타이머 완료 체크가 먼저
{
    if (IsGrounded2D())
        → Idle 상태
    else
        → Fall 상태
}
```

**핵심:** 피격 시간이 끝나기 전에는 어떤 경우에도 상태 전환하지 않음

---

### 2-5. 피격 횟수 기반 추가 데미지 삭제

**변경 전:**
```csharp
// PlayerDamageController.RPC_TakeDamage() 내부
_playerStat.IncreseDamagedCount();
int increaseDamagePerDamagedCount = _playerStat.CurrentPlayerDamagedCount / 15;
damage += increaseDamagePerDamagedCount;
maxDamage += increaseDamagePerDamagedCount;

// 예: 30번 맞으면 → 30/15 = +2 추가 데미지
```

**변경 후:**
```
해당 시스템 완전 삭제
→ 데미지는 폭발의 순수 AttackPower와 거리 계산만으로 결정
```

---

### 2-6. 히트스탑 중 마지막 폭발 넉백 (핵심 변경)

이 항목이 가장 큰 구조적 변경입니다.

#### 변경 전: 속도 저장/복원 방식

```
[첫 피격]
  Explosion.AddExplosionForce2D() → Rigidbody에 힘 적용 → 속도 발생
  InitializeHitStop()
    → StoreCurrentVelocityAndFreeze()
      → _storedVelocity = rigidbody.velocity   ← 현재 속도 저장
      → rigidbody.velocity = 0                  ← 정지

[히트스탑 중 추가 피격]
  Explosion.AddExplosionForce2D() → Rigidbody에 힘 적용
  OnAdditionalHit()
    → UpdateStoredVelocity()
      → _storedVelocity = rigidbody.velocity   ← ⚠️ 문제: 이미 0으로 초기화된 상태!
      → rigidbody.velocity = 0                  ←    새 폭발 힘이 소실될 수 있음

[히트스탑 완료]
  OnHitStopComplete()
    → RestoreStoredVelocity()
      → rigidbody.velocity = _storedVelocity   ← 저장된 속도 복원 (0일 수 있음)
```

**문제점:**
- AddExplosionForce2D가 힘을 가하지만, 물리 엔진이 속도에 반영하기 전에 UpdateStoredVelocity()가 호출되면 속도가 0으로 저장됨
- 결과: 히트스탑 후 넉백이 약하거나 없을 수 있음

#### 변경 후: 폭발 정보 직접 저장 방식

```
[첫 피격]
  Explosion.AddExplosionForce2D()
    → Rigidbody에 힘 적용
    → player.StoreLastExplosionInfo(force, position, radius)  ← 폭발 정보 별도 저장
  InitializeHitStop()
    → FreezePlayer()
      → rigidbody.velocity = 0   ← 단순 정지 (속도 저장 안 함)

[히트스탑 중 추가 피격]
  Explosion.AddExplosionForce2D()
    → player.StoreLastExplosionInfo(force, position, radius)  ← 새 폭발 정보로 덮어쓰기
  OnAdditionalHit()
    → FreezePlayer()
      → rigidbody.velocity = 0   ← 단순 정지

[히트스탑 완료]
  OnHitStopComplete()
    → ApplyLastExplosionForce()   ← 저장된 폭발 정보로 직접 힘 계산 & 적용
      → direction = playerPos - explosionPos
      → forceMag = force × (1 - distance/radius)
      → rigidbody.AddForce(direction × forceMag)
```

**개선점:**
- 폭발 정보(힘, 위치, 반경)를 별도 저장하므로 타이밍 이슈 없음
- 마지막 폭발 정보가 항상 정확하게 보존됨
- 히트스탑 완료 시 저장된 정보로 넉백 힘을 직접 계산하여 적용

#### 변경된 파일 & 코드

**Player.cs — 폭발 정보 저장 필드 추가:**
```csharp
private float _lastExplosionForce;
private Vector3 _lastExplosionPosition;
private float _lastExplosionRadius;
private bool _hasLastExplosionInfo;

void StoreLastExplosionInfo(float force, Vector3 position, float radius)  // 저장
void ApplyLastExplosionForce()                                            // 적용
void ClearLastExplosionInfo()                                             // 초기화
```

**Explosion.cs / WaterMissile.cs — AddExplosionForce2D():**
```csharp
// 힘 적용 후 폭발 정보 저장 추가
rb.AddForce(direction * forceMagnitude, ForceMode2D.Impulse);
if (player != null)
{
    player.StoreLastExplosionInfo(explosionForce, explosionPosition, explosionRadius);
}
```

**PlayerDamagedState.cs — 메서드 변경:**
```
StoreCurrentVelocityAndFreeze() → FreezePlayer()        (속도 저장 제거, 단순 정지만)
UpdateStoredVelocity()          → FreezePlayer()        (동일)
RestoreStoredVelocity()         → ApplyLastExplosionForce()  (속도 복원 → 폭발 힘 직접 적용)
```

---

## 3. 유지된 항목 (변경 없음)

| 항목 | 설명 |
|------|------|
| 기본 넉백 | `Explosion.AddExplosionForce2D()` — 거리 비례 감소하는 힘 |
| 추가 넉백 힘 | `(1 - 체력비율) × 10 × 거리보정` — 체력 낮을수록 더 날아감 |
| UPWARD_FORCE(10) | 추가 넉백 시 위쪽으로 10의 힘 (기획 확인 필요) |
| CalculateStartDamping 하드코딩 | `damageRatio = 1.0f` (기획 확인 필요) |

---

## 4. 미결 사항 (기획 확인 필요)

### G-1: CalculateStartDamping()의 damageRatio 하드코딩
```csharp
// PlayerDamagedState.cs line 483
damageRatio = 1.0f;  // ← 거리 보정이 무효화됨
```
- 현재: 거리와 관계없이 감쇠값이 동일 (멀리서 맞아도 가까이서 맞은 것과 같은 감속)
- 질문: 멀리서 맞으면 빨리 멈추도록 할지? (이 줄 제거 시 거리 보정 활성화)

### G-2: UPWARD_FORCE(10)
```csharp
// PlayerDamagedState.cs line 36
private const float UPWARD_FORCE = 10f;
```
- 추가 넉백 시 항상 위쪽으로 10의 힘이 추가됨
- 질문: 의도된 동작인지? 값 조정이나 제거가 필요한지?

---

## 5. 전체 피격 흐름도 (변경 후)

```
[폭발 발생]
  │
  ├─ AddExplosionForce2D()  → Rigidbody에 기본 넉백 힘 적용
  │                         → Player에 폭발 정보 저장 (히트스탑용)
  │
  └─ TakeDamage(damage, maxDamage, maxStunTime, ...)
       │
       ├─ 팀 체크 (같은 팀이면 데미지 0)
       ├─ 체력 감소 (DecreaseGunPowderCount)
       ├─ 건파우더 드랍
       └─ RegisterHitDamage(damage, maxDamage, maxStunTime)
            │
            ├─ _lastDamageRatio 계산 (damage/maxDamage = 거리 비례)
            ├─ _lastMaxStunTime 저장
            └─ OnHit 이벤트 발생
                 │
                 └─ [PlayerBaseState.HandleHit()]
                      │
                      ├─ 슈퍼아머? → 무시
                      │
                      ├─ 건파우더 > 50 → 일반 DamagedState
                      │   │
                      │   └─ InitializeDamaged()
                      │       ├─ 피격시간 = Lerp(MaxStunTime×0.3, MaxStunTime, damageRatio)
                      │       ├─ 무적 ON (피격시간의 25%)
                      │       ├─ 추가 넉백 힘 적용 (체력 비례)
                      │       └─ 감쇠 트윈 시작
                      │
                      └─ 건파우더 ≤ 50 → 히트스탑 DamagedState
                          │
                          ├─ [첫 피격] InitializeHitStop()
                          │   ├─ 히트스탑 시간 계산 (체력 비례 0.4~0.7초)
                          │   ├─ FreezePlayer() (정지)
                          │   └─ 흔들림 효과 시작
                          │
                          ├─ [추가 피격] OnAdditionalHit()
                          │   ├─ 타이머 리셋
                          │   ├─ FreezePlayer() (재정지)
                          │   └─ 폭발 정보는 Explosion에서 이미 덮어씀
                          │
                          └─ [히트스탑 완료] OnHitStopComplete()
                              ├─ ApplyLastExplosionForce() (마지막 폭발 넉백)
                              ├─ 피격시간 계산
                              ├─ 무적 ON (25%)
                              ├─ 추가 넉백 힘 적용
                              └─ 감쇠 트윈 시작

[피격 상태 진행 중 - MineUpdate()]
  │
  ├─ 무적 타이머 체크 → 25% 경과 시 무적 OFF
  │
  └─ 피격 타이머 체크 → 완료 시
       ├─ 바닥 → Idle
       └─ 공중 → Fall
```

---

## 6. 디버그 로그

현재 `[피격시스템]` 접두사 로그가 추가되어 있습니다.
Unity Console에서 `피격시스템`으로 필터링하여 동작을 확인할 수 있습니다.

**확인 완료 후 로그 제거가 필요한 파일:**
- `Player.cs` — `RegisterHitDamage`, `StoreLastExplosionInfo`, `ApplyLastExplosionForce`
- `PlayerDamagedState.cs` — `InitializeDamaged`, `InitializeHitStop`, `MineUpdate`, `OnHitStopComplete`, `OnAdditionalHit`
